# Away-queue — items needing Thomas's sign-off on return

Generated by auto-status away-mode orchestration. Each item is an open question the orchestrator deliberately did NOT decide.

## 2026-05-16 — sub-to-10 directive: minuend-spread rule?

**Context:** During PR #244 dispatch (distractorClass directive reword + re-bake), the orchestrator's brief listed "no dual-exposure of the same minuend across slots" as a composition rule. Kevin verified — that rule does NOT exist in `api/_planner.ts`'s sub-to-10 directive. The actual "DUAL-EXPOSURE RULE" (#5) is about subtraction-fact + its addition-inverse, NOT minuend repetition.

The re-baked sub-to-10 canon (`76a1dfc`) has P4=10-2, P5=10-3, P8=10-7 — all minuend 10. Kevin's pedagogical read: this is allowed and probably _desirable_ (reinforces the make-10 mental model).

**Question for Thomas:** Was the orchestrator's brief paraphrasing imprecisely, or do you want a stricter minuend-spread rule added to the sub-to-10 directive?

- (a) No change — the directive is correct as-is; 3-of-8 minuend-10 facts in one session is fine.
- (b) Add a rule — e.g. "at most 2 facts may share the same minuend per session". Would require a directive-tightening + re-bake.

Source: Kevin's NOF #3 from PR #244 fix-up dispatch (2026-05-16).

**RESOLVED (Thomas, 2026-06-12, sponsor-questions-walkthrough): (a) no change** — directive correct as-is; same-minuend clustering allowed (Kevin's make-10 read). Consistent with Wave 10's "canon at pedagogical ceiling, do NOT re-bake" ruling + voice-QA baseline protection.

---

## 2026-05-16 — Session arc summary (drain-complete)

**7 merges this session:**

- `e659160` PR #239 — sub-to-10 distractor-class-2 stopgap (Jessica, deferred from prior session)
- `180ca25` PR #242 — sub-to-10 visual E2E spec patches (Jessica, deferred from prior session)
- `7de0156` PR #243 — Wave 3.4 `lifetimeFirstEncounters: SkillNode[]` (Kevin + Devon review)
- `35bd95c` PR #244 — distractorClass directive reword + canon re-bake + DOUBLES-CAP SELF-CHECK (Kevin + REQUEST_CHANGES + fix-up + APPROVE)
- `7c4211b` PR #245 — composition-rule-spec lint for sub-to-10 (Kevin + Devon review)
- `ce11985` PR #246 — SUB_TO_TEN_POOL drift-guard (Kevin + Devon review)
- _(no-PR drains)_ — 9-spec forceHowlerUnlock audit + 2-spec directive-injection follow-up (Devon, both NO-FIX-NEEDED)

**Doc deliverables:**

- `testing-and-ci.md` §4.1.2 (audit-9 + directive-injection extensions; Devon), §4.1.3 (Jessica's 3 NOFs: absence-assertion patterns, sub-to-10 DOM seams, chip-walk + silent-MP3 hang), §11 (per-role worktree topology), §12 (dispatch-sentinel hook documentation) — all Jessica
- `planner-and-canon.md` — "Wire shape is utterance-only" invariant (Kevin) + "Canon-lint scope is text-encoding hygiene ONLY" (Kevin) + "Mechanical gate (sub-to-10 today; extensible)" rewrite (orch) + psychological-framing consensus addendum (orch)
- `progress-and-persistence.md` — line 264 `WordSongNode[]` → `SkillNode[]` + helper-API vs storage-shape boundary documentation (orch) + new "E2e returning-user seed pattern" section (Devon)
- `feedback_haiku_directive_sharpening.md` memory — pattern #5 documented (DOUBLES-CAP SELF-CHECK validated empirically via PR #244 fix-up cycle)

**Backlog items remaining (NEED Thomas's directional input to scope):**

- Spec-2 structural redesign — `sub-to-10-distractor-class-2.spec.ts` test 1 + 3 stopgapped with `test.fixme()` per Jessica's PR #239. Proper fix path: (a) bake a sub-to-10 canon variant whose P4 is Class-2-eligible (e.g. `9-1=8` with `a+b=10` in range) + read via `installDigraphsThClaudeMock`-pattern, OR (b) thread `__testInitiallyAudioUnlocked` through Math.tsx. Decision needs Thomas's canon-bake-budget + plumbing-effort preference.
- Dave canon-pool consult — Kevin NOF #2 from PR #240: canon wrong-op delivery is 1/5 (only `9-1=8` in shipped sub-to-10 canon) vs spec target ≥2/5. Pool-widening toward low-`a+b` discriminate facts is Dave's research domain; Thomas-directional.
- Defaults.ts migration — greenfield-only flip vs migration for existing localStorage. Per `[[project_marian_not_using_yet]]` not blocking; Thomas signals when Marian readiness arrives.
- Forward composition-rule lint to other tiers — add-to-10, add-to-20, digraphs-_, cvc-words-_. TierLintBinding shape from PR #245 is extension-ready. Mechanical work but Thomas-prioritized.

**Drain-complete state reached.** Cron `ff35c71a` continues to tick on `*/15`. No autonomous work remains; orchestrator is standing by for Thomas's next directional input on the backlog above.

---

(Add new items below this line as away-mode generates them.)

## 2026-06-13 — Wave 14 = DEFERRED (Thomas, walkthrough)

Thomas chose to **hold Wave 14 dispatch until Wave 13's tail closes** — i.e. until (a) the 59-clip simple-sentences ear-test passes on production voice-qa.html, and (b) the MJ scene pack lands. Dev team stays idle until then; do NOT dispatch a new wave. Re-open the Wave 14 direction question (candidates: M4.x slow-fact analysis, stop-for-now affordance 86ca7urx1, polish backlog) AFTER the tail closes. Cleanest-sequencing call; not a throughput problem (the tail is sponsor-side work, not dev work).

**RESOLVED (Thomas, 2026-06-14 sponsor-questions-walkthrough): Wave 14 = POLISH BACKLOG.** Scope = PNG-in-SVG Emma → true vector re-trace + the unwired rotateZ tilt + breathing loop (per CLAUDE.md "polish backlog" + emma-character-and-animation.md). Thomas directive (verbatim): "Polish backlog but do save-session first" — run `/save-session` BEFORE dispatching any Wave 14 work; Wave 14 likely resumes in a fresh session.

## 2026-06-12 — Round-6 ear-test (post-#418 merge)

**Context:** PR #418 (voice-QA round-5 fixes — your audition winners v2/o3/f2, "row" re-render, alt-3 gap) merged `e1ada4a`, sponsor-authorized in tonight's walkthrough. Production deploy live.

**Action for Thomas:** Ear-test the **9 unanswered items** on https://marian-learning.vercel.app/voice-qa.html — the four/O/vvv/row re-rendered clips + the alt-3 hub line. PASS → issue #417 closes and voice-QA is fully green. FAIL items → report in chat; targeted fix-forward round.

**RESOLVED (Thomas, 2026-06-12, walkthrough #2):** Plan #419 approved & merged with defaults; Q2 = SCENES IN V1 (Thomas produces the MJ pack); dave-wt full cleanup authorized & done; W13-01 pre-dispatch accepted. Round-6 ear-test still standing.

## 2026-06-12 — Wave 13 plan PR #419 approval

**Context:** You picked simple-sentences as Wave 13 in the walkthrough. Matt's plan is open as **PR #419** (design/wave-13-simple-sentences-plan.md; anchor ticket 86ca8cp4z). Sponsor-gated — away-mode will NOT merge it. The plan carries **7 walkthrough-ready open questions** (§Open questions) with recommended defaults — the big one is Q1 (sentence mechanic), which Dave's research will rule on first.

**Action for Thomas:** Review/approve PR #419 (a walkthrough pass over the 7 questions works well). Dave's W13-01 research (ticket 86ca8cpe6) was pre-dispatched per merge-gates≠dispatch-gates — logged in decisions-while-away.md; all other tracks stay gated on your plan approval + Dave's ruling.

**UPDATE — Dave's research landed (PR #420):** verdict **PROCEED-SENTENCE-COMPLETION** (sentence ordering rejected on Tagalog PSO→SVO transfer risk; 40-sentence pool, no net-new vocabulary). Two of the plan's 7 questions now have Dave's evidence behind them: **Q1** (mechanic) = ratify sentence-completion; **Q2** (scene illustration) — Dave rules scenes LOAD-BEARING for the gentle phase (overrides sight-words' text-only precedent, which was word-recognition-specific), but flags the asset-production risk vs the August deadline — he explicitly wants this to be YOUR call, not a timeline default. Research PR #420 merges direct on green CI per convention.

## 2026-06-12 — Kyle's W13-02 spec §7: 5 open questions (PR #421)

**Context:** Kyle's simple-sentences spec (PR #421, Devon review in flight) collects 5 open questions with recommended defaults in its §7 — same walkthrough-ready shape as his prior specs. Kevin's W13-03 content dispatch consumes the spec after merge; §7 answers may adjust details but the vocabulary contract ('simple-sentence' / "Finish the sentence: <sentence>." / \_\_\_ / sentenceFrame) is binding for the Pattern-B parallel work already in flight (Jessica W13-05).

**RESOLVED (Thomas, walkthrough 2026-06-13):** all 5 §7 answered (Q1 per-template ~20 scenes, Q2 fill-gap-on-correct, Q4 leading blanks OK; Q3 32px + Q5 reuse-selector auto-decided). PR #421 MERGED (3cd633d). Kevin dispatched on W13-03 content (86ca8e6fr). Resolutions logged as a ticket comment on 86ca8cujn.

**YOUR remaining Wave 13 task — the MJ scene pack:** Kyle's spec §8 has your generation worklist — ~20 gentle-phase scenes, per-template, filename scene-<sentence-id>.svg, style brief in §8.4 (extends the picture-pack anchor), per-scene table in §8.5. Kevin assigns the final sentence-ids in his pool; generate against those once W13-03 lands. Pipeline: MJ → bgclear.ai → SVG-embed (scenes work cleanly with bgclear, unlike the Emma face-crops).

**UPDATE 2026-06-13 — W13-03 MERGED (4a7b364), tier is LIVE.** simple-sentences content shipped end-to-end (6 surfaces, W13-04 collapsed in, Devon APPROVE, byte-proof PASS). Anchor 86ca8cp4z + 86ca8e6fr COMPLETE. The final sentence-ids + the SIMPLE_SENTENCE_SCENES registry are now on main in src/screens/WordSong/sceneRegistry.ts (SCENE_PICTURES intentionally empty → graceful text-only fallback until your SVGs land). Generate the ~20 scenes against those ids → drop into public/assets/scenes/scene-<sentence-id>.svg. TWO sponsor gates now live:

1. **59-clip sentence-prosody ear-test** — production https://marian-learning.vercel.app/voice-qa.html. Highest-risk audio class (sentence reads, non-deterministic prosody). Your gate. Fails → targeted re-bake.
2. **MJ scene pack** — above; until it lands the tier renders text-only (no brick).

**Wave 13 state at queue-time:** Jessica's failing-first spec is OPEN as intentionally-RED PR #422 (5 tests; stays open until W13-03 lands — drain must not merge/close it). Jessica flags: the eventual W13-03 canon (~59 sentence-prosody clips, the highest-risk audio class yet) goes to your ear-test on production voice-qa post-merge, same flow as today's rounds.

## 2026-06-12 — dave-wt left in stash-pop conflict state (cleanup queued)

**Context:** dave-wt had stray uncommitted WIP from an old session (e2e/sub-to-10-progression-mastery.spec.ts modification, plus 6 old stash entries). My blanket `git stash` + `pop` during W13-01 git ops left a UU conflict on that file; the classifier (correctly) blocked discarding it without your say-so. W13-01 was routed through the main repo instead (PR #420) — nothing lost. Old WIP preserved in dave-wt stash@{0}.

**Action for Thomas:** OK to discard the conflicted working-tree state in dave-wt (`git checkout -- e2e/sub-to-10-progression-mastery.spec.ts`; the WIP stays recoverable in stash@{0})? Also: the 6 accumulated stashes there look like dead-session leftovers — say the word and I'll triage them (squash-merge branch-triage method) or drop them wholesale.

---

## 2026-06-15 — CVC continuant-onset word ordering = DEFERRED (Thomas, sponsor-questions-walkthrough)

**Context:** Dave's pass-1 phonics research (`design/research/cvc-blend-audio-phonics.md`) flagged that synthetic-phonics evidence (Five-from-Five) recommends opening CVC blending instruction with CONTINUANT-onset words (fan/man/van/sun) before STOP-onset (cat/dog). Current CVC session plans open with 'cat'.

**RESOLVED (Thomas, 2026-06-15, walkthrough): DEFER until blend audio is final.** Reordering touches the per-slot canon (a re-bake), so doing it mid-blend-iteration churns; revisit as one holistic CVC-plan update AFTER the blend pass-2/3 audio lands + Thomas has heard the final result. Not urgent (Marian not using the app yet).

**Trigger to re-surface:** once the blend pass-3 production fix merges + Thomas's final voice-QA ear-pass on the blend clips passes → re-open the CVC continuant-onset reordering question (Kyle owns the planner/canon reorder).

---

## 2026-06-15 — Away-window state (at `/auto-status away` start)

**Autonomous (away orch may proceed — mechanical, sponsor pre-approved):**
- **#471** (Wave-16 CVC review mode, 86c9qa6n3) — Thomas approved SHIP-V1 + must-fix ticket (86ca9atqh). **⚠️ E2E REGRESSION found 2026-06-15 ~20:40Z (NOT merged):** Playwright FAILED on HEAD 9c284db — ~30 focus-picker/planner-wire tests across the FORWARD tiers (cvc-words-short-i, digraphs-ch/sh/th, sight-words, simple-sentences, cvc-words-short-e) + cross-vowel regression. fast-gate (vitest 3329) + Devon's preview spot-check (cross-vowel seed only) both missed it; the e2e caught it. Root-cause hypothesis: the `pickFocusNode → {node, mode}` widening broke the forward focusNode threading to the session-start planner request (a consumer passing the `{node,mode}` object instead of `.node`).
  - **AUTONOMOUS DECISION (away orch, fix-forward):** re-dispatched **Kevin** (agentId `ac0f5f354193cd015`, same branch kevin/86c9qa6n3-cvc-review-mode) to find + fix the forward-threading regression + add the missing wire-focusNode coverage. **Foundation:** the e2e gate is a hard testing-bar rule (never merge past it); the sponsor already approved ship-v1, so making it green IS the approved outcome — not a new decision. **Reversibility:** a fix commit on the existing PR branch; trivially revertable. **Status:** Kevin in flight.
  - On Kevin's fix → e2e green → merge #471 → rebase Jessica's **#469** (intentional-RED) onto merged main → it greens → merge the pair → flip 86c9qa6n3 → COMPLETE.
  - **UPDATE 2026-06-16 ~22:01Z:** Kevin's fix (526d47e) Devon-RE-REVIEWED **APPROVE** + e2e GREEN → **#471 MERGED (60e8908).** Jessica's **#469 rebased onto merged main + force-pushed (34e4da4); CI re-running** (poll `b1s71t4q2`) — the failing-first spec should flip GREEN now the impl is on main. On #469 green → merge it → flip 86c9qa6n3 → COMPLETE. Wave-16 then fully done.
  - **UPDATE 2026-06-16 ~22:40Z:** #469 e2e came back FAILURE — but ONLY Jessica's own spec failed; every forward-tier focusNode test now PASSES (Kevin's fix is good). Root cause: Jessica's spec seeded the OLD/looser trigger ("3 CVC mastered, short-i unmastered → review fires"), but Kevin's FIX correctly fires review only when the WHOLE word-song tree is mastered — so the picker correctly lands on forward short-i, no cross-vowel chips. The spec encoded the pre-fix mechanic. **Re-dispatched Jessica** (agentId `a03bc9a9761086e8d`) to seed the whole-tree-mastered + latch-unset state (the real graduation trigger) + reframe the now-merged spec as a standing regression lock. **Foundation:** the impl is correct + merged (Devon APPROVE, forward tests green); the spec must follow the corrected invariant (progress-and-persistence.md firing-order invariant). **Reversibility:** a one-file seed change on the existing PR branch. 86c9qa6n3 stays IN REVIEW until #469 greens + merges.
  - **✅ DONE 2026-06-16 ~23:40Z:** Jessica's seed-fix (8853744) verified GREEN locally + CI green → **#469 MERGED (fb60fec)** → **86c9qa6n3 flipped COMPLETE.** Wave-16 fully shipped (impl + fix + regression-lock e2e). The cross-vowel review mechanic now fires for real in production play. Must-fix follow-up 86ca9atqh (periodic skillFocus mislabel) remains in TO DO for before-CVC-mastery.

## 2026-06-16 — PENDING wind-down housekeeping (not blocking)

- **Docs PR not yet landed.** Accumulated uncommitted `.claude/docs` captures sit in the MAIN workspace working tree (against a STALE local main): planner-and-canon.md (blend isolated-stop scratch gotcha), progress-and-persistence.md (CVC-review firing-order invariant), testing-and-ci.md §4.1.8 (latch-default test-masking). DEFERRED from away-mode autonomous handling because landing them needs the careful stale-base reconciliation (the #462 dance) — better done with full attention at wind-down / sponsor-return than fiddly midnight git surgery on non-blocking housekeeping. Captures are safe on disk. Jessica confirmed worktree docs are stale without them (her finding #2). Land via a docs branch FROM origin/main (not local main) re-applying the 3 additions.
  - **ATTEMPTED + BACKED OFF 2026-06-16 ~23:5xZ:** tried landing autonomously from the PRIMARY worktree (branch off stale main + commit 3 files + rebase onto origin/main, the #462 pattern). Rebase failed `could not detach HEAD` — the branch was checked out in the same primary worktree running the rebase. Recovered cleanly (captures restored to working tree, away-queue restored, branch deleted; nothing lost). **LESSON: do the docs-PR from a SEPARATE worktree off origin/main (not the primary main workspace), `git checkout`/Edit the 3 additions in, commit + PR there.** Still non-blocking; deferred to wind-down.
- **#468** (Dave pass-2 phonics note) — research, merges direct when e2e greens.

**Blocked on Thomas's ear (do NOT proceed):**
- **Blend pass-2 picks ARE IN (2026-06-16) — see the "Blend pass-2 audition RESULTS" section at the bottom.** #470 stays OPEN (preview still needed for the pass-3 A/B). Pass-3 audition now in flight (Dave phonics ruling → Devon audition build).

**Deferred:** CVC continuant-onset reorder (above). Polish backlog / Wave 14 = decided-but-fresh-session (do not start).

---

## 2026-06-16 — Blend pass-2 audition RESULTS (Thomas, on session resume) + pass-3 dispatched

Thomas A/B'd #470 (pass-2 audition). **Verbatim per-word verdicts:**
- **cat** (stop-control): `fric-elong` → stops stay clean (control passed).
- **hat** (/h/): `fric-rel`.
- **hen** (/h/): `fric-rel`.
- **fan** (/f/): none accepted — "she should pronounce F as 'aef' or 'æf'".
- **fox** (/f/): none accepted (same as fan).
- **sip** (/s/): none accepted — "she says 'soe' for S; she should say 'es' or 'aes' or 'æs'".
- **sun** (/s/): none accepted (same as sip).
- **van** (/v/, floored): `fric-elong` column → whole-word "van" accepted → **/v/ FLOOR confirmed**.
- **jam** (/dʒ/): none accepted (no alternative offered).
- **web** (/w/): none accepted — "she says 'ua' now 'wua'; the deep voice is closest".
- **wig** (/w/): none accepted (same as web).

**LOCKED (pass-3 bakes these):** stops = keep shipped (released `/kə/`); **/h/ = `fric-rel`** (hə minimal vowel-support); **/v/ = FLOOR** (whole-word "van"). Do NOT revisit.

**UNRESOLVED → pass-3 audition (4 classes):** /f/, /s/, /dʒ/, /w/. Thomas's directional hints: F → leading-vowel "æf"/"aef"; S → "es"/"æs" (current "soe" is a trailing-schwa artifact); w → deeper pitch ("deep voice closest"); j → no hint.

**ROUTING:** Dave dispatched for a pass-3 phonics ruling (`design/research/cvc-blend-audio-phonics-pass3.md`) — per-class recommend {specific audition candidate spec | FLOOR} for f/s/j/w, ruling on leading-vowel-fricative blendability (Thomas's æf/es proposal vs phoneme-leads ideal) + proposing j and w treatments (deep-voice hint). Then Devon builds a pass-3 audition with the survivors → Thomas ears again → Devon pass-3 bake (h=fric-rel, v=floor, stops as-is, f/s/j/w=winners), splice-only byte-preserve. #470 held OPEN for the pass-3 A/B.

**Dave DONE (2026-06-16):** `design/research/cvc-blend-audio-phonics-pass3.md` delivered (in PRIMARY checkout, NOT yet git-landed — batch with the deferred docs-PR from a clean worktree). Ruling: /f/→"ef", /s/→"es" (orthographic letter-name leading-vowel, no IPA, rate -20%); /dʒ/→"juh" (rate -30% pitch -15%) then FLOOR if rejected; /w/→"wuh" (rate -25% pitch -20%, deep-pitch per Thomas's hint) then FLOOR if rejected. Leading-vowel isolation ruled pedagogically OK (Marian already owns the phonemes; it's a 2nd-tap hint; vowel = Azure coarticulation anchor). Key standing strategy (defer to docs until pass-3 ear-validated): orthographic syllable > bare IPA for isolated-consonant TTS in en-GB-Olivia.

**Devon DONE (2026-06-16):** pass-3 audition shipped as **PR #472** (OPEN, stacked on #470, held as instrument — do NOT merge). Preview live for Thomas's A/B:
`https://marian-learning-git-devon-blend-audi-cbbf57-tsandvaers-projects.vercel.app/blend-audition.html`
Per word: 3 clips (pass3 candidate / FLOOR whole-word / broken control), grouped by phoneme class. Devon's hash invariants confirm the levers applied (cat.pass3==broken stop-control clean; van 3-way-equal floor uniform; fan.pass3!=broken onset applied) → audio is trustworthy for the ear-test without a separate code review. Nested `<prosody>` compounds multiplicatively (NOT override) — captured in planner-and-canon.md.

**THE GATE (now):** Thomas A/Bs #472 → per class, ACCEPT the sounded-out candidate OR reject→FLOOR (whole-word): /f/ (fan/fox, "ef"-led), /s/ (sip/sun, "es"-led, kills "soe"), /dʒ/ (jam, "juh" deep — likely floor), /w/ (web/wig, "wuh" deep — likely floor). On his picks → **pass-4 bake** (Devon ports winners into `renderBlendInnerText` + /v/ & floored-class routing + splice-only re-bake → Kevin review → merge → Thomas final voice-qa ear-pass).

**Pending git-landing (orchestrator housekeeping, non-blocking):** Dave's pass-3 note + the 4 `.claude/docs` captures (3 session + the new nested-prosody finding) — one clean-worktree batch off origin/main.

---

## 2026-06-16 — Blend pass-3 audition RESULTS (Thomas) — leading-vowel/deep-pitch REFUTED; close-out decision pending

Thomas A/B'd #472. **Manifest VERIFIED** (devon-wt `blend-audition-data.json`, jq hash compare): all pass3 onset levers DID apply — `p3.audioHash != broken` for fan/fox/sip/sun/jam/web/wig; cat (stop-control) & van (floor) `p3==broken` as designed. So the candidates were genuinely auditioned — NOT a lever bug (my initial hypothesis, refuted). SSML confirmed: fan/fox=`<prosody rate="-20%">ef</prosody>`, sip/sun=`es`, jam=`<prosody rate="-30%" pitch="-15%">juh</prosody>`, web/wig=`<prosody rate="-25%" pitch="-20%">wuh</prosody>`. Verdicts:
- cat (stop-control): OK (stops clean). van (/v/ floor): OK (floored whole-word fine).
- **/f/ (fan, fox): REJECTED** — "ef" swallows the /f/ (Thomas heard "a,a,n" / detached letter).
- **/s/ (sip, sun): REJECTED** — "es" no better than the broken bare onset.
- **/dʒ/ (jam): REJECTED** — "juh" wrong; Thomas wants "dje" (front vowel, jet-onset).
- **/w/ (web, wig): REJECTED** — deep "wuh" no better.

RESULT: leading-vowel fricatives + deep-pitch glide/affricate **REFUTED by ear**. After 3 passes — classes that WORK = stops, /h/ (fric-rel), /v/ (floor); classes that DON'T = /f/, /s/, /dʒ/, /w/. Neural Azure resists clean isolated fricative/glide/affricate (trained on connected speech).

**Close-out decision (Thomas, popup):** (1) RECOMMENDED — build a sponsor-driven tweak handle (live param page if a runtime render path is feasible, else a dense candidate grid seeded by Thomas's correctives af/ef, dje, deeper-w); Thomas converges each class by ear in ONE pass; floor what still can't crack. (2) FLOOR all 4 now + ship the blend (Dave: floor pedagogically adequate; hint is a 2nd-tap scaffold). Either path ENDS the loop. NOT another blind orchestrator-guessed bake.

**DECIDED (Thomas, popup 2026-06-16): Option 1 — build the handle.** Runtime Azure path confirmed (`api/claude.ts`→`api/_session.ts`→`synthesizeUtterance`) → LIVE handle feasible.
**Devon DONE (2026-06-16): live tuning handle shipped as PR #473** (OPEN, off origin/main, branch `devon/blend-tweak-handle`, held as instrument — gated non-prod so harmless on main; do NOT merge until winning params are baked). Live page (private tab):
`https://marian-learning-git-devon-blend-twea-71ae06-tsandvaers-projects.vercel.app/blend-tweak.html`
Path = LIVE (preview deploys DO carry Azure creds — confirmed by a real MP3 from the endpoint). `api/blend-tweak.ts` renders the FULL blend SSML mirroring `renderBlendInnerText`, onset slot parameterized via a NEW opt-in `ssmlOverride` seam on `synthesizeUtterance` (production paths never set it), escapeSsml'd, `VERCEL_ENV==='production'`→404, rate-limited, 23 tests + `public/blend-tweak.html` (per-word onset/rate/pitch/break inputs, live Play, SSML readout; rows fan/fox/sip/sun/jam/web/wig + cat-control + van-floor-ref). Full suite 3362 green; `blend-tweak` 0× in `dist/sw.js`. Runtime-render-on-preview + VERCEL_ENV-gate captured in planner-and-canon.md.

**THE GATE (now):** Thomas tunes each class live → reports winning onset params (e.g. "/f/ = ff @ -25%") or "floor it". Seed correctives: /f/ stretch the f (ff/fff/af/ef), /s/ (ss/sss/es), /dʒ/ "dje" (front vowel), /w/ deeper pitch (wuh/oo/woo). On his params → **pass-4 bake** (Devon ports winners into `renderBlendInnerText` + floors the rest → Kevin review → merge → final voice-qa ear-pass).

**Kevin review #473 — INTERRUPTED (API 500 ×2, transient server-side):** Kevin (agentId `afbb0dbdc8844ce37`) did the analysis (~35 tool-calls) but the API 500'd before he posted the verdict; the SendMessage-resume also 500'd. **Verdict NOT on the PR.** NON-BLOCKING. **DEFERRED** until Devon's IPA-mode addition lands (#473 is now in flux — re-dispatch Kevin fresh to review the FINAL state, read-only so nothing lost). Focus: `ssmlOverride` seam opt-in / production byte-unchanged; endpoint prod-gate + escapeSsml + rate-limit; SSML fidelity vs `renderBlendInnerText`.

## 2026-06-16 — Blend fricatives: orthographic EXHAUSTED → IPA length-mark mode added

Thomas tuned live and hit the wall: **orthographic onsets cannot produce a held fricative** — `fff` → Azure spells "F-F-F"; `ef`/`es` → letter names; bare `f`/`s` → schwa-buzz. Decision (popup): **(1) add IPA-onset mode + try `fː`/`sː`** [chosen] vs (2) floor /f/+/s/. Foundation for the lever: planner-and-canon.md "length-marked IPA consonants are honoured by Olivia (`vːə` extended cleanly)".
**In flight:** Devon (agentId `a0aba532be8e7f059`) extending #473 (branch `devon/blend-tweak-handle`) — adds `onsetMode: 'text'|'ipa'` to `api/blend-tweak.ts` (IPA mode → `<phoneme alphabet="ipa" ph="{ipa}">{grapheme}</phoneme>`, XML-escaped attr but IPA unicode preserved) + per-row Text/IPA toggle on the page with chips (`fː`/`fːf`/`fːə`, `sː`/`sːs`/`sːə`, plus `dʒ`/`w` IPA for the other rows). Preview redeploys → Thomas tries `fː`/`sː` live. If IPA length-mark ALSO fails to sustain a clean fricative → FLOOR /f/+/s/ (Dave-blessed). API recovered (Devon dispatch succeeded).

## 2026-06-16 — IPA mode shipped + preview-region BLOCKER; /whip → must-fix dispatched

**Devon DONE (#473 IPA mode):** `onsetMode: 'text'|'ipa'` added; `fː`/`sː` CONFIRMED to render as held fricatives on real Azure (local westeurope, 200 + audio) — **the length-mark lever WORKS**. BUT the Vercel **preview's Azure region REJECTS** length-mark/schwa IPA (400) while bare IPA + text mode render → **Thomas can't ear-test `fː`/`sː` on the preview live endpoint.** Static PRE-BAKED clips (baked westeurope, served as base64) sidestep this entirely (no runtime render → region-independent). Gotcha: a NUL byte got written into `api/blend-tweak.ts` via an Edit regex char-range (`[…' -]` collapsed to a control range) — Devon caught+fixed; Edit's old_string silently fails against a NUL-containing file. (maintain-docs to capture both.)

**/whip pass (Thomas-invoked):** team was idle (sponsor-ear-gated phase). Thomas green-lit the deferred must-fix to fill idle capacity.
**Dispatched — must-fix 86ca9atqh (Wave-16 shape):**
- **Kevin** (agentId `a2cc2238ddc3522fa`, branch `kevin/86ca9atqh-cvc-review-skillfocus`) — impl AC1-4: thread the picked focus identity (node+mode from `pickFocusNode`) through Math/WordSong → SessionEnd payload so a periodic CVC review credits the review tier, not the forward node (`qualifies()` mastery contamination). Files: progressHistory.ts, App.tsx, mastery.ts(~620), + SessionEnd-level test + AC4 note→cross-vowel-mix-spec.md. Devon reviews.
- **Jessica** (agentId `ae03eabad1f44071c`, branch `jessica/86ca9atqh-cvc-review-contamination-e2e`) — failing-first e2e: forward-node 90/3 counter UNCHANGED by a periodic post-graduation CVC review. Intentional-RED until Kevin merges (Wave-16 #469 pattern). Devon reviews.

**Blend-unblock: SURFACED to Thomas (popup)** — static `fː`/`sː` A/B bake [keeps iPad flow] vs run the live handle locally (`yarn dev`). Pending his pick. Devon justified-idle until then.
**#473 Kevin review:** still unposted (API 500'd ×2) + now Kevin's on the must-fix — re-dispatch a reviewer for #473's FINAL state later (non-blocking, instrument PR).

**UPDATE 2026-06-16 (post-dispatch):**
- **Jessica DONE — PR #474** (`jessica/86ca9atqh-cvc-review-contamination-e2e`, intentional-RED, reviewer Devon). RED proof on main: recorded `skillFocus` = `simple-sentences` (forward fallback) vs expected `cvc-words-short-u` (review tier); GREEN with Kevin's fix (verified locally then reverted). Jessica reverse-engineered the exact fix — `App.tsx` ~L308 passes `sessionCount`, `SessionEnd.tsx` ~L481 omits it → `sessionCount>0` guard skips the periodic branch → records the forward fallback. **Sibling call site flagged:** `focusRecapCopy` (`SessionEnd.tsx` ~L380) ALSO omits `sessionCount` — RELAYED to Kevin (SendMessage) to fix in the SAME PR. NIT follow-up to file: widen `SeedProgressOptions` to expose `cvcGraduationSessionFired?` (3rd raw-spread adopter, testing-and-ci §4.1.1c). Nuance: contamination is currently MASKED in the all-mastered state (forward fallback already mastered; mastery rule scans only `practicing` nodes) — Jessica's spec correctly locks the skillFocus ATTRIBUTION, not a counter delta.
- **Devon dispatched — static IPA-fricative bake** (agentId `a65971b5127fbb691`, extends #472 `devon/blend-audition-pass3`): "pass-4" section baking `fː`/`sː` (+`fːə`/`sːə`) × ~2 rates × fan/fox/sip/sun + whole-word floor, **westeurope** creds → region-independent static clips. Thomas A/Bs on the #472 preview → reports winning `ph`+rate per fricative → pass-5 production bake (or floor). Surface the #472 preview URL on Devon's completion.
- Kevin (must-fix impl, `a2cc2238ddc3522fa`) in flight, sibling-site note delivered.

**Kevin DONE — must-fix impl PR #475** (`fix(word-song): periodic CVC-review skillFocus mislabel (86ca9atqh)` → main; ticket flipped IN REVIEW). Fix: threads the picked focus identity through a new **`SessionEndPayload.sessionFocus: { node: SkillNode; mode: FocusMode }`** field (mirrors the existing `wordSongCurrentTargetVowelRef` W9.4 pattern), frozen at session-start kick-time. Covered BOTH diverging call sites (the P0.2 `skillFocus` write + `focusRecapCopy` recap caption — Jessica's flagged sibling). AC1-4 done; 5 new SessionEnd-level tests (RED→GREEN); `mastery.ts`/`progressHistory.ts` untouched (they faithfully record what they're handed — the bug was the contaminating WRITE). `yarn test` 3344 pass; fast-gate SUCCESS, Playwright IN_PROGRESS at report.
**MERGE SEQUENCE:** Devon reviews #475 → CI green (verify e2e via statusCheckRollup) → merge #475 → Jessica rebases #474 onto merged main → #474 greens → merge #474 → flip 86ca9atqh COMPLETE.
**QUEUED next-action:** Devon's #475 review is BLOCKED on his in-flight static-fricative bake (one Agent per devon-wt). **On Devon's bake-completion notification → dispatch Devon to review #475** (cross-review; verdict via gh pr comment, self-review block).
**Latent NIT follow-ups (batch post-merge, don't dispatch now):** (1) widen `SeedProgressOptions` for `cvcGraduationSessionFired?` (3rd raw-spread adopter); (2) add a `periodic-cvc-review` debug seed (no SEEDS entry reaches that state → only programmatic seedStorage can; blocks iPad smoke of this invariant).
**RESUME NEXT-ACTION:** waiting on (a) Devon's static `fː`/`sː` bake → then surface its #472 preview URL to Thomas + dispatch Devon's #475 review; (b) Thomas's `fː`/`sː` ear-pick. Kevin/Jessica idle-justified (must-fix in review-wait). Devon busy (bake).

**Devon DONE — pass-4 IPA-fricative bake** (`7be09c1` on `devon/blend-audition-pass3`, extends #472). 20 static clips (westeurope, region-independent): fan/fox/sip/sun × {`fː`,`fːə`} × {-25%,-40%} + whole-word floor. Live at the #472 preview `…blend-audi-cbbf57…/blend-audition.html`, pass-4 section below pass-3. URL surfaced to Thomas. Devon's two poles: `fː`@-40% = purest held /f/; `fːə`@-25% = most natural blend. **Thomas A/Bs → reports winning form+rate per fricative (or floor).**
**PASS-5 CAVEAT (carry into the production bake):** the length-mark renders on westeurope at BUILD time (canon bake), but the production RUNTIME region rejects `fː`/`sː` (400). Pass-5 must CONFIRM the shipping path accepts the length mark before porting into `renderBlendInnerText` — else FLOOR regardless of clip quality. (Blend is pre-baked canon → build-time westeurope bake produces the clips, so playback is likely fine; but any runtime cache-miss render of a blend clip would 502 in the prod region. Verify before pass-5 ships.)
**#475 review — QUEUED, NOT yet dispatched:** Devon is the reviewer but a `status?` SendMessage to his agentId was queued just before he completed → it may resume him on devon-wt. Do NOT dispatch the #475 review until devon-wt is confirmed free (avoid the one-Agent-per-worktree race). Fire Devon's #475 review once the status?-resume (if any) clears.
**Verified liveness (2026-06-16):** Devon's bake was confirmed in-flight mid-session via SendMessage("queued at next tool round" = alive) + no-new-commit, then completed with `7be09c1`. STATE was accurate throughout.

## 2026-06-16 — Thomas's pass-4 picks: FRICATIVES WON (no floor)

Via the #472 pass-4 page (screenshots, "Pick" highlighted on all four words):
- **/f/ (fan, fox) = `fːə` (held + schwa tail) @ -25% onset rate** ("flen-schwa-r25").
- **/s/ (sip, sun) = `sːə` (held + schwa tail) @ -25% onset rate** ("slen-schwa-r25").
Devon's "most natural blend" pole. **NO floors** — the IPA length-mark + schwa-tail cracked the fricatives after 4 passes. Locked render for pass-5: `<phoneme alphabet="ipa" ph="fːə">f</phoneme>` / `ph="sːə"` inside the onset `<prosody rate="-25%">`, then production candidate-f for the rest.

**Remaining open — /dʒ/ (jam) + /w/ (web/wig):** failed pass-3 ("juh"/"wuh"), NOT in the pass-4 fricative round. Surfaced to Thomas (popup): floor both [recommended — affricate can't be held; both Dave-blessed] vs one more IPA try for /w/ (`wː`/`ʊw`) + floor /dʒ/.

**/dʒ//w/ DECIDED = FLOOR** (Thomas "continue" → orchestrator recommendation; affricate can't be held, both failed pass-3, Dave-blessed; redirect window left open for a `wː` /w/ retry if Thomas wants). 

**Full pass-5 blend spec (FINAL):** stops=shipped(`kə`), /h/=fric-rel(`hə`), /v/=FLOOR, **/f/=`fːə`@-25%, /s/=`sːə`@-25%**, **/dʒ/=FLOOR, /w/=FLOOR**. ONE splice-only re-bake of all blend clips (byte-preserve the rest).

**DISPATCHED — pass-5 PRODUCTION bake** (Devon, agentId `a1b2701c4cae3bf6a`, branch `devon/blend-pass5-production`): port the per-class renders into `renderBlendInnerText` (`api/_tts.ts`) + `BLEND_FLOOR_GRAPHEMES`/`wordIsFloored` for v/dʒ/w + splice re-bake (byte-preserve proof) → Kevin review → merge → Thomas's final voice-QA ear-pass. **HARD GATE (Devon does first):** verify NO production runtime path renders a blend clip (else prod region's `fː` 400 → 502); if pre-baked-canon-only (likely), ships fine; if a runtime path exists, STOP + report (may need to floor f/s or align region).
**#475 review now QUEUED behind pass-5 on devon-wt** (serial; pass-5 is sponsor-priority). Kevin (free) reviews pass-5; Devon reviews #475 after the bake. Must-fix #474/#475 not urgent.

## 2026-06-16 — Pass-5 HARD GATE TRIPPED: length-mark is runtime-reachable (Devon stopped, no port/bake)

Devon ran the runtime-region gate FIRST and it FAILED → stopped before porting (branch `devon/blend-pass5-production` clean). **Evidence (live main tree):** graduation `cvc-words` sessions **bypass canon** (`api/claude.ts:855-859` `bypassCanonAndCache`) and runtime-render the blend (`_planner.ts:3296` emits the blend slot for cvc-words; `renderBlendInnerText` runs live with `tierFilter='cvc-words'`). Base short-a pool has `fan`(/f/)+`van`(/v/). The prod runtime Azure region rejects the IPA length mark (400) → per-utterance SOFT-FAIL (`_session.ts:199-220`, ticket 86c9kjdh2) → **the /f//s/ blend clip goes SILENTLY ABSENT on a graduation run** (not a 502; degraded, rare, hard-to-QA — the silent-audio regression class). So "pre-baked-canon-only → safe" is FALSE; the length mark is runtime-reachable.
**DECISION SURFACED to Thomas (popup) — 3 options, all unblock the same port+splice-rebake:**
- **A — Floor /f//s/ too** (add f,s to BLEND_FLOOR_GRAPHEMES; no length mark anywhere). Safe, ships now, but LOSES Thomas's picked held-fricatives. (/h/ `hə` has no length mark → stays; slight fricative-class inconsistency under A.)
- **B — Align prod runtime Azure region to westeurope** (Vercel env-var = INFRA, Thomas's action) + re-confirm the region accepts the length mark at RUNTIME. Fricatives everywhere, no divergence. Cleanest end-state IF the region is freely changeable.
- **C — Region/bake-context branch in `renderBlendInnerText`** (RECOMMENDED): emit `fːə`/`sːə` only at build/westeurope (→ baked canon keeps the held fricatives for everyday play), FLOOR /f//s/ at runtime → no silent drop, no infra change, ships now. Cost: a /f//s/ word floors (whole-word) instead of holding on a RARE graduation runtime run + a context branch in the SSML path. Negligible given Marian isn't using the app + graduation is months out.
**Devon confirmed ready (fast once unblocked):** exact pass-4 onset SSML in hand; `/h/` fric-rel + v/dʒ/w floor specified; precedent scripts `rebakeBlendCandidateF.ts` + `verifyBlendBytePreservation.ts` reusable. Awaiting A/B/C.

**DECIDED (Thomas, popup): Option B — align the prod runtime Azure region to westeurope.** INFRA change = Thomas's action (Vercel env). **KEY GOTCHA (confirmed in `api/_tts.ts`:108-120, endpoint `https://{region}.tts.speech.microsoft.com`):** the runtime reads BOTH `AZURE_SPEECH_REGION` and `AZURE_SPEECH_KEY`, and the key is REGION-BOUND — a region/key mismatch → 403 (`_tts.ts:1377`) breaking ALL runtime audio. So Thomas must set BOTH to a westeurope Speech resource (the bake's resource, which renders the length mark). **Plan given to Thomas:** (1) set Preview-scope `AZURE_SPEECH_REGION=westeurope` + `AZURE_SPEECH_KEY=<westeurope key>` → redeploy preview; (2) orchestrator/Devon re-verify `/api/blend-tweak` `fː`→200 + a normal session still generates (no 403); (3) if clean → set same for Production + redeploy → (4) dispatch Devon to port + splice-rebake pass-5. Fallback offered: if the key+region coupling is too much prod risk, switch to Option C (no infra). **Devon REASSIGNED to the queued #475 review** (agentId `a99bfbc9a427f3c35`, `gh pr checkout 475` in devon-wt) — using his idle capacity while pass-5 is blocked on Thomas's infra change. Pass-5 port now waits on BOTH: (a) Thomas confirms the preview Azure region + the runtime length-mark re-verify passes, AND (b) devon-wt frees from the #475 review. RESUME NEXT-ACTION: on Devon's #475 verdict → if APPROVE + CI green, merge #475 → rebase Jessica's #474 → green → merge → flip 86ca9atqh COMPLETE; on Thomas "preview region set" → re-verify `/api/blend-tweak` `fː`→200 + session-gen OK → (once devon-wt free) dispatch Devon's pass-5 port.

**UPDATE 2026-06-16/17 — must-fix landing + region-fix in flight:**
- **#475 MERGED** (Devon APPROVE no-NITs + CI green) → `e5d2e77` on main. Kevin's impl shipped.
- **#474 MERGED (`0f869ac`, CI green: fast-gate + Playwright SUCCESS) → 86ca9atqh flipped COMPLETE (2026-06-17 04:19Z).** Must-fix fully landed: #475 (impl `e5d2e77`) + #474 (e2e regression-lock). DONE. (Cosmetic: both --delete-branch failed on the local worktree-held branch; remote branches deleted fine.)
- **Thomas set Preview-scope `AZURE_SPEECH_REGION=westeurope` + `AZURE_SPEECH_KEY=<marian-tutor-tts westeurope key>` + deployed a preview.** (His resource `marian-tutor-tts` IS westeurope — confirmed via Azure screenshot; the mismatch was Vercel pointing runtime at a different region. Values masked in Vercel UI so set, not read.) **Devon dispatched to VERIFY** (agentId `a040d5a85d6f1cd73`, on devon/blend-tweak-handle): fresh #473 preview deploy → `/api/blend-tweak` `fː`/`sː`→200 (was 400) + no 403. **On FIXED → Thomas applies the same 2 vars to Production → then dispatch Devon's pass-5 port.** On still-400/403 → diagnose.

**RESULT — region change did NOT fix it (Devon `a040d5a85d6f1cd73`):** fresh preview deploy still Azure-400 for `fː`/`sː`/**`kə`** (endpoint maps to HTTP 502 `detail: tts http error (400)`). **`kə` (the already-shipped STOP release) ALSO 400s** → the runtime region has NEVER rendered the blend's compound IPA; shipped canon unaffected (pre-baked CDN), only the live-render (graduation) path. **Auth WORKS (no 403)** → creds are a consistent pair, just not behaving like local westeurope. **Broader implication:** any runtime render using a phoneme override (compound IPA — e.g. `PHONEME_OVERRIDES` 'four'/'row') would ALSO 400 at runtime here → fixing the region fixes ALL runtime IPA, not just the blend (argues for B over C).
**Hypothesis (likely):** Preview-scope mismatch — PR previews use the **Preview** scope, but the env screenshot showed region vars scoped to Development/Production; editing Development (`vercel dev` only) wouldn't touch preview deploys.
**INSTRUMENT dispatched (Devon `ab71df430304449a7`):** echo `azureRegion` + `vercelEnv` from the non-prod-gated blend-tweak endpoint → see the ACTUAL preview runtime region. `azureRegion != westeurope` → Thomas's env didn't take (fix the scope) ; `== westeurope` but still 400 → deeper (different resource behind the region name) → rethink. Awaiting the echo. (Devon left 2 cosmetic empty redeploy commits `c129201`/`40b07d3` on the branch — squash later.)

**ECHO RESULT (Devon `ab71df430304449a7`): region IS westeurope** (`azureRegion:"westeurope"`, `vercelEnv:"preview"`) — Thomas's env change took effect; NOT a scope/typo issue. **Root cause = SSML SHAPE, not region/IPA/length-mark:** bare `<phoneme>` renders 200 (the medial æ/n in the same clip); the blend ONSET's nested shape `<prosody>(root)<prosody>(onset)<phoneme/></prosody></prosody>` 400s on THIS runtime resource — while the local bake resource accepts the identical shape (same "westeurope" name, different resource behaviour). So only the rare runtime live-render path is affected; pre-baked canon is fine.
**DECIDED (Thomas, popup): RUNTIME-SAFE blend** — full-fidelity `fːə`/`sːə` blends in baked CANON (his picks intact, no re-test); whole-word render at RUNTIME. No further Vercel work (region change stays, now harmless).
**DISPATCHED — pass-5 FINAL (runtime-safe)** (Devon `a7bd9003ff65bf736`, branch `devon/blend-pass5-production` off origin/main `e5d2e77`): `renderBlendInnerText` gets a `blendFullFidelity` flag — bake-path opt-in = per-class segmented (stops `kə` / `/h/`=`hə` / `/f/`=`fːə`@-25% / `/s/`=`sːə`@-25% / v·dʒ·w=floor); runtime DEFAULT = whole-word plain text (always renders) + thread the flag from `generateSessionCanon` (bake) only + splice re-bake (byte-proof) → Kevin review → merge → Thomas final voice-qa ear-pass.
**DOC-WORTHY (now root-caused, capture once pass-5 lands):** this runtime Azure resource rejects a `<phoneme>` nested two prosody levels deep while accepting bare `<phoneme>`; "same region name ≠ same resource behaviour"; the runtime-safe (whole-word-at-runtime, full-fidelity-at-bake) pattern.

**DONE — pass-5 shipped as PR #476** (Devon, `ff69960`, branch `devon/blend-pass5-production`). `renderBlendInnerText` `blendFullFidelity` flag via `RenderSessionOptions.synthOptions` (NOT a literal `ssmlOverride` — that was the brief's analogy): bake-path opt-in = full per-class segmented; runtime DEFAULT = whole-word floor (plain, renders on the rejecting resource). Re-baked 40 blend clips full-fidelity on westeurope (0 rejections, splice-only). **BYTE-PROOF PASS: 300/300 non-blend identical; only 15 f/s/h/v/j/w clips changed** (verifier upgraded from "all-40-changed" to a per-grapheme-class expected-change set). Full suite 3358 green; CI fast-gate IN_PROGRESS at report. **Kevin verdict (`a70f65261abe9e281`): APPROVE_WITH_NITS** — runtime-safe default verified (test + dual code-trace; `blendFullFidelity` never reaches runtime), byte-proof independently reproduced, fidelity matches pass-4 picks. Non-blocking NITs (follow-up): (1) **pre-existing soft-`g` /dʒ/ floor gap** — floor keyed on grapheme not phoneme, so `gem`/`gel` (soft-g = /dʒ/) escape the floor that hard-`j` words take → would sound out as a hard-g stop; LATENT (no soft-g word in current CVC pools; guard when adding content); (2) `bus` coda-/s/ → Thomas's ear-pass. **#476 MERGED (`bccd332`, 2026-06-17 05:33Z)** — e2e went green (verified via `gh run list` ground-truth, not cached rollup), Kevin APPROVE_WITH_NITS, byte-proof PASS → merged. **BLEND PASS-5 IS IN PRODUCTION:** full per-class canon (stops `kə` / `/h/`=`hə` / `/f/`=`fːə`@-25% / `/s/`=`sːə`@-25% / v·dʒ·w=floor) + runtime-safe whole-word render. **THE FINAL GATE → Thomas's ear-pass:** after the Vercel prod deploy (~2-3 min) the 15 re-baked f/s/h/v/j/w blend clips flip to needs-retest on https://marian-learning.vercel.app/voice-qa.html — Thomas ear-tests (esp. fricatives + `bus` coda-/s/). PASS → blend DONE; FAIL → targeted fix-forward re-bake.
**POST-MERGE TODO:** (1) **capture pass-5 doc findings** (no longer deferred — #476 merged): per-word byte-preservation, `bus` per-grapheme floor gotcha, blend `tierFilter`/`WORD_SONG_FIRST_CLASS_FOCUS_NODES` routing, runtime-safe pattern (whole-word@runtime / full-fidelity@bake), nested-prosody-rejected-by-runtime-resource (same-region-name≠same-resource); (2) **file NIT tickets:** soft-`g` /dʒ/ floor gap, SeedProgressOptions widening (`cvcGraduationSessionFired?`), periodic-cvc-review debug seed; (3) **instrument PRs #470/#472/#473** cleanup decision (merge-to-main-as-registered-instruments vs close — they carry the audition pages + the live blend-tweak handle + region-echo debug); (4) **docs/research landing** (Dave's pass-3 note + the accumulated `.claude/docs` captures) — wind-down.
**Devon's pass-5 findings to capture on-merge:** (1) pass-5 byte-preservation is PER-WORD not blanket (only SSML-diverging clips change; derive expected-change set from grapheme class); (2) `bus` coda-fricative gotcha (`sːə` keyed per-grapheme → coda `s` diverges even with a stop onset; scan ALL graphemes for "which words change"); (3) blend `tierFilter` routing — CVC focus node must be in `WORD_SONG_FIRST_CLASS_FOCUS_NODES` or the bake routes to `blending-cv`, `tierFilter` mismatches `BLEND_CVC_TIERS`, `renderBlendInnerText` early-returns null → blend audio silently falls to the plain path (no error). No ClickUp ticket for the blend (PR-driven).
**DOC-WORTHY (next maintain-docs, NOT deferred — cites live main):** blend clips ARE runtime-reachable via the graduation-cvc-words canon bypass; not pre-baked-canon-only. Refines the runtime-render notes.

## 2026-06-17 — POST-MERGE housekeeping DONE (#477 + NIT tickets); only the ear-pass remains

Thomas (popup) chose "do the housekeeping now" on session resume. Completed:
- **Docs/research landing — PR #477 MERGED** (`93f3510`, 2026-06-17 09:07Z). Landed the 3 `.claude/docs` captures (planner-and-canon blend pass-5 section + nested-prosody/runtime-render/runtime-reachability/isolated-stop findings; progress-and-persistence CVC firing-order invariant; testing-and-ci §4.1.8 latch-masking) + the away-queue audit log + Dave's pass-3 note (`design/research/cvc-blend-audio-phonics-pass3.md`). 378 insertions / 0 deletions, clean superset. Done from a fresh worktree off origin/main (NOT the primary — avoided the #462-style rebase trap, which failed last attempt). Primary worktree synced (`reset --hard origin/main`); temp docs worktree + local branch removed. (POST-MERGE TODO items 1 + 4 = DONE.)
- **NIT tickets filed** (POST-MERGE TODO item 2): soft-`g` /dʒ/ floor gap = `86caa6jyf`; SeedProgressOptions `cvcGraduationSessionFired?` widening = `86caa6k18`; periodic-cvc-review debug seed = `86caa6k4h`. All TO DO, low priority.
- **Instrument PRs #470/#472/#473** (POST-MERGE TODO item 3) — STILL HELD OPEN, gated on Thomas's ear-pass (a blend class may need a re-tune). Cleanup decision (merge-as-registered-instrument vs close) deferred until the ear-pass passes.

**THE ONLY REMAINING GATE → Thomas's voice-QA ear-pass** on the 15 re-baked f/s/h/v/j/w blend clips at https://marian-learning.vercel.app/voice-qa.html (esp. fricatives + `bus` coda-/s/). PASS → blend effort fully DONE + instrument-PR cleanup unblocks; FAIL → which words → targeted re-bake (instruments #470/#472/#473 stay open for it). No code blockers; team idle-justified (sponsor-ear-gated phase).

## 2026-06-17 — Ear-pass DONE (report #478): fricatives WON; pass-6 audition for /v/ + /w/ dispatched

**Voice-QA report #478 (11 fails / 780 verdicts).** Decisive result: **every crackable blend class PASSED** — /f/ (fan, fig), /s/ (sip, sun, `bus` coda-s), stops, /h/ (hat, hen). The 5-pass `fːə`/`sːə` held-fricative effort succeeded in production.
- **6 fails = exactly the floored v/dʒ/w words** (by design, "just says the word"): van (/v/), jam/jet/jug (/dʒ/), web/wig (/w/). No bug — floor routing correct.
- **5 fails are STALE** (decidedAt 2026-06-13, not re-tested today): `letter-names#word.p5.hint` "O scratchy" + 4× `letter-sounds-audit#word.p2.*` "vvv scratchy". Separate from blend, pre-existing, still open. (Possible cross-benefit: if `vːə` cracks the blend /v/, it may also help the letter-sounds vvv — different tier, note for later.)

**DECIDED (Thomas, popup): pass-6 audition for /v/ + /w/** (keep /dʒ/ floored — affricate, exhausted). Foundation: /v/ is a voiced fricative floored in pass-2 BEFORE the pass-4 `vːə`/`sːə` length-mark+schwa trick was found; planner-and-canon cites "vːə extended cleanly". /w/ gets the redirect-window levers (`wː`/`wːə`/`ʊw`).

**DISPATCHED — Devon pass-6 static audition** (agentId `a42204c5b1d3d3dc7`, name `devon-pass6`, branch `devon/blend-audition-pass3` extending #472): static base64 westeurope bake of /v/ `van`=`vːə`@-25%/-40% + /w/ `web`/`wig`=`wː`/`wːə`/`ʊw`@-25%, each vs whole-word floor, onset SSML mirroring `renderBlendInnerText` full-fidelity for 1:1 port. Hash-invariant self-check. #472 stays OPEN (instrument). On completion → surface the #472 preview URL to Thomas → he A/Bs → reports winners → pass-7 production bake (port winners, un-floor won classes) → Kevin review → merge → final ear-pass.
**Devon DONE — pass-6 audition** (`dd92b30` on `devon/blend-audition-pass3`, extends #472, OPEN instrument). Static base64 westeurope clips, 0 rejections, hash-invariants PASS (every candidate ≠ floor, mutually distinct). Live: `https://marian-learning-git-devon-blend-audi-cbbf57-tsandvaers-projects.vercel.app/blend-audition.html` (pass-6 section). Candidates: van=`vːə`@-25%(lead)/`vːə`@-40%/floor; web+wig=`wː`/`wːə`(lead)/`ʊw`@-25%/floor. Onset SSML mirrors `renderBlendInnerText` full-fidelity → 1:1 port.
**KEY CONSTRAINT (Devon's finding) — pass-7 production port MUST branch off `origin/main`** (has the pass-5 `blendFullFidelity` seam), NOT off `devon/blend-audition-pass3` (stacked pre-pass-5 at candidate-f `a9dd245`, no `blendFullFidelity` flag). The audition branch mirrors candidate-f render intentionally.
**Thomas's pass-6 picks (2026-06-17, screenshots) — BOTH /v/ AND /w/ RECOVERED, no floors:**
- **/v/ `van` = `vːə`@-25%** (held + schwa tail).
- **/w/ `web` + `wig` = `wːə`@-25%** (held glide + schwa tail).
- /dʒ/ (jam/jet/jug) stays FLOORED (affricate, not auditioned). The held+schwa-tail length-mark shape (same as pass-5 `fːə`/`sːə`) cracked /v/ and /w/ too → after pass-7, ONLY /dʒ/ remains floored.

**DISPATCHED — Devon pass-7 production bake** (agentId `a88b24e2f5bd3af6d`, name `devon-pass7`, branch `devon/blend-pass7-vw-production` off `origin/main`): full-fidelity-PATH-ONLY change to `renderBlendInnerText` — add /v/=`vːə`@-25% + /w/=`wːə`@-25% per-class onset renders (mirror /f/`fːə` case), remove `v`+`w` from `BLEND_FLOOR_GRAPHEMES`/`wordIsFloored` (keep `j`/dʒ/ floored). Runtime path UNCHANGED (whole-word default — `vːə`/`wːə` are runtime-400ing length-mark IPA, bake-only). Splice byte-proof (expected-change set = v/w-onset words: van/web/wig + any others; scan ALL graphemes). Reviewer Kevin. Self-Test Report required.
**RESUME NEXT-ACTION:** awaiting devon-pass7 (`a88b24e2f5bd3af6d`) → on PR + Kevin APPROVE + CI green → I merge → Thomas's final voice-QA ear-pass on re-baked van/web/wig. /dʒ/ floored. Stale O/vvv letter-sounds fails = separate open item.

## 2026-06-17 — /whip pass: Kevin dispatched on NIT 86caa6k18 (fill idle capacity during pass-7)

Thomas-invoked /whip while pass-7 in flight. Dispatched **Kevin** (agentId `aadf670851df0d2d9`, branch `kevin/86caa6k18-seed-progress-options`) on the `SeedProgressOptions.cvcGraduationSessionFired?` widening NIT — unblocked, mechanical, NO file conflict with Devon's pass-7 (Kevin=seed helpers, Devon=`api/_tts.ts`). Ticket flipped IN PROGRESS. Reviewer Devon (queues naturally behind pass-7).
**Queued (NOT dispatched, with reason):** `86caa6k4h` (periodic-cvc-review debug seed) — Pattern A behind 86caa6k18 (consumes its typed option; vocab contract = `cvcGraduationSessionFired?` on `SeedProgressOptions`) + needs Kevin/Devon worktree (both occupied). `86caa6jyf` (soft-g floor) — conflicts with pass-7's `BLEND_FLOOR_GRAPHEMES`/`wordIsFloored` edits + latent (no soft-g word in pools); dispatch after pass-7 merges.
**Justified-idle:** Jessica (no unblocked e2e — runtime-safety is in Devon's pass-7 unit test; debug-seed e2e depends on queued 86caa6k4h), Kyle (CVC continuant-onset reorder + Wave-14 polish both sponsor-gated), Dave (no live research spike), Matt (orchestrator handles tracker).
**Surfaced (never-auto-decide, for Thomas):** (1) CVC continuant-onset reorder — re-surface trigger (pass-7 merge + final ear-pass) nearly met; (2) stale O/vvv letter-sounds fails + the vːə cross-benefit hypothesis (vːə cracked blend /v/ → may fix letter-sounds "vvv" scratchy) — post-pass-7 Devon audio spike; (3) instrument PR #470/#472/#473 cleanup after final ear-pass.

## 2026-06-17 — Devon pass-7 DONE → PR #479 (awaiting Kevin review + CI); Kevin still on NIT 86caa6k18

**Devon pass-7 COMPLETE — PR #479** (`feat(word-song): blend pass-7 — recover /v/ and /w/ from FLOOR to held onset`, branch `devon/blend-pass7-vw-production`, HEAD `c1e6d97`, OPEN + MERGEABLE). `BLEND_FRICATIVE_ONSET_IPA` += `v→vːə, w→wːə`; `BLEND_FLOOR_GRAPHEMES` -= `v,w` (keeps `j`). **Byte-proof PASS: 3 changed (van, wig, web), 337 byte-identical.** Runtime-safety verified (code-trace + 2 unit tests — `blendFullFidelity` never reaches runtime). Suite 3359 green. CI: fast-gate passed, **e2e IN_PROGRESS** (~30-55min). Self-Test Report posted. Reviewer = Kevin (cross-review).
**Devon NOFs:** (1) byte-proof divergence set must be recomputed against the ACTUAL merge-base, not copied from the prior pass's verifier — pass-7's baseline is merged-pass-5 (f/s/h already shipped) so divergence = `{v,w}` only; Devon wrote a dedicated `verifyBlendBytePreservationPass7.ts`. (2) `BLEND_FRICATIVE_ONSET_IPA` is now a misnomer (covers /w/ glide + /v/) — name kept, comment clarified. (3) `gem` soft-g gap still live (NIT 86caa6jyf), unchanged. (4) stray `tools/emma-vectorize/node_modules/` untracked in devon-wt (Wave-14 leftover, harmless).
**REVIEW ROUTING (one-Agent-per-worktree):** #479 (Devon) → **Kevin**; Kevin's NIT 86caa6k18 → **Devon**. Status (probed 2026-06-17): **Kevin's NIT PR = #480 OPEN (`be8d9c6`)** but kevin-seedopts (`aadf670851df0d2d9`) STILL RUNNING (alive, finishing self-test/report) → kevin-wt NOT free yet. Do NOT dispatch Kevin's #479 review until kevin-wt frees (one-Agent-per-worktree race). On Kevin's completion → dispatch BOTH reviews in parallel (Kevin→#479 in kevin-wt; Devon→#480 in devon-wt, devon-wt FREE). Reviewing #480 NOW is deferred only to avoid a stale-SHA review if Kevin pushes a follow-up before his task ends.
**Kevin NIT 86caa6k18 DONE → PR #480** (`be8d9c6`, IN REVIEW). +138/-34 across 4 files: `SeedProgressOptions.cvcGraduationSessionFired?` (absent-on-omit), dropped the §4.1.8 masking default in `focusNode.test.ts` (+ explicit `true` at the one all-mastered fallback test), migrated the raw-spread call site, 4 vitest cases. vitest 3362 green. Kevin NOF: a MIRROR masking instance hid in `focusNode.test.ts` (all-mastered fallback passed only via the convenience `true`) — flipping the default surfaced it; grep for no-override helper calls asserting on a default-dodged branch.

**BOTH cross-reviews DISPATCHED (parallel, different worktrees):**
- **devon-rev480** (agentId `a6becfb9c752e1b41`) — Devon reviews #480 (Kevin's NIT) in devon-wt.
- **kevin-rev479** (agentId `a2976a3d7533b63cf`) — Kevin reviews #479 (Devon's pass-7 blend) in kevin-wt. Focus: runtime-safety (blendFullFidelity bake-only) + byte-proof {v,w}.
Verdicts via `gh pr comment` `## REVIEW VERDICT:` (self-review block — same auth user).
**BOTH REVIEWS DONE — APPROVE×2 (0 agents in flight):** Kevin **APPROVE** on #479 (pass-7; verified runtime-safety 4-hop trace + byte-proof reproduced 3-changed + suite 3359 — comment 4730280015); Devon **APPROVE** on #480 (NIT; AC4 de-mask genuine, not relocated — comment 4730244158). Both PRs now gated ONLY on e2e (in_progress at record).
**Kevin NOFs (#479):** (1) `gh pr comment --body` with inline backticks gets mangled by the bash wrapper (command-substitution fires inside `--body "..."`) → use `--body-file` for any verdict body with backticks/fences; (2) the pass-7 byte-proof verifier (4-cell discrimination + `git show origin/main:` baseline, bake-script-independent) is a reusable template for future single-class splice re-bakes.
**DRAIN-AND-SAVE (async) in progress:** /drain-and-save-session invoked. Tripwire `a998da57`→re-armed (~15:11) merges #480 (→flip 86caa6k18 COMPLETE) + #479 (→van/web/wig flip needs-retest for Thomas's FINAL ear-pass) on e2e-green, then chains into save-session + emits the resume one-liner. No new dispatches; no recurring cron. Instrument PRs #470/#472/#473 INTENTIONALLY OPEN (held for final ear-pass) — do NOT block drain.
**RESUME NEXT-ACTION:** tripwire merges both on e2e-green → saves. If e2e fails → surface blocker, no save. /dʒ/ floored.

## 2026-06-17 — DRAIN COMPLETE: #479 + #480 MERGED; only Thomas's final blend ear-pass remains

Both closure PRs landed (e2e SUCCESS both, APPROVE×2):
- **#480 MERGED** (NIT 86caa6k18, 13:28:46Z) → ticket flipped COMPLETE. `SeedProgressOptions.cvcGraduationSessionFired?` exposed; §4.1.8 mask de-fanged.
- **#479 MERGED** (blend pass-7, 13:28:51Z) → **origin/main `7b9e657`**. /v/ (van)=`vːə`@-25% + /w/ (web/wig)=`wːə`@-25% recovered from FLOOR; /dʒ/ (jam/jet/jug) stays floored. Runtime-safe (bake-only full-fidelity). Byte-proof: 3 changed.
- 0 agents in flight. No recurring cron. Local main now STALE (origin = `7b9e657`) — ALWAYS branch from origin/main.

**THE ONLY OPEN BLEND ITEM → Thomas's FINAL voice-QA ear-pass** (next session, sponsor-side): after the Vercel prod deploy, van/web/wig flip to needs-retest on https://marian-learning.vercel.app/voice-qa.html. PASS → entire blend DONE (all classes sounded-out except the deliberately-floored /dʒ/) → then instrument PRs #470/#472/#473 cleanup + the 2 surfaced never-list items (CVC continuant-onset reorder re-surface; vːə cross-benefit for the stale letter-sounds "vvv" fail) unblock. FAIL → targeted re-bake (instruments stay open).
**Still queued (post-ear-pass):** NIT 86caa6k4h (periodic-cvc-review debug seed — now unblocked, consumes #480's typed option) + 86caa6jyf (soft-g floor — now mergeable, pass-7 done). Stale O/vvv letter-sounds fails (report #478) = separate open item.
