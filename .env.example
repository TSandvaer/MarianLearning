# Marian Tutor — example environment file.
# Copy to .env.local and fill in. NEVER commit .env.local.

# --- Browser-side (VITE_*) -------------------------------------------------
# URL of the Vercel Function that proxies requests to the Anthropic API.
# Defaults to /api/claude (same origin) — only override for unusual local
# setups, e.g. running `vercel dev` on a different port.
VITE_CLAUDE_API_ENDPOINT=/api/claude

# Shared-secret bearer token for /api/progress (cloud-sync, ticket
# 86c9pkfyu). MUST match `PROGRESS_API_SECRET` server-side for the same
# environment. This value ships in the browser bundle by design — the
# protection model is "make casual abuse non-trivial," not zero-trust.
# Generate with `openssl rand -hex 32`. Do NOT reuse `ANTHROPIC_API_KEY`.
VITE_PROGRESS_API_SECRET=

# --- Server-side (Vercel Function only) ------------------------------------
# Anthropic API key. SERVER ONLY. Read by the /api/claude function from
# process.env. It must NEVER be prefixed with VITE_ and must NEVER be added
# to the browser bundle. In production, set it in the Vercel dashboard
# (Project Settings -> Environment Variables) and never check it in here.
ANTHROPIC_API_KEY=

# Optional: extra origins allowed by the /api/claude CORS handler, comma
# separated. The function already allows http://localhost:5173 and the
# deployment's own Vercel URL. Leave blank in normal use.
CLAUDE_API_EXTRA_ORIGINS=

# Azure Speech subscription key. SERVER ONLY. Read by the /api/_tts.ts
# pipeline from process.env. It must NEVER be prefixed with VITE_ and
# must NEVER be added to the browser bundle. In production, set it in
# the Vercel dashboard (Project Settings -> Environment Variables) and
# never check it in here. Free tier (F0) is sufficient for Marian's
# usage (~60K chars/mo against the 500K/mo free quota).
AZURE_SPEECH_KEY=

# Azure region the Speech resource lives in (e.g. westeurope, eastus).
# Pinned per resource — F0 free tier is region-locked. Production uses
# westeurope.
AZURE_SPEECH_REGION=westeurope

# --- Vercel KV / Upstash Redis (cloud-sync, ticket 86c9pkfyu) --------------
# These are auto-populated when an Upstash Redis (or legacy Vercel KV)
# store is linked to the Vercel project. The `@upstash/redis` SDK reads
# them automatically via `Redis.fromEnv()` — do NOT rename.
KV_REST_API_URL=
KV_REST_API_TOKEN=
# Optional read-only token. Not consumed by /api/progress today (we use
# the read/write token above for both GET and POST), but Vercel
# populates it for completeness.
KV_REST_API_READ_ONLY_TOKEN=

# Server-side bearer token for /api/progress. SERVER ONLY. MUST match
# VITE_PROGRESS_API_SECRET above within the same environment. Mismatch
# means every cloud-sync request fails 401.
PROGRESS_API_SECRET=
