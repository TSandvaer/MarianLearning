# Word Song — digraphs voiceless `th` /θ/ word list (7 words, all fully decodable; DRAFT — pending Dave's th-addendum validation)

**Ticket:** TBD — Matt to file. This spec lands the design-side word selection for the THIRD digraph tier. Implementation downstream (Kevin's planner-widen + canon-bake against the `digraphs-th-voiceless` sibling node already created in PR #211; Devon's `wordPack.ts` rows + picture-pack embed wiring).
**Status:** **DRAFT — every word choice below is marked "pending Dave's phonics validation".** Dave is producing `design/research/digraph-th-addendum.md` (the /θ/-specific authority — recommended target-word inventory, voiced-vs-voiceless boundary, hybridMode candidates) **in parallel**; that file was NOT yet on disk at the time this spec was drafted. When the addendum lands, §0 must be filled with the reconciliation (the ch tier's §0 is the exact template) and the pending markers resolved or the pool revised. The pool below is drafted from (a) the structural ch/sh template and (b) the standard decodable /θ/ word inventory cited in `design/research/digraph-acquisition-marian.md` §Q3/§Q4. **Conservative posture: all voiced-`/ð/` words (`the, this, that, they, them, then, than, there`) are EXCLUDED entirely — see §1.4.**
**Predecessor research:** `design/research/digraph-acquisition-marian.md` (Dave, 2026-05-14 — sh/ch/th sequencing §Q2, the /θ/→/t/ error profile §Q3, word count §Q4, isolation-vs-interleaving §Q5; the voiceless-before-voiced rationale §Q2 Sources 7–9). **`design/research/digraph-th-addendum.md` (Dave — IN PARALLEL, the /θ/-specific authority; NOT yet on disk — §0 reconciliation pending).**
**Companion spec:** `design/word-song/digraphs-th-picture-pack-prompts.md` (Phase-1 MJ prompt sheet sibling).
**Structural predecessor (cloned section-for-section):** `design/word-song/digraphs-ch-word-list.md` — the ch tier spec'd 2026-05-14 (PR #225 spec + #227 wordPack; planner/canon mid-build). This spec mirrors its structure; the _content_ is drafted from the standard /θ/ inventory and **must be reconciled against Dave's th-addendum when it lands** (§0).
**Predecessor pool specs (format reference):** `design/word-song/digraphs-sh-word-list.md`, `design/word-song/short-e-pool-expansion.md`, `design/word-song/short-i-pool-expansion.md`.

---

## 0. Reconciliation with Dave's `digraph-th-addendum.md` — PENDING

**This section is a stub.** Dave's `design/research/digraph-th-addendum.md` is being produced in parallel and was NOT on disk when this spec was drafted. It is the **authority on which voiceless-th words are level-appropriate, the voiced-vs-voiceless boundary, and the hybridMode candidates.** When it lands:

1. Fill this section with the reconciliation table (the ch tier's §0 table — `Point | Pre-addendum draft | Dave's addendum (LOCKED)` — is the exact template).
2. Resolve every "pending Dave's phonics validation" marker in §1: either confirm the draft pool verbatim, or adopt Dave's revised inventory.
3. If Dave's pool differs, the §1 audit table, the §1 final-pool table, the §2 distractor matrix, and the §8 AC list all move to match — the structural skeleton (audit lens, distractor classes, AC numbering, schema reuse) stays unchanged, as it did across the sh→ch reconciliation.

**Anticipated divergence axes** (from the ch reconciliation pattern + the parent research note):

- **Pool**: this draft proposes `thin, thick, thumb, path, bath, math, moth`. Dave's addendum may swap members — `thin/thick` are the canonical word-initial /θ/ anchors (parent note §Q3 Source 7 cites `thin, think, thank, three`), `thumb` is a body-part anchor (the ch tier's `chin` precedent — body parts are the highest-familiarity vocabulary class), and `path/bath/math/moth` are the word-final /θ/ stock (parent note §Q2 Source 7: voiceless /θ/ "appears at word endings in path, with, math"). `think` and `thank` are strong word-initial candidates the draft holds in reserve (§7 Q3) because `think` is borderline-abstract and `thank` is a social action — both picture-brief-dependent.
- **`hybridMode`**: this draft proposes **ZERO** (mirrors ch). Every drafted word uses a short vowel Marian has formally covered (`thin/thick` short-i, `thumb` short-u, `path/bath/math` short-a, `moth` short-o). Dave's addendum confirms or revises.
- **th-final words**: this draft INCLUDES word-final th (`path, bath, math, moth`) deliberately — unlike the ch tier, which shipped word-initial-only. The parent research note §Q2 Source 7 explicitly flags that voiceless /θ/ "appears at word beginnings in content words (thin, think, thank, three) AND word endings (path, with, math)" — covering both positions is part of the /θ/ lesson. Dave's addendum is the authority on whether word-final th belongs in the FIRST th tier or a follow-up arc.
- **`th`-spelling-of-/θ/ vs `-the`/`-th` variants**: the draft pool is all clean `th` grapheme (no trigraphs, no silent-letter complications). Dave confirms.

**Until §0 is filled, treat this spec as DRAFT — do not file the impl tickets.** The ch tier's §0 reconciliation gate (its old AC13) is the model: the spec is not "ready for Thomas review" until reconciled.

---

## Why this spec, why now

Per Dave's research (`digraph-acquisition-marian.md` §Q1/§Q2 + §Q3, and pending the th-addendum):

- The digraph introduction order is **`sh` first, `ch` second, voiceless `th` /θ/ third.** Voiced `/ð/` is deferred entirely (sight-word domain — parent note §Q2 4th row, §Q3 "th voiced", Bottom-line).
- `sh` shipped 2026-05-14 (`digraphs-sh` tier — 7-word pool, planner directive, committed canon, regression spec). `ch` is mid-build (PR #225 spec + #227 wordPack; planner/canon in flight). `th` (voiceless /θ/) is the next tier in the locked sequence.
- **Voiceless /θ/ third is a deliberate pedagogical choice, not just an ordering** (parent note §Q2 Sources 7–9, Bottom-line): voiceless /θ/ "requires the most explicit articulation scaffolding (no L1 reference) and the picturable content-word stock is smaller than sh or ch." It is introduced after sh and ch are consolidated so the conceptual load of the new articulation is manageable — "two letters, one sound" is by now thoroughly reviewed, not new.
- **/θ/ is the HARDEST of the three digraphs for Marian** (parent note §Q3, Sources 10–12): Tagalog has no /θ/ and no /ð/. The substitution `/θ/ → /t/` ("think" → "tink", "thank" → "tank") is "the most systematic of the three" and "structural, not a beginner error that resolves with exposure." Crucially — **this is a PRODUCTION error, not a perception error.** Parent note §Q3 is explicit: "In the chip-tap format, the production error is not directly testable — what matters is whether Marian can match the spoken word (Emma says 'thin') to the text chip 'thin' vs. 'tin' or 'sin.' The spelling difference makes this visually unambiguous as long as the distractor words don't also start with th." The chip-tap recognition task is achievable; the /θ/→/t/ substitution does not block it.
- **The ONE thing /θ/ has that sh and ch did not: it needs an articulation-placement scaffold** (parent note §Q3): "Emma's instruction note: 'th together says a special sound — put your tongue between your teeth and blow.' A brief animation of Emma's mouth position (or an illustration of a mouth) would help more than TTS alone here." This is the th-tier analogue of the ch tier's c-says-/k/ script clause — see §4.
- **Voiced `/ð/` must be EXCLUDED from this tier entirely** (parent note Bottom-line, §Q3): `the, this, that, they, them, then, than, there` are voiced /ð/ function words — low-imageability, the /ð/→/d/ substitution is "deeply embedded in Philippine English at all levels", and they "share a grapheme with the voiceless form Marian will have just learned, creating a two-realizations-of-one-grapheme puzzle that exceeds the scope of first digraph introduction." If they ever appear, they arrive as **sight words** (Emma reads, Marian matches text), NOT as digraph phonics items. **No voiced-th word is a th-tier target or distractor.** See §1.4.
- 8–10 words per digraph; the parent note §Q4 notes the th picturable content-word stock "is smaller than sh or ch" — the th tier ships at **7** (matching the sh + ch tier depth), pending Dave's addendum confirming 7 is adequate.
- **Vocabulary-familiarity is the binding constraint** — same as sh and ch (parent note §Q4). Picture-grounding the chip is the design response. The drafted th pool is chosen for picture-stability; §3 carries the per-word picture concerns.

**Scope of this spec:** word selection (DRAFT — pending Dave's th-addendum, §1), focus-node naming guidance (not architecture — the `digraphs-th-voiceless` `SkillNode` already exists per Kevin's PR #211 3-sibling split), picture-pack requirements summary, the `phoneme: '/θ/'` schema posture and **why th SETS `phoneme` where ch omitted it** (§6.1 — the load-bearing schema divergence from ch), the `hybridMode` posture (§6.2), and the open questions for Thomas (§7).

**Out-of-scope (deferred):**

- `SkillNode` shape — already locked in PR #211 (`digraphs-sh`, `digraphs-ch`, `digraphs-th-voiceless` — 3 sibling nodes). This spec consumes the `digraphs-th-voiceless` node.
- **Voiced `/ð/` content** — deferred indefinitely from chip-tap decoding (sight-word domain, parent note Bottom-line / §Q3). `the/this/that/they` are a future sight-word-tier concern, NOT this spec's. See §1.4.
- **wh / ck digraphs** — parent note §Q2 "On wh and ck": out of scope; they follow th in the skill tree as separate specs.
- **sh/ch/th three-way interleaving rules** — parent note §Q5 + Bottom-line: interleave sh and ch once both reach ~70%; th joins the interleave only after IT is consolidated. Cross-pool distractors (`thin` vs `shin` vs `chin`) belong to the _interleaving_ phase, NOT the th-introduction tier (§2 below). A planner-side session-composition decision for a future interleaving spec.
- Distractor architecture refactor — the sh tier built the cross-orthography distractor pattern; ch reused it; th reuses it again. §2 / §6 specify the th-specific rows, no new architecture.

---

## 1. Word selection — DRAFT pool of 7 voiceless-th words (PENDING Dave's th-addendum)

> **Every word in this section is marked "pending Dave's phonics validation."** The pool is drafted from the standard decodable /θ/ inventory in the parent research note (`digraph-acquisition-marian.md` §Q3 cites `thin, think, thank, three` word-initial and `path, with, math` word-final) cross-referenced against Marian's formal vowel instruction. Dave's `digraph-th-addendum.md` is the authority — §0 reconciliation gates this spec.

### Brief / source pool

Candidate voiceless-/θ/ words, cross-referenced against Marian's formal vowel instruction (short-a, -o, -u, -i, -e all formally introduced as of 2026-05-14):

- **Word-initial th-CVC (short vowel Marian has covered):** `thin, this`(✗voiced)`, thick` (short-i); `thud, thumb` (short-u); `thank` (short-a); `theft` (short-e, CVCC). Plus near-CVC content words `think` (short-i + `nk` blend), `thaw`(long), `three`(blend+long), `thorn`(r-controlled).
- **Word-final th (CVCC):** `bath, path, math` (short-a); `moth, broth, cloth` (short-o); `with`(short-i — borderline, /ð/ in some dialects); `mouth, tooth, teeth, booth` (long/diphthong vowels — out of formal instruction).
- **EXCLUDED — voiced /ð/:** `the, this, that, they, them, then, than, there, this, with`(dialect-variable). Not candidates — see §1.4.

### DRAFT recommended pool of 7 (PENDING Dave — this spec's draft, NOT yet locked)

> **`thin, thick, thumb, path, bath, math, moth`**

Draft rationale (pending Dave's §3-equivalent):

- All use short vowels Marian has formally covered (short-i ×2, short-u ×1, short-a ×3, short-o ×1).
- Covers **BOTH th-positions**: word-initial (`thin, thick, thumb` — 3) and word-final (`path, bath, math, moth` — 4). The parent research note §Q2 Source 7 flags both positions as part of the /θ/ lesson — unlike the ch tier (word-initial-only), the th tier deliberately spans both. **Pending Dave: is word-final th in the FIRST th tier, or a follow-up arc?**
- All are picturable single-subject concrete objects, or a body part, or have a strong concrete depiction (`math` = numbers/sum, `path` = a trail).
- **None require `hybridMode` treatment** — every vowel pattern is within formal instruction (§6.2). Pending Dave's confirmation.
- `thin` is the canonical word-initial /θ/ anchor (parent note §Q3 uses "thin" as its worked recognition example) and forms the **`thin`/`tin` th/t-contrast minimal pair** that the §2 distractor matrix is built around.
- `thumb` is the body-part anchor — the ch tier's `chin` precedent (body parts are the highest-familiarity vocabulary class; "the picture is literally on Marian's body").
- `bath/path/math` are a tight word-final /θ/ short-a rhyme family with strong t-contrast minimal pairs (`bath/bat`, `path/pat`, `math/mat`).

### Audit of the candidate set (DRAFT — pending Dave's th-addendum)

Audited against v1 word-pack constraints (single-syllable, picturable, distinct silhouette at 96pt, vocabulary-familiar for an L2 8yo, clean `th` grapheme). Same audit lens as `digraphs-ch-word-list.md` §1 and `digraphs-sh-word-list.md` §1. **Every verdict is provisional — Dave's th-addendum overrides where it differs, exactly as Dave's ch-addendum overrode the ch pre-addendum draft.**

| Word                                               | th-position | Pattern                       | Concrete                                                                                        | Picturable for L2 8yo                                                                                                                                    | Vocabulary risk                                                                               | Silhouette risk                                                                                                                                                                                                                                                    | Verdict (DRAFT — pending Dave)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| -------------------------------------------------- | ----------- | ----------------------------- | ----------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **thin**                                           | initial     | th-CVC (short-i)              | ✓ (a thin object — a thin line / a thin slice / a thin character)                               | ✓ **with the right brief** — a clearly thin single object next to nothing (no thick comparator needed if the object reads as slender). Dave's authority. | Moderate — "thin" is an adjective; the picture must ground "thinness" as the salient property | Low — a slender silhouette is distinct                                                                                                                                                                                                                             | **KEEP (DRAFT)**. The canonical word-initial /θ/ anchor; parent note §Q3 worked example. Forms the `thin`/`tin` th/t-contrast minimal pair — the lead trap pair in §2. Picture brief is load-bearing (adjective → must read as "thinness"). **Pending Dave's phonics validation.**                                                                                                                                                                                                                                                                                                                                                                                                |
| **thick**                                          | initial     | th-CVCC (short-i; `ck` coda)  | ✓ **with the right brief** — a clearly chunky/thick single object (a thick book, a thick slice) | ✓ — thickness reads as a chunky silhouette                                                                                                               | Moderate — adjective; same grounding concern as `thin`                                        | Low                                                                                                                                                                                                                                                                | **KEEP (DRAFT)**. The `thin`/`thick` antonym pair is itself a useful in-pool contrast (Marian distinguishes by picture). `ck` coda is acoustically inert for chip-tap recognition (spells /k/ she already decodes — same posture as the ch tier's `chick`). **Pending Dave's phonics validation.**                                                                                                                                                                                                                                                                                                                                                                                |
| **thumb**                                          | initial     | th-CVCC (short-u; silent `b`) | ✓ (a thumb — body part)                                                                         | ✓ — a hand with the thumb as the salient feature; the ch-tier `chin` precedent                                                                           | High — body part, universally known                                                           | Low — a thumbs-up hand silhouette is distinct                                                                                                                                                                                                                      | **KEEP (DRAFT)**. Body-part anchor — highest-familiarity vocabulary class (the ch tier's `chin` rationale). **CAVEAT — the silent `b`:** `thumb` has a silent-letter complication (`mb` → /m/) that `thin/thick` do not. The `th` digraph is still the lesson and the `b` is acoustically inert for chip-tap RECOGNITION, but **this is exactly the kind of call Dave's th-addendum must rule on** — it may be a `hybridMode` candidate, or Dave may swap it for `thud` (clean short-u th-CVC, no silent letter — but harder to picture). **Pending Dave's phonics validation — flagged as the pool's #1 uncertainty.**                                                           |
| **path**                                           | final       | CVCC (short-a; th-final)      | ✓ (a path — a trail through grass)                                                              | ✓ — a simple winding trail reads clearly                                                                                                                 | Low — common word, concrete                                                                   | Low                                                                                                                                                                                                                                                                | **KEEP (DRAFT)**. Word-final /θ/ short-a; forms the `path`/`pat` th/t-contrast minimal pair. **Pending Dave: word-final th in the first tier?**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| **bath**                                           | final       | CVCC (short-a; th-final)      | ✓ (a bathtub)                                                                                   | ✓ — a bathtub is storybook-universal single object                                                                                                       | Low — common word                                                                             | Low-moderate vs a generic tub/basin — manageable with bubbles/feet detail                                                                                                                                                                                          | **KEEP (DRAFT)**. Word-final /θ/ short-a; forms the `bath`/`bat` th/t-contrast minimal pair — `bat` is a shipped CVC target, a strong real-word trap. **Pending Dave: word-final th in the first tier?**                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| **math**                                           | final       | CVCC (short-a; th-final)      | ✓ **with the right brief** — numbers / a simple sum / a chalkboard with `2+2`                   | Moderate — "math" is a domain noun; the picture must ground it as "numbers/sums", not "school"                                                           | Low                                                                                           | **KEEP (DRAFT)**. Word-final /θ/ short-a; forms the `math`/`mat` th/t-contrast minimal pair — `mat` is a shipped CVC target, a strong real-word trap. Picture brief is load-bearing (domain noun → must read as "numbers"). **Pending Dave's phonics validation.** |
| **moth**                                           | final       | CVCC (short-o; th-final)      | ✓ (a moth — animal)                                                                             | ✓ — a moth silhouette (rounded wings, fuzzy body)                                                                                                        | Moderate — "moth" vs "butterfly" confusion; brief must force the moth read                    | Moderate vs a butterfly — manageable with the fuzzy-body / drab-colour / folded-wing detail                                                                                                                                                                        | **KEEP (DRAFT)**. The short-o entry — the pool's one non-short-a word-final th, giving vowel spread. **Pending Dave's phonics validation; pending the moth-vs-butterfly picture brief grounding.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| **think**                                          | initial     | th + short-i + `nk` blend     | ✗-ish — abstract (cognition); pictures read as "a thought bubble" / "a confused face"           | Marginal — concept known, picture-grounding unreliable                                                                                                   | Moderate — common word                                                                        | n/a                                                                                                                                                                                                                                                                | **NOT in the DRAFT 7** — abstract, picture-grounding unreliable (a thought-bubble reads as "idea", not "think"). The `nk` blend also stacks complexity. **§7 Q3 optional-depth candidate** if Dave's addendum can supply a grounding brief. Pending Dave.                                                                                                                                                                                                                                                                                                                                                                                                                         |
| **thank**                                          | initial     | th + short-a + `nk` blend     | ✗-ish — a social action; pictures read as "a gift" / "a hug" / "two people"                     | Marginal — the ch-tier `chat` precedent shows social actions CAN be grounded with a specific composition brief, but `thank` is harder than `chat`        | Low                                                                                           | n/a                                                                                                                                                                                                                                                                | **NOT in the DRAFT 7** — but a real candidate IF Dave's addendum supplies a composition brief (the ch-tier `chat` precedent). The `nk` blend is the same concern as `think`. **§7 Q3 optional-depth candidate.** Pending Dave.                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| **three**                                          | initial     | th + `r` blend + long-e       | ✓ (the numeral 3 / three objects)                                                               | ✓ — highly picturable                                                                                                                                    | Low — universal                                                                               | Low                                                                                                                                                                                                                                                                | **NOT in the DRAFT 7** — the `thr-` onset is a **digraph + consonant-blend stack** (`th` + `r`), which violates digraph-isolation exactly as `chr-` did for the ch tier (ch-word-list §1 phonetic-spread check). The long-e vowel is also outside the short-vowel set. **REJECT** unless Dave's addendum explicitly allows blend-onset th — unlikely given the ch tier rejected `chr-`. Pending Dave.                                                                                                                                                                                                                                                                             |
| **thud, thaw, thorn, theft, broth, cloth, with**   | varies      | varies                        | varies                                                                                          | varies                                                                                                                                                   | varies                                                                                        | varies                                                                                                                                                                                                                                                             | **NOT in the DRAFT 7.** `thud` = clean short-u th-CVC but hard to picture (an onomatopoeic impact); a `thumb`-replacement candidate IF Dave drops `thumb` for the silent-`b` reason. `thaw` = long-vowel. `thorn` = r-controlled. `theft` = abstract + short-e CVCC. `broth/cloth` = short-o word-final th alternatives to `moth` (picture-stable but `broth` is vocab-marginal for an L2 8yo, `cloth` collides with the `sh`-tier register). `with` = function word AND /ð/-in-some-dialects (voicing-ambiguous — a hard exclude). All available as §7 Q3 optional-depth / substitution candidates; **Dave's addendum is the authority on which, if any, swap into the core 7.** |
| **the, this, that, they, them, then, than, there** | initial     | th + vowel (VOICED /ð/)       | function words / low-imageability                                                               | ✗                                                                                                                                                        | n/a                                                                                           | n/a                                                                                                                                                                                                                                                                | **EXCLUDED ENTIRELY — voiced /ð/, NOT voiceless /θ/.** Not th-tier candidates in any role (target OR distractor). See §1.4 — this is the load-bearing exclusion for the voiceless-th tier. Parent note Bottom-line / §Q3.                                                                                                                                                                                                                                                                                                                                                                                                                                                         |

### The pool-shape decision — DRAFT, pending Dave's th-addendum

The sh tier faced a genuine pool-shape decision (short-vowel-only vs long-vowel allowance) because its short-vowel sh-initial stock was thin. The ch tier did NOT face it (Dave's ch-addendum settled it — rich short-vowel stock). **For th, the pool-shape question is whether word-final th belongs in the first tier** — the parent research note flags both positions as part of the /θ/ lesson, but Dave's th-addendum is the authority on first-tier scope. This draft INCLUDES word-final th (4 of 7); Dave may pull it to a follow-up arc, leaving a thinner word-initial-only first tier (and then `think`/`thank` from §7 Q3 likely come in to reach 7). **§0 reconciliation resolves this.**

### DRAFT final v1 voiceless-th pool (7 words — PENDING Dave's th-addendum)

| #   | Word  | Vowel inside              | th-position | Picture status | Category  | hybridMode      | phoneme | Notes (DRAFT — pending Dave's phonics validation)                                                                                                                                                                                                                                                      |
| --- | ----- | ------------------------- | ----------- | -------------- | --------- | --------------- | ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | thin  | short-i (`/ɪ/`, mastered) | initial     | NEW            | object    | false           | `/θ/`   | Canonical word-initial /θ/ anchor (parent note §Q3 worked example). Lead word. Forms `thin`/`tin` th/t-contrast minimal pair. Picture brief load-bearing: must read as "thinness" (adjective → salient property). **Pending Dave.**                                                                    |
| 2   | thick | short-i (`/ɪ/`, mastered) | initial     | NEW            | object    | false           | `/θ/`   | The `thin`/`thick` antonym pair is a useful in-pool picture contrast. `ck` coda acoustically inert for chip-tap (spells /k/ Marian decodes — `chick` precedent). Picture brief: must read as "thickness". **Pending Dave.**                                                                            |
| 3   | thumb | short-u (`/ʌ/`, mastered) | initial     | NEW            | body-part | false (PENDING) | `/θ/`   | Body-part anchor — highest-familiarity vocabulary class (ch-tier `chin` rationale). **CAVEAT: silent `b` (`mb`→/m/).** Acoustically inert for RECOGNITION, but Dave's addendum must rule whether this is a `hybridMode` candidate or a swap-for-`thud` case. **Pool's #1 uncertainty — pending Dave.** |
| 4   | path  | short-a (`/æ/`, mastered) | final       | NEW            | object    | false           | `/θ/`   | Word-final /θ/ short-a. Forms `path`/`pat` th/t-contrast minimal pair. Picture: a simple winding trail. **Pending Dave: word-final th in the first tier?**                                                                                                                                             |
| 5   | bath  | short-a (`/æ/`, mastered) | final       | NEW            | household | false           | `/θ/`   | Word-final /θ/ short-a. Forms `bath`/`bat` th/t-contrast minimal pair — `bat` is a shipped CVC target (strong real-word trap). Picture: a bathtub (storybook-universal). **Pending Dave: word-final th in the first tier?**                                                                            |
| 6   | math  | short-a (`/æ/`, mastered) | final       | NEW            | object    | false           | `/θ/`   | Word-final /θ/ short-a. Forms `math`/`mat` th/t-contrast minimal pair — `mat` is a shipped CVC target (strong real-word trap). Picture brief load-bearing: numbers/a simple sum, NOT "school". **Pending Dave.**                                                                                       |
| 7   | moth  | short-o (`/ɒ/`, mastered) | final       | NEW            | animal    | false           | `/θ/`   | The short-o entry — the pool's vowel-spread word-final th. Picture brief: fuzzy body + drab colour + folded wings to stay distinct from a butterfly. **Pending Dave.**                                                                                                                                 |

**ZERO `hybridMode: true` entries in the DRAFT** — but `thumb`'s silent `b` is flagged as the pool's #1 uncertainty pending Dave's th-addendum (§6.2). **All 7 SET `phoneme: '/θ/'`** — the load-bearing schema divergence from the ch tier (which omitted `phoneme`); see §6.1.

### Pool composition cross-check (DRAFT)

- **All 7 are concrete nouns / a body part** — each has a stable chip read (with the picture briefs in §3 / the companion prompt sheet).
- **All 7 are picture-distinct from existing CVC + sh + ch pack pictures** — `thin`/`thick` are slender-vs-chunky objects (unique as a property-contrast pair); `thumb` is a hand (unique — no hand in prior packs); `path` is a trail; `bath` is a tub; `math` is numbers/a sum; `moth` vs everything (closest neighbour is no existing pack word — butterfly is not in any pack).
- **Position spread:** word-initial th (3: thin, thick, thumb), word-final th (4: path, bath, math, moth) — **both /θ/ positions covered**, unlike the ch tier (initial-only). This is deliberate per the parent research note; pending Dave's confirmation it belongs in the first tier.
- **Vowel spread:** short-i (2: thin, thick), short-u (1: thumb), short-a (3: path, bath, math), short-o (1: moth) — 4 vowel positions, all within Marian's formal instruction. Short-a is over-represented (3 of 7, all word-final) — a real composition note for the planner: **avoid all-short-a / all-word-final trios.**
- **Category spread:** object (4 — thin, thick, path, math), body-part (1 — thumb), household (1 — bath), animal (1 — moth).
- **`hybridMode: true` split:** 0 of 7 (DRAFT). Like the ch tier (also 0), unlike the sh tier (3 of 7). `thumb`'s silent `b` is the one pending question (§6.2).
- **Picture-brief-dependence:** 3 of 7 carry a load-bearing brief (`thin` = must read as thinness, `math` = numbers not school, `moth` = moth not butterfly). Lower than ch's count but non-zero.

### Phonetic spread within the pool (digraph isolation check)

The whole point of this tier is for Marian to internalize **`th = /θ/`** as a single decoded unit — _and_ to NOT misread it as `/t/` (the /θ/→/t/ substitution, parent note §Q3). Every word in the pool contains the clean `th` grapheme.

- **th-initial position:** ✓ 3 of 7 (`thin, thick, thumb`).
- **th-final position:** ✓ 4 of 7 (`path, bath, math, moth`). Deliberate — parent note §Q2 Source 7 flags both positions. **Pending Dave: confirm word-final th belongs in the first tier.**
- **th + consonant blend at onset:** ✗ NONE in the DRAFT 7 (no `thr-` words; `three` rejected — `thr-` stacks digraph + blend, violating digraph-isolation exactly as `chr-` did for ch). `think`/`thank` carry a `nk` CODA blend — held in §7 Q3 reserve partly for that reason.
- **Single-syllable:** ✓ ALL 7.
- **Voiced /ð/ words:** ✗ NONE — excluded entirely (§1.4).
- **NO trigraph / silent-letter complications EXCEPT `thumb`'s silent `b`** — flagged as the pool's #1 pending question (§6.2). `thick`'s `ck` and the word-final `th`'s themselves are clean.
- **/θ/→/t/ trap mitigation:** Emma's first-encounter script (§4) explicitly scaffolds the tongue-between-teeth articulation; the §2 distractor matrix uses the **th/t contrast** as the trap class (the th-tier analogue of sh's sh/s and ch's ch/s).

### Vocabulary-familiarity audit (the binding constraint per parent note §Q4)

For each of the DRAFT 7, the picture-grounding-without-explanation check:

| Word  | Marian's L1 (Tagalog) referent                     | Picture-grounds-without-explanation?                                               | Audio-anchor risk                                      |
| ----- | -------------------------------------------------- | ---------------------------------------------------------------------------------- | ------------------------------------------------------ |
| thin  | manipis (common adjective)                         | ✓ **with the brief** — a clearly slender object reads as "thin"                    | Moderate — adjective; picture must ground the PROPERTY |
| thick | makapal (common adjective)                         | ✓ **with the brief** — a clearly chunky object reads as "thick"                    | Moderate — adjective; same as `thin`                   |
| thumb | hinlalaki (body part — universally known)          | ✓ — a thumbs-up hand is unambiguous; body parts universal                          | Low — body part, universal                             |
| path  | daan / landas (common — a trail)                   | ✓ — a winding trail reads clearly                                                  | Low — concrete, common                                 |
| bath  | ligo / banyo (everyday — bathing is daily routine) | ✓ — a bathtub is storybook-universal                                               | Low — concrete, everyday                               |
| math  | matematika / "math" (school subject — known)       | ✓ **with the brief** — numbers / a sum grounds it; "school" would not              | Moderate — domain noun; picture must ground "numbers"  |
| moth  | gamu-gamo (everyday in PH — moths around lights)   | ✓ **with the brief** — fuzzy-bodied insect; `gamu-gamo` is everyday Filipino vocab | Moderate — moth-vs-butterfly picture risk              |

**4 of 7 are vocabulary-familiar without scaffolding** (thumb, path, bath, moth — strong L1 referents). 3 of 7 (`thin`, `thick`, `math`) rely on the picture brief + audio anchor to ground the meaning — `thin`/`thick` because they are adjectives (the property must be the salient picture feature), `math` because it is a domain noun. **This is a comparable risk profile to ch's** (ch had 3 picture-brief-dependent words too). **Pending Dave's th-addendum confirming the vocabulary calls.**

### Tagalog phonology check per word — `/θ/` → `/t/` risk (PRODUCTION-only)

The `/θ/`→`/t/` substitution is structural across ALL th-words for Marian (parent note §Q3, Sources 10–12: "the most systematic of the three", "structural, not a beginner error that resolves with exposure"). **But it is PRODUCTION-only — irrelevant to chip-tap recognition** (parent note §Q3 explicit: "the production error is not directly testable — what matters is whether Marian can match the spoken word ... to the text chip"). Emma's Azure neural voice produces /θ/ correctly; the chip-tap test is whether Marian matches the heard /θ/ audio to the chip starting (or ending) with `th`, NOT to the chip with `t`.

Per-word substitution prediction:

| Word  | Predicted L1 /θ/→/t/ substitution (production-only) | th/t-contrast trap word | Both real English words?               | Visual discriminator                |
| ----- | --------------------------------------------------- | ----------------------- | -------------------------------------- | ----------------------------------- |
| thin  | "tin" (REAL WORD)                                   | tin                     | YES — direct minimal pair              | th- vs t- (visual)                  |
| thick | "tick" (REAL WORD)                                  | tick                    | YES — direct minimal pair              | th- vs t-                           |
| thumb | "tum" / "tumb" (non-word; "tum" is informal)        | —                       | NO clean real-word trap                | th- vs t- (use a th-pool neighbour) |
| path  | "pat" (REAL WORD)                                   | pat                     | YES — direct minimal pair              | -th vs -t                           |
| bath  | "bat" (REAL WORD — shipped CVC target)              | bat                     | YES — direct minimal pair, strong trap | -th vs -t                           |
| math  | "mat" (REAL WORD — shipped CVC target)              | mat                     | YES — direct minimal pair, strong trap | -th vs -t                           |
| moth  | "mot" (non-word)                                    | —                       | NO clean real-word trap                | -th vs -t (use a th-pool neighbour) |

**Five of seven produce a real English word under /θ/→/t/ substitution** (`thin→tin`, `thick→tick`, `path→pat`, `bath→bat`, `math→mat`). The distractor architecture (§2) handles the /θ/-vs-/t/ contrast as a trap class — the same machinery as sh's sh-vs-s and ch's ch-vs-s. **The strong-trap subset is `thin/tin`, `thick/tick`, `path/pat`, `bath/bat`, `math/mat`** — 5 of 7, the richest strong-trap subset of any digraph tier so far (sh had 3, ch had 3). `bath/bat` and `math/mat` are especially strong because `bat` and `mat` are shipped CVC targets Marian already knows. For the weak-trap subset (`thumb`, `moth` — no clean real-word t-contrast), use a same-pool th-neighbour as the trap. **Pending Dave's th-addendum confirming the th/t trap-class posture (the parent note §Q3 explicitly endorses `thin` vs `tin` as the diagnostic distractor).**

---

## 1.4 Voiced /ð/ exclusion — the load-bearing call for the voiceless-th tier

This section is the th-tier analogue of the ch tier's §1.3 (the long-vowel allowance) — the one design call that most defines the tier's scope.

**No voiced /ð/ word is a th-tier target OR distractor.** The excluded set: `the, this, that, they, them, then, than, there` (and `with`, which is /ð/ in some dialects — voicing-ambiguous, a conservative exclude).

Rationale (parent research note Bottom-line, §Q2 4th row, §Q3 "th voiced"):

> Defer voiced /ð/ indefinitely in the chip-tap decoding format — it dominates function words (the, this, that, they) that are more productively treated as sight words and that share a grapheme with the voiceless form Marian will have just learned, creating a two-realizations-of-one-grapheme puzzle that exceeds the scope of first digraph introduction.

Three compounding reasons:

1. **Low imageability.** `the/this/that/they` are function words — they cannot be depicted as a standalone chip picture. The chip-tap format is picture-grounded; a function word has no picture.
2. **The /ð/→/d/ substitution is unfixable in this format.** Parent note §Q3 / Sources 12, 13: the /ð/→/d/ substitution "is deeply embedded in Philippine English at all levels" — "structural, not a beginner error." Correcting it "requires sustained phonetic training that is beyond this app's scope."
3. **The same-grapheme puzzle.** `th` spells BOTH /θ/ and /ð/. Introducing both in the same tier "would pit the voiceless /θ/ (content words, picturable, phonics-teachable) against voiced /ð/ (function words, low-imageability, not easily drillable)" — a two-realizations-of-one-grapheme puzzle that exceeds first-digraph-introduction scope.

**Design response:** if `the/this/that/they` ever enter the app, they arrive as **sight words** (Emma reads them aloud correctly; Marian matches text to spoken word without needing to produce /ð/) — a future sight-word-tier concern, NOT this spec. The `digraphs-th-voiceless` SkillNode name (locked in PR #211) already encodes this: the node is explicitly `-voiceless`. A future `digraphs-th-voiced` node, if ever created, is a separate sibling — and per the parent research note it should probably be a sight-word treatment, not a digraph-phonics tier at all.

**Schema consequence — this is why th SETS `phoneme: '/θ/'` (§6.1):** because `th` is the canonical multi-phoneme grapheme, the `WordEntry.phoneme` field exists precisely to scope distractor selection so a future voiced-/ð/ word can never co-occur in a chip trio with a voiceless-/θ/ word. The ch tier omitted `phoneme` (no pack-resident word shares `ch` with a different phoneme); the th tier MUST set it, because the voiced/voiceless split on `th` is the exact case the field was designed for. See §6.1.

---

## 2. Distractor word list — th-tier needs cross-orthography th/t contrast distractors

This is structurally the same change the sh tier introduced (`digraphs-sh-word-list.md` §2) and the ch tier reused (`digraphs-ch-word-list.md` §2) — the th-tier reuses that machinery. Per the parent research note §Q3:

> In the chip-tap format ... what matters is whether Marian can match the spoken word (Emma says "thin") to the text chip "thin" vs. "tin" or "sin." The spelling difference makes this visually unambiguous as long as the distractor words don't also start with th.

For the th-tier, the diagnostically useful distractor for a th-word is the **t-contrast** word (the /θ/→/t/ substitution). **Pending Dave's th-addendum** — but the parent research note explicitly names `thin` vs `tin` as the diagnostic distractor, so the th/t-contrast trap class is well-anchored in the existing research.

**One note the parent research raises that Dave's th-addendum must confirm:** the parent note §Q3 also mentions `sin` as a distractor for `thin` ("thin vs. tin or sin"). A /θ/→/s/ axis is plausible (sh→/s/, ch→/s/, and /θ/ is also a fricative). **But this draft uses ONLY the th/t contrast as the trap class**, mirroring the parent note's primary substitution finding (/θ/→/t/, NOT /θ/→/s/). If Dave's addendum endorses a /θ/-vs-/s/ axis as a SECOND trap class, §2 widens — but conservatively, this draft ships th/t only. **Pending Dave (this is the th-tier analogue of the ch tier's Q2 — "which contrast axes ship in v1").**

### Two distractor classes for th-tier

**Class 1 — th/t contrast traps (the digraph-tier pattern, established by sh, reused by ch):**

| th-target | t-contrast trap | Both real English words?            | Notes                                                                                |
| --------- | --------------- | ----------------------------------- | ------------------------------------------------------------------------------------ |
| thin      | tin             | YES — both real, 8yo-appropriate    | Direct minimal pair. Parent note §Q3 worked example. Strong trap.                    |
| thick     | tick            | YES — both real                     | Direct minimal pair. `tick` (clock sound / the insect) is 8yo-knowable. Strong trap. |
| thumb     | tum             | NO — "tum" is informal/non-standard | Non-shippable as an 8yo chip. Weak-trap subset — use a th-pool neighbour.            |
| path      | pat             | YES — both real                     | Direct minimal pair. Strong trap.                                                    |
| bath      | bat             | YES — `bat` is a shipped CVC target | Direct minimal pair — strongest possible trap (Marian already knows `bat`).          |
| math      | mat             | YES — `mat` is a shipped CVC target | Direct minimal pair — strongest possible trap (Marian already knows `mat`).          |
| moth      | mot             | NO — "mot" is a non-word            | Weak-trap subset — use a th-pool neighbour.                                          |

**The strong-trap subset is `thin/tin`, `thick/tick`, `path/pat`, `bath/bat`, `math/mat`** — 5 of 7, the richest of any digraph tier (sh: 3, ch: 3). For the weak-trap subset (`thumb`, `moth` — no clean real-word t-contrast), use a same-pool th-neighbour as the trap.

**`bat` and `mat` are dual-role cross-tier-load-bearing distractors** (the sh tier established this pattern with `sip`, the ch tier reused it with `sip` again):

`bat` and `mat` are already `TARGET_WORDS` entries with `vowel: 'a'`, `isTarget: true` (shipped in the short-a CVC tier). Using them as `bath`'s / `math`'s t-contrast trap distractors means each is now referenced from **TWO tiers** — short-a CVC (own tier) and th (`bath`'s / `math`'s trap).

1. **Retain `bat`'s and `mat`'s existing entries** in `TARGET_WORDS` with `vowel: 'a'`, `isTarget: true`. No change.
2. **Reference `bat` / `mat` by string** from the th-tier `TARGET_PAIRINGS` rows for `bath` / `math`. `getWordEntry('bat')` resolves to the existing entry.
3. **Operational rule applies** (`.claude/docs/skill-trees-and-content.md` §"Cross-vowel-tier load-bearing — generalization"): before any future removal of `bat` or `mat` from `TARGET_WORDS`, the impl ticket must grep for `bat` / `mat` as string tokens in `TARGET_PAIRINGS` and either retain the entry or substitute every reference.
4. **`bat` / `mat` are NOT `phoneme`-tagged.** They are plain short-a CVC words. The th-tier targets carry `phoneme: '/θ/'`; `bat`/`mat` carry no `phoneme`. The opt-in phoneme-scoping branch in `pickDistractors` only fires when BOTH target and distractor are phoneme-tagged with DIFFERENT values — so a `/θ/`-tagged target + an untagged `bat` distractor passes the check by design (same posture as the sh tier's `ship` + untagged `sip`). **This is load-bearing — see §6.1.**

**`tin`, `tick`, `pat` are NEW distractor-only entries.** None is in `TARGET_WORDS` today. Each needs a new `WordEntry` row (`isTarget: false`, `vowel: 'i'` for `tin`/`tick` / `vowel: 'a'` for `pat`, a picture asset — silhouette placeholder acceptable per the distractor-only-entry pattern). They are 3 of the th-tier's 5 new t-contrast traps. The other 2 strong traps (`bat`, `mat`) are dual-role (existing entries, not new).

**They are NOT `phoneme`-tagged.** `tin`/`tick`/`pat` are plain CVC words — tagging them `/t/` is unnecessary (the /θ/-vs-/t/ contrast is tested by chip SELECTION, not by phoneme-scoping) and would risk the th-tier `TARGET_PAIRINGS` rows tripping the phoneme-mismatch defensive check in `pickDistractors`. Same posture as the sh tier's untagged `sip`/`sell`/`sop` and the ch tier's untagged `sat`/`sick`. See §6.1.

### Class 2 — same-pool th-neighbour (gentle distractor, mirrors short-vowel + sh + ch tier pattern)

For every th-target, pair it with another th-target as the gentle distractor — same-sound-class, different referent. e.g. `thin` (target) with `path` (gentle distractor) — Marian distinguishes by picture, not by th-vs-t. This builds confidence + reinforces the th-pool as a cohesive set.

### Recommended distractor matrix (concrete example for Kevin's impl ticket — DRAFT, pending Dave)

Full `TARGET_PAIRINGS` rows are Kevin's to author; design preview here so the structure is clear (mirrors `digraphs-ch-word-list.md` §2 and `digraphs-sh-word-list.md` §2).

**Shape contract.** Every row's `gentle` and `trap` are `readonly [string, string]` tuples — exactly 2 entries each. Matches the existing `WordPairing` / `TargetPairings` type in [`wordPack.ts`](../../src/screens/WordSong/wordPack.ts). Single-entry tuples are a type error.

**FORBIDDEN_PAIRS pre-check.** Every distractor below is cross-checked against §6's FORBIDDEN_PAIRS additions: `[thin, thick]`, `[path, moth]`. No row pairs a target with a forbidden silhouette neighbour.

```ts
// Strong-trap subset — Class 1 (th/t contrast) + Class 2 (th-pool neighbour):
thin:  { gentle: ['bath',  'math'],  trap: ['tin',  'path'] },  // gentle: 2 th-neighbours (NOT thick — FORBIDDEN_PAIR); trap: th/t contrast + th-neighbour
thick: { gentle: ['bath',  'math'],  trap: ['tick', 'path'] },  // gentle: 2 th-neighbours (NOT thin — FORBIDDEN_PAIR); trap: th/t contrast + th-neighbour
path:  { gentle: ['bath',  'thumb'], trap: ['pat',  'thick'] }, // gentle: 2 th-neighbours (NOT moth — FORBIDDEN_PAIR); trap: th/t contrast + th-neighbour
bath:  { gentle: ['path',  'math'],  trap: ['bat',  'thin'] },  // gentle: 2 th-neighbours; trap: th/t contrast (bat — dual-role) + th-neighbour
math:  { gentle: ['path',  'bath'],  trap: ['mat',  'thick'] }, // gentle: 2 th-neighbours; trap: th/t contrast (mat — dual-role) + th-neighbour

// Weak-trap subset — Class 2 (th-pool neighbour only; no clean real-word t-contrast):
thumb: { gentle: ['bath',  'math'],  trap: ['thin', 'path'] },  // gentle + trap both th-pool; "tum" non-standard
moth:  { gentle: ['bath',  'thumb'], trap: ['thin', 'thick'] }, // gentle + trap both th-pool (NOT path — FORBIDDEN_PAIR); "mot" non-word
```

> **Authoring note:** every distractor string above MUST be a member of the DRAFT 7-word th pool OR a shipped distractor-only entry (`tin`, `tick`, `pat` new; `bat`, `mat` dual-role existing). No word outside that set may appear. Kevin's `TARGET_PAIRINGS` rows must validate every distractor string against the locked pool + the distractor-only entries. **This matrix is DRAFT — it will be re-derived after Dave's th-addendum reconciliation (§0) if the pool changes.**

**Distractor-class summary per row:**

| Target | Gentle tier (problems 1-3) | Trap tier (problems 4-8) | Trap class                          |
| ------ | -------------------------- | ------------------------ | ----------------------------------- |
| thin   | bath, math                 | tin, path                | th/t + th-neighbour                 |
| thick  | bath, math                 | tick, path               | th/t + th-neighbour                 |
| path   | bath, thumb                | pat, thick               | th/t + th-neighbour                 |
| bath   | path, math                 | bat, thin                | th/t (bat dual-role) + th-neighbour |
| math   | path, bath                 | mat, thick               | th/t (mat dual-role) + th-neighbour |
| thumb  | bath, math                 | thin, path               | th-neighbour only                   |
| moth   | bath, thumb                | thin, thick              | th-neighbour only                   |

**Note on the matrix preview:** illustrative; Kevin owns the final `TARGET_PAIRINGS` rows in the impl ticket per `screens-and-flows.md` convention. The load-bearing constraints:

- Tuple shape `[string, string]` (2 entries each).
- No FORBIDDEN_PAIR adjacency.
- `gentle` always th-pool; `trap` is th/t contrast where a strong-trap is available (`thin`, `thick`, `path`, `bath`, `math`), else another th-pool neighbour (`thumb`, `moth`).
- `bat` / `mat` documented as dual-role (NOT new entries — existing short-a CVC targets); `tin`, `tick`, `pat` are the three new distractor-only entries.
- **Every distractor string is a member of the DRAFT 7-word th pool OR a shipped distractor-only entry** — no out-of-pool words anywhere.

### Cross-tier hygiene — NO short-vowel CVC distractors, AND no sh/ch-tier distractors, in th-trios

Per the parent research note §Q5 + Bottom-line: "Do not interleave digraphs with CVC content until the digraph is consolidated" AND "introduce each digraph as its own isolated tier; reserve interleaving for after each digraph reaches ~90% accuracy." Cross-pool distractors (`thin` vs `shin` vs `chin`) belong to the **interleaving phase** (when sh + ch + th are all consolidated), NOT the th-introduction tier. **th-tier trios contain ONLY th-words + th/t-contrast traps. No `cat`, `dog`, `pen` etc. — AND no `ship`, `chip`, etc. either.**

**One subtlety the th tier introduces that ch did not:** `bat` and `mat` ARE short-a CVC words, and they appear in th-trios as dual-role distractors for `bath`/`math`. This is NOT a violation of the "no CVC words in th-trios" rule — it is the **deliberate th/t-contrast trap** (`bath` vs `bat` is the diagnostic minimal pair, exactly as `thin` vs `tin` is). The rule is "no CVC words EXCEPT the t-contrast minimal-pair partner of a th-target." This mirrors the sh tier's `sip` (an `/s/`-onset CVC word that IS the diagnostic distractor for `ship`) and the ch tier's `sat`/`sick`. The general principle: a CVC word may appear in a digraph trio **only when it is the substitution-minimal-pair partner of a target in that trio** — never as a generic filler.

Realised in code through (a) `pickDistractors(target, problemIndex, options)` in [`wordDistractors.ts`](../../src/screens/WordSong/wordDistractors.ts) — matrix-curated, runtime-dumb — and (b) render-time chip ordering via `buildChipOrder()` in [`WordSong.tsx`](../../src/screens/WordSong/WordSong.tsx). No new function.

### FORBIDDEN_PAIRS additions

| New pair        | Reason                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| --------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `[thin, thick]` | `thin` and `thick` are an antonym pair whose pictures are deliberately the SAME object class at opposite extremes of one property (slenderness). At 96pt a "thin X" and a "thick X" could read as near-identical silhouettes differing only in width — the discriminator is real but stacking them in one trio over-loads the property-contrast. In-pool hygiene: pair `thin` and `thick` against the other 5 th-words, never each other. |
| `[path, moth]`  | `path` (a thin winding trail) and `moth` (a small drab insect) are both low-mass, irregular-outline silhouettes that could read with insufficient contrast at 96pt — `path` is a thin line, `moth` is a small shape; neither has a bold blocky silhouette. In-pool hygiene keeps them out of the same trio.                                                                                                                               |
| `[bath, box]`   | Cross-pool silhouette hygiene — `bath` (a tub — open-topped rounded vessel) vs. `box` (short-o target, plain closed cuboid). Both are mid-size container-class objects; the open-top/rounded vs closed/cuboid detail distinguishes them, but the cross-pool rule documents the risk for future interleaving work. Hygiene-only in v1 (the th-tier isolated-trio rule already prevents `box` appearing in a th-trio).                      |

**Pending Dave's th-addendum** — if the pool changes, the FORBIDDEN_PAIRS additions are re-derived. The cross-pool `[bath, box]` is the th-tier analogue of the ch tier's `[chest, box]`.

---

## 3. Picture-pack requirements (summary — full prompts in companion sheet)

See `digraphs-th-picture-pack-prompts.md` for the per-word MJ prompts.

Summary requirements:

- **7 wholly-new pictures** (no re-traces — none of the 7 th-words have existing distractor SVGs from prior tiers).
- Pictures embedded via `yarn embed-pictures` per the established Phase-3 pipeline.
- Filenames: `picture-thin.svg`, `picture-thick.svg`, `picture-thumb.svg`, `picture-path.svg`, `picture-bath.svg`, `picture-math.svg`, `picture-moth.svg` at `public/assets/pictures/`.
- Style anchor: `picture-pack-style-anchor.md` §2 + §3 (locked, byte-for-byte preamble) + the short-form 14-word preamble convention established in `digraphs-sh-picture-pack-prompts.md` §1.4 and re-used by `digraphs-ch-picture-pack-prompts.md` §1.4.
- **Load-bearing picture briefs** (these are NOT optional polish — they are AC7 content):
  - `thin` — a clearly slender single object; "thinness" must be the salient property. NOT a thin-vs-thick comparison (that would need two subjects); a single object that unmistakably reads as thin (e.g. a thin slice, a thin stick).
  - `thick` — a clearly chunky single object; "thickness" must be the salient property (e.g. a thick book, a thick slice). The `thin`/`thick` pair should be the same object class at opposite extremes so the property contrast is legible across the pack.
  - `thumb` — a hand with the thumb as the salient feature (a "thumbs-up" hand reads cleanly); the ch-tier `chin` precedent — a body part shown clearly, not a free-floating fragment.
  - `math` — numbers / a simple sum / a small chalkboard with `2 + 2`; must read as "numbers", NOT "a school" or "a classroom".
  - `moth` — a moth: fuzzy body, drab/muted colour, folded or flat wings; must NOT read as a butterfly (bright colour, upright wings, slender body are the butterfly cues to avoid).
- **3 additional distractor-only pictures needed** for the t-contrast traps `tin`, `tick`, `pat` — silhouette placeholders acceptable per Devon's picture-pack §6 finding #4 path; vector trace deferred to polish backlog. `picture-bat.svg` and `picture-mat.svg` already exist (short-a CVC tier, reused — `bat`/`mat` are dual-role distractors).
- **Pending Dave's th-addendum** — if the pool changes, the picture-pack scope and the load-bearing briefs are re-derived. The companion prompt sheet carries the same pending markers.

---

## 4. Emma's voiceless-th-introduction lines (design-side TTS script — not the spec for Kevin)

This is informational; the Word Song spec for Kevin's planner will lift the exact script lines. **DRAFT — pending Dave's th-addendum, which is the authority on the exact articulation-scaffold wording.** The parent research note §Q3 gives the core instruction note directly:

> Emma's instruction note: "th together says a special sound — put your tongue between your teeth and blow." A brief animation of Emma's mouth position (or an illustration of a mouth) would help more than TTS alone here.

**First voiceless-th-encounter (lifetime-once scaffolding, mirrors sh's two-letters-one-sound opener / ch's "not /k/ like in cat" / short-u's `sun/soon` AC9b mechanism) — DRAFT:**

> "Look — t and h together! They make a brand-new sound — th! Put your tongue between your teeth and blow — thhh! Listen: thin!"

The clause **"put your tongue between your teeth and blow"** is the th-tier's load-bearing articulation scaffold — the analogue of the ch tier's "not /k/ like in 'cat'" clause. It is **load-bearing, not decorative**: per the parent research note §Q3, /θ/ "has no L1 scaffolding" and "TTS alone ... may not be sufficient" — the placement cue is what gives Marian a production handle. **Pending Dave's th-addendum confirming the exact wording.** (Note: production is not chip-tap-testable — but the scaffold still matters because it gives Marian a mental model of "th is a special sound", which supports the recognition task.)

**The sh-vs-ch-vs-th comparison (DRAFT — when th arrives after sh and ch):** Emma can reference the now-familiar pattern:

> "Remember sh and ch? Two letters, one sound. Th is the same — t and h make one new sound! But this one — your tongue peeks out. Thhh!"

The "two letters, one sound" concept is by now **thoroughly reviewed** (sh introduced it, ch reviewed it) — th does not re-teach it. What IS new for th is the **articulation** (tongue between teeth) — that is the one new scaffold.

**The mouth-position visual cue (DRAFT — flagged for Kyle's screen-design follow-up + Thomas):** the parent research note §Q3 explicitly says "a brief animation of Emma's mouth position (or an illustration of a mouth) would help more than TTS alone here." This is a th-tier-SPECIFIC ask that sh and ch did not have. Options:

- A small static mouth-position illustration in the screen corner during th-tier sessions (the th-tier analogue of the ch-tier digraph-cue, but specifically a mouth diagram).
- A brief Emma mouth-position animation on the first-encounter line.
  **This is an OPEN QUESTION for Thomas (§7 Q1) — it has a screen-design cost the sh/ch cues did not.** A static illustration is the low-cost option; an animation is richer but is a new asset + motion-brief item.

**Concept reminder (per session-open Emma greeting in th-tier sessions, before any item) — DRAFT:**

> "Today we're practicing the th sound! When 't' and 'h' stand together, they say th — like 'thin'. Tongue between your teeth!"

**Per-item Emma lines (each th-word, default) — DRAFT:**

> "[word]! Listen: [word]. Can you find [word]?"

**No `hybridMode` long-vowel-hybrid script variant is anticipated** — the DRAFT th pool has zero `hybridMode` entries (§6.2), pending Dave's call on `thumb`'s silent `b`. Every th-word gets the standard decodable per-item line.

**Pending Dave's th-addendum** — Dave's research is the authority on the articulation-scaffold wording, on whether the mouth-position visual is essential or nice-to-have, and on the exact first-encounter opener text. The above is the draft Kevin's planner spec works from until §0 reconciliation lands.

---

## 5. The scaffold gate th needs — the articulation cue

sh's scaffold gate was the "two letters, one sound" concept. ch's scaffold gate was the c-says-/k/ orthographic trap. **th's scaffold gate is the articulation placement** — and it is the only one of the three that may need a VISUAL cue, not just a script line.

- Marian internalized "two letters, one sound" on the sh tier and reviewed it on the ch tier — that concept does NOT need re-teaching (parent note §Q2: th third specifically so the concept is thoroughly consolidated).
- What IS new for th: the **articulation** has no L1 reference (parent note §Q3 — Tagalog has neither /θ/ nor /ð/; the /θ/→/t/ substitution "is automatic and deep"). Emma must explicitly scaffold "tongue between your teeth and blow."

**Recommended scaffold gate:** before the first th-introduction session, Emma's session-open includes the concept reminder (§4) explicitly naming the articulation. This is NOT a separate lifetime-once item beyond the first-encounter opener — it's a per-session reminder at the start of each th-introduction session while `digraphs-th-voiceless.state === 'practicing'` (surfaced through the `practicing` state; suppressed once the node flips to `'mastered'`). State mapping mirrors the sh tier's AC6 and the ch tier's AC6.

**Implementation note:** parallels the sh + ch tier digraph-cue mechanism — a small persistent visual cue showing "th" + a keyword picture (e.g., `th - thin`) during th-tier sessions. **th-tier-SPECIFIC addition:** the parent research note §Q3 recommends the cue include (or be) a **mouth-position illustration** ("tongue between teeth"). Whether the corner cue is `th - thin` (matching the sh/ch pattern) OR `th - [mouth diagram]` (the th-specific articulation cue) is **Open Question Q1 for Thomas (§7)** — it carries a screen-design / asset cost the sh and ch cues did not. Spec-side note for Kevin's planner + Kyle's screen-design follow-up.

---

## 6. Cross-pool hygiene + planner constraints

### Planner constraints

1. **th-tier sessions contain only th-words + th/t-contrast traps.** No CVC words from prior tiers (EXCEPT the t-contrast minimal-pair partner of a th-target — `bat` for `bath`, `mat` for `math` — which IS the diagnostic distractor, not generic filler), and NO sh-tier or ch-tier words, in th-trios in v1. Cross-pool `shin`/`chin`-as-distractor-for-`thin` is the _interleaving_ phase's job (parent note §Q5).
2. **Maximum 2–3 new th-items per 8-item session** (parent research note §Q4, Source 16 internal-research note: "no more than 2–3 items should be on a brand-new phonics pattern; the remaining 5–6 items revisit mastered patterns"). The remaining 5–6 items are review — handled via the planner's existing cross-tier-review mechanism, NOT via in-trio cross-tier inclusion. (Note `.claude/docs/skill-trees-and-content.md` §"Cross-tier review": as of 2026-05-14 cross-tier review is design intent, not shipped planner behaviour — every word-song tier emits 8 problems from its own pool. th follows the shipped behaviour; this constraint is the design target.)
3. **First th-session is lifetime-once-scaffolded** — Emma's "two letters, one sound" review + the "tongue between your teeth and blow" articulation scaffold fires the first time th-tier is encountered. Mirrors the sh tier's AC5 / ch tier's AC5 / short-u's `sun/soon` AC9b.
4. **Per-session th-digraph-cue persists during th-tier sessions while `digraphs-th-voiceless.state === 'practicing'`** (per `applyMasteryRule()` in `src/lib/progress/mastery.ts`; `intro` + `practicing` both show the cue, `mastered` hides it). Small visual cue in screen corner. **Open Question Q1 (§7): is the cue `th - thin` (sh/ch pattern) or `th - [mouth diagram]` (th-specific articulation cue)?** Mirrors the sh + ch tier's AC6 mechanism.
5. **Word-final th words may need conservative planner weighting in the FIRST introduction sessions** — IF Dave's th-addendum confirms word-final th belongs in the first tier at all. Word-initial th (`thin/thick/thumb`) is the cleaner decoding-position arc; word-final th (`path/bath/math/moth`) adds the "th can also be at the END" concept. The planner may want to lead introduction sessions with word-initial th and let word-final th rise as the position-flexibility consolidates — the analogue of the ch tier's `chest` conservative-weighting note. **Pending Dave's th-addendum** — this is a planner-weighting note, NOT a `hybridMode` flag; all word-final th words are fully decodable.
6. **`thin`/`thick`/`path`/`bath`/`math` are th/t minimal-pair anchors** — and `bath`/`math` specifically pair with shipped CVC targets `bat`/`mat`. The planner / canon-bake should annotate these with a "minimal pair: t-contrast" note so future interleaving-phase planner work can leverage the pairs intentionally. In the th-introduction tier the th/t pairs ARE used as the trap distractor class (§2) — unlike the sh/ch tiers, where the cross-pool minimal pairs were annotation-only. (The th/t pair is WITHIN the th-tier's scope because it tests the /θ/-vs-/t/ recognition that IS the tier's learning target; the sh/ch cross-pool pairs test cross-digraph discrimination, which is the interleaving phase's job.)

### Cross-tier hygiene

1. **FORBIDDEN_PAIR additions:** `[thin, thick]` (in-pool), `[path, moth]` (in-pool), `[bath, box]` (cross-pool silhouette).
2. **No th/t contrast words in CVC, sh, or ch trios:** the `tin`, `tick`, `pat` distractor words appear ONLY in th-trios, never in CVC / sh / ch trios. This prevents confusion where Marian sees `tin`/`tick`/`pat` in a non-th trio and learns it as a target, then meets it as a distractor in th-tier. (`bat` and `mat` are the dual-role exception — already short-a CVC targets; their role is established and the th-tier reference is additive.)
3. **NO voiced-/ð/ words as targets OR distractors for th-targets** (§1.4). `the/this/that/they` are excluded entirely — they are voiced /ð/, not voiceless /θ/, and belong to the (deferred, sight-word) voiced-th domain. This is enforced structurally by the `phoneme: '/θ/'` tag (§6.1): a future voiced-/ð/ word would carry `phoneme: '/ð/'` and the phoneme-scoping branch in `pickDistractors` would prevent it co-occurring in a chip trio with a `/θ/`-tagged target.
4. **NO `s`-onset words as distractors for th-targets in v1** — the parent research note §Q3 mentions `sin` as a possible distractor for `thin` ("thin vs. tin or sin"), but this draft ships the th/t contrast ONLY (the parent note's primary /θ/→/t/ substitution finding). A /θ/-vs-/s/ axis is a possible SECOND trap class but is held pending Dave's th-addendum (§7 Q2). If Dave endorses it, §2 widens; conservatively, this draft does not include it.

### Distractor architecture refactor (already done by the sh tier — th reuses it)

The cross-orthography distractor pattern (`wordPack.ts` distractor-only entries + dual-role string references + the matrix-lookup `pickDistractors` being runtime-dumb) was **built and shipped for the sh tier** (PR #220) and reused by the ch tier (PR #227). The th-tier does NOT need an architecture refactor — it adds rows to the existing structures:

- **`wordPack.ts TARGET_WORDS` additions:** 7 new th-target entries, all with `phoneme: '/θ/'` set + `vowel` set (every th-word uses a short vowel Marian has covered). **3 new distractor-only entries** (`tin` `vowel: 'i'`, `tick` `vowel: 'i'`, `pat` `vowel: 'a'`, all `isTarget: false`, none `phoneme`-tagged). `bat` / `mat` are dual-role (NOT new entries — already exist). ~10 new `WordEntry` rows.
- **`pickDistractors` runtime is unchanged.** Matrix-lookup-only — adding th-tier `TARGET_PAIRINGS` rows is sufficient. **The phoneme-scoping branch DOES become live for th-tier targets** (they carry `phoneme: '/θ/'`) — but since every th-tier distractor (`tin`/`tick`/`pat` untagged, `bat`/`mat` untagged, th-pool neighbours all `/θ/`-tagged) either matches `/θ/` or is untagged, the branch never rejects a th-tier matrix pairing. The branch is the architectural floor that would reject a future `/ð/`-tagged voiced-th word — see §6.1.
- **`buildChipOrder` runtime is unchanged.** Render-time positioning is target-agnostic.
- **`FORBIDDEN_PAIRS` extension** for in-pool th-hygiene rules (table above).
- **`WordEntry.vowel` field** — every th-tier entry sets `vowel` (all 7 use short vowels Marian has covered). **`WordEntry.phoneme` field — every th-tier TARGET sets `phoneme: '/θ/'`** (unlike ch, which omitted it; like sh, which set `phoneme: '/ʃ/'`). See §6.1 for why. **No `hybridMode: true` entries anticipated** — pending Dave's call on `thumb`'s silent `b` (§6.2).
- **Picture assets for the 3 distractor-only entries.** `tin`, `tick`, `pat` each need a picture (silhouette placeholder acceptable). `bat`, `mat` reuse their existing short-a CVC picture assets.

**This is downstream for Kevin's + Devon's impl tickets.** The work is comparable to ch's (no `hybridMode` complication anticipated) PLUS the `phoneme: '/θ/'` tagging — which is LESS work than sh's was (sh built the machinery; th + ch both just consume it) but reactivates the phoneme-scoping branch that ch left inert.

### 6.1 `phoneme` annotation — th SETS `phoneme: '/θ/'` on all 7 (the load-bearing schema divergence from ch)

**This is the th tier's most important schema decision, and it diverges from the ch tier.** The ch tier explicitly OMITTED the `phoneme` field (ch-word-list §6 + the wordPack.ts ch-tier comment block: "NO `phoneme` field ... the ch pool needs no such scoping (no pack-resident word shares the `ch` grapheme with a different phoneme)"). **The th tier MUST set `phoneme: '/θ/'` on all 7 targets** — because `th` IS the canonical multi-phoneme grapheme.

The `WordEntry.phoneme` field's own doc comment names this exact case:

> Optional phoneme tag (IPA, content phoneme only). Used for distractor-selection scoping when a grapheme covers multiple phonemes (**e.g. `th` → /θ/ vs /ð/**, `g` → /g/ vs /dʒ/).

Why this matters — three load-bearing reasons:

1. **`th` spells two phonemes.** Voiceless /θ/ (`thin, bath`) and voiced /ð/ (`the, this`). The whole point of the `phoneme` field is to scope distractor selection so a `/θ/` target can never co-occur in a chip trio with a `/ð/` word. Even though §1.4 EXCLUDES all voiced-/ð/ words from v1, a future voiced-th sight-word tier (or any future pack addition) could introduce a `/ð/`-tagged word — and the `phoneme: '/θ/'` tag on the th-tier targets is the structural guarantee that the phoneme-scoping branch in `pickDistractors` will keep them apart. **Setting `phoneme: '/θ/'` now is the architectural floor; omitting it (the ch posture) would leave the door open for a future cross-phoneme co-occurrence bug.** This is exactly the reasoning the sh tier used to set `phoneme: '/ʃ/'`.
2. **It mirrors the sh tier, not the ch tier.** The sh tier set `phoneme: '/ʃ/'` on all 7 sh entries (wordPack.ts sh-tier comment: "All 7 carry `phoneme: '/ʃ/'` — the canonical disambiguator"). The ch tier omitted `phoneme` because `ch` is unambiguously /tʃ/ in this pack — there is no /k/-via-`ch` or /ʃ/-via-`ch` pack-resident word. **th is like sh (multi-phoneme grapheme), NOT like ch (single-phoneme-in-this-pack grapheme).** The `digraphs-th-voiceless` SkillNode name itself encodes the multi-phoneme reality: the node is `-voiceless` precisely because a `-voiced` sibling is conceivable.
3. **The phoneme-scoping branch only rejects a pairing when BOTH target and distractor are phoneme-tagged with DIFFERENT values.** All th-tier distractors are either th-pool neighbours (all `/θ/`-tagged — match, no rejection) or the t-contrast traps `tin`/`tick`/`pat`/`bat`/`mat` (all UNTAGGED — the branch does not fire, by design). So setting `phoneme: '/θ/'` on the targets does NOT break any §2 matrix pairing — it is inert for every v1 th-tier pairing and only becomes active if a future `/ð/`-tagged word is added. **Verified against the sh tier's identical posture: `ship` is `/ʃ/`-tagged, `sip` is untagged, and the `ship`/`sip` pairing passes the check** (wordPack.ts sh-tier comment, lines ~606-608).

**The t-contrast distractors (`tin`, `tick`, `pat`, `bat`, `mat`) are NOT `phoneme`-tagged.** Tagging them `/t/` would be wrong: (a) the /θ/-vs-/t/ contrast is tested by chip SELECTION, not by phoneme-scoping; (b) tagging them `/t/` would make every th-tier `TARGET_PAIRINGS` row trip the phoneme-mismatch defensive check in `pickDistractors` (target `/θ/` vs distractor `/t/`), breaking the matrix. They stay untagged — exactly the posture the sh tier used for `sip` and the ch tier used for `sat`/`sick`.

**Impl-ticket consequence:** Kevin's wordPack ticket sets `phoneme: '/θ/'` on all 7 th targets and leaves `tin`/`tick`/`pat` untagged. The phoneme-scoping branch in `pickDistractors` is reactivated for th-tier targets (it was inert for the ch tier) — but it never rejects a v1 th-tier matrix pairing. A `_planner.test.ts` / `wordPack.test.ts` assertion should pin: (i) all 7 th targets carry `phoneme: '/θ/'`; (ii) `tin`/`tick`/`pat` carry no `phoneme`; (iii) every §2 matrix pairing passes the `pickDistractors` phoneme-scoping check.

### 6.2 `hybridMode` annotation — ZERO th-tier entries anticipated, but `thumb`'s silent `b` is PENDING Dave

The `hybridMode: boolean` field on `WordEntry` is **already in the schema** (PR #220 — the sh tier set it `true` for `shoe/sheep/shark`; the ch tier set it `true` on ZERO entries). **The DRAFT th-tier sets it `true` on ZERO entries** — but unlike ch, where the zero-`hybridMode` call was clean, the th tier has ONE genuinely-uncertain word: **`thumb`'s silent `b`.**

The DRAFT posture, pending Dave's th-addendum:

- **6 of 7 th-words (`thin, thick, path, bath, math, moth`) are unambiguously `hybridMode: false`** — every one uses a short vowel Marian has formally covered, with a clean grapheme→phoneme mapping (`thick`'s `ck` and the word-final `th`'s are all acoustically inert for chip-tap recognition and spell sounds Marian decodes). No whole-word picture-carry of an unlearned pattern is needed. This is the same situation as the ch tier's clean zero-`hybridMode` call.
- **`thumb` is the one open question.** `thumb` has a silent `b` (`mb` → /m/). For chip-tap RECOGNITION the silent `b` is acoustically inert — Marian hears /θʌm/, sees `thumb`, taps it; the `b` does not block the recognition task. **BUT** the `hybridMode` flag exists to "suppress segmentation / spelling / decode-from-phoneme prompts" for words whose grapheme→phoneme mapping is NOT clean — and `thumb`'s silent `b` is exactly such a case. Dave's th-addendum must rule one of three ways:
  1. **`thumb` stays `hybridMode: false`** — the silent `b` is treated as acoustically inert for chip-tap recognition (the only problem type the th tier emits), the same way `thick`'s `ck` is treated as inert. This is the DRAFT's lean.
  2. **`thumb` is `hybridMode: true`** — the silent `b` is enough of a non-clean mapping that segmentation/spelling prompts should be suppressed for it. This would make th-tier `hybridMode` count 1 of 7 — structurally between ch (0) and sh (3).
  3. **`thumb` is dropped and replaced** — Dave's th-addendum may swap `thumb` for `thud` (clean short-u th-CVC, no silent letter — but harder to picture: an onomatopoeic impact) or another short-u option. This would keep th-tier `hybridMode` at a clean 0.

**The DRAFT carries `thumb` as `hybridMode: false` (option 1) but flags this as the pool's #1 uncertainty.** §0 reconciliation resolves it. **This must be stated explicitly** so the impl ticket does not assume th is a clean zero-`hybridMode` tier the way ch was — it may or may not be, pending Dave.

**Why state it explicitly (mirrors the ch tier's §6.1 rationale):** the ch tier's spec was directed to state its zero-`hybridMode` outcome explicitly to "prevent future misapplication of the `hybridMode` pattern." The th tier's analogous discipline is to state that the zero-`hybridMode` outcome is **DRAFT and pending one specific word's resolution** — so the impl ticket waits for §0 reconciliation rather than assuming.

---

## 7. Open questions for Thomas

The sh-tier spec resolved 6 open questions before shipping; the ch-tier inherited most of those resolutions and Dave's ch-addendum closed the rest. **The th-tier spec is DRAFT — its open questions are NOT all closed, because Dave's th-addendum is not yet on disk.** Q1 is a genuine Thomas decision with a screen-design cost; Q2 and Q3 are pending Dave's th-addendum (§0) but flagged here for visibility.

### Q1 — the mouth-position visual cue — GENUINE Thomas decision, has a screen-design cost

**This is the one open question that is a real Thomas call, not a Dave-pending item.** The parent research note §Q3 explicitly recommends that the th-tier scaffold include a **mouth-position illustration** ("tongue between teeth") — "a brief animation of Emma's mouth position (or an illustration of a mouth) would help more than TTS alone here." This is a th-tier-SPECIFIC ask: sh's and ch's corner digraph-cues were just `sh - ship` / `ch - chin` keyword-picture pairs, which reuse the existing picture-pack assets at zero new asset cost. A mouth-position cue is a NEW asset class. Three options:

- **Option A (low-cost):** the th-tier corner cue is `th - thin` (a keyword-picture pair, exactly matching the sh/ch pattern, zero new asset). The articulation scaffold lives ONLY in Emma's script lines (§4). Cheapest; consistent with sh/ch; but drops the parent research note's visual recommendation.
- **Option B (mid-cost):** the th-tier corner cue is a small STATIC mouth-position illustration (`th - [mouth diagram]`). One new static SVG asset. Honours the research note's recommendation at modest cost.
- **Option C (high-cost):** a brief Emma mouth-position ANIMATION fires on the first-encounter line. New asset + motion-brief item + the Emma character motion vocabulary currently has no mouth-articulation pose. Richest pedagogically; most expensive.

**Recommendation: Option B** (static mouth-position illustration) — it honours the parent research note's explicit recommendation that a visual "would help more than TTS alone," at a cost (one static SVG) far below Option C, and the th-tier is genuinely the one digraph where articulation has no L1 scaffold. **Thomas to lock A / B / C.** This is the only genuinely-gating open question that is Thomas's (not Dave's) call.

### Q2 — a /θ/-vs-/s/ second trap class — PENDING Dave's th-addendum

The parent research note §Q3 mentions `sin` as a possible distractor for `thin` ("thin vs. tin or **sin**"). This draft ships the **th/t contrast ONLY** (the parent note's PRIMARY /θ/→/t/ substitution finding). A /θ/-vs-/s/ axis is plausible — /θ/ and /s/ are both fricatives, and sh→/s/ + ch→/s/ are established — but the parent note's per-word error analysis (§Q3) names /θ/→/t/, not /θ/→/s/, as "the most systematic" substitution. **Dave's th-addendum is the authority on whether a /θ/-vs-/s/ axis ships as a SECOND trap class in v1.** If Dave endorses it, §2 widens (more distractor-only `s`-words needed: `sin` is adult-register so probably not, but `sick`/`sock`-shaped traps might). Conservatively, this DRAFT does NOT include it — th/t only. **Nothing for Thomas to lock directly — pending Dave; §0 reconciliation resolves it.**

### Q3 — pool composition + depth — PENDING Dave's th-addendum

The DRAFT pool of 7 is `thin, thick, thumb, path, bath, math, moth`. **Dave's th-addendum is the authority on the final inventory** and may revise it on several axes (all enumerated in §0):

- **Word-final th in the first tier?** This DRAFT includes 4 word-final th words (`path, bath, math, moth`). Dave may pull word-final th to a follow-up arc — in which case `think`/`thank` (held in reserve) likely come in to reach 7, and the pool becomes word-initial-heavy.
- **`thumb`'s silent `b`** — keep / `hybridMode: true` / swap-for-`thud` (§6.2).
- **`think`/`thank`** — both are strong word-initial /θ/ content words held in reserve because `think` is borderline-abstract and `thank` is a social action; both are picture-brief-dependent (the ch-tier `chat` precedent shows social actions CAN be grounded). Dave may bring either in.
- **Pool depth** — Dave may ship 7, or recommend 8–10 (the parent note §Q4 range, with the caveat that th's picturable content-word stock "is smaller than sh or ch").

**Recommendation: do not file the impl tickets until Dave's th-addendum lands and §0 is reconciled.** Unlike the ch tier (where Dave's ch-addendum was already on disk when the spec was finalised), this spec is genuinely DRAFT-ahead-of-research. **Thomas should treat the whole pool as provisional pending §0.**

### Inherited resolutions (locked on the sh / ch tiers / by PR #211 — listed for pattern-matching, not live)

- **th-tier `SkillNode` shape** — RESOLVED in PR #211: 3 sibling nodes (`digraphs-sh`, `digraphs-ch`, `digraphs-th-voiceless`). The `digraphs-th-voiceless` node already exists. Note the name encodes the voiceless/voiced split (§1.4 / §6.1).
- **th-tier mastery rule** — RESOLVED: same 90/3 as CVC + sh + ch tiers. After 90% accuracy across 3 consecutive sessions, `digraphs-th-voiceless` graduates. (What it unlocks downstream — `sight-words` or a `wh`/`ck` digraph tier — is a separate skill-tree-ordering question, not this spec's.)
- **Digraph introduction order** — RESOLVED: sh → ch → th-voiceless (parent note §Q2). th is third.
- **When the `digraphs-th-voiceless` node opens** — parent note §Q5 + Bottom-line: th is introduced "after ch approaches mastery." The node should open when ch has had its introduction sessions and ch accuracy is trending upward — and per the parent note, sh+ch interleaving should already be underway before th opens. This is a planner session-composition decision, NOT a word-list decision — flagged for Kevin's impl ticket. **Dave's th-addendum may refine the exact opening threshold.**
- **Voiced /ð/ deferral** — RESOLVED: voiced /ð/ is deferred indefinitely from the chip-tap decoding format (parent note Bottom-line / §Q3). `the/this/that/they` are a future sight-word concern. The `digraphs-th-voiceless` node name + the `phoneme: '/θ/'` schema posture (§6.1) both encode this. See §1.4.
- **sh/ch/th interleaving trigger** — DOWNSTREAM (not a live question for THIS spec): th joins the sh+ch interleave only after th itself is consolidated (~90%). Cross-pool distractors (`thin` vs `shin` vs `chin`) belong to that phase. A planner-side composition decision for a future interleaving spec.

---

## 8. Acceptance criteria

For this spec's downstream impl tickets (Kevin's planner + Devon's wordPack + canon-bake). **GATING PRECONDITION: §0 reconciliation against Dave's `digraph-th-addendum.md` must be complete before these tickets are filed.** The pool, distractor posture, `phoneme` posture, and `hybridMode` posture below are DRAFT (§0) — they may move when Dave's addendum lands. The ch tier's old AC13 reconciliation-gate is the model: the spec is not impl-ready until reconciled.

- [ ] **AC0 — §0 reconciliation complete (GATING):** Dave's `design/research/digraph-th-addendum.md` is on disk; §0 of this spec is filled with the reconciliation table; every "pending Dave's phonics validation" marker in §1 is resolved (pool confirmed or revised); the §1 audit table, §1 final-pool table, §2 distractor matrix, §3 picture briefs, and §6.2 `thumb` resolution all reflect Dave's locked inventory. **No impl ticket is filed until AC0 is checked.**
- [ ] **AC1 — pool size:** `wordPack` for the `digraphs-th-voiceless` sibling node (PR #211) contains exactly the §0-reconciled locked th-voiceless word inventory (the DRAFT is `thin, thick, thumb, path, bath, math, moth` — subject to §0 reconciliation). All voiceless /θ/; ZERO voiced /ð/ words (§1.4).
- [ ] **AC2 — distractor-only pool:** th-tier-specific t-contrast distractor entries exist as text+audio chips per the §0-reconciled matrix. The DRAFT set is `tin` (`vowel: 'i'`, `isTarget: false`), `tick` (`vowel: 'i'`, `isTarget: false`), `pat` (`vowel: 'a'`, `isTarget: false`) as NEW distractor-only entries; `bat` and `mat` are dual-role (reuse the existing short-a CVC `TARGET_WORDS` entries, see §2 dual-role subsection). None of `tin`/`tick`/`pat` is `phoneme`-tagged (§6.1). Weak-trap t-contrast words for `thumb`/`moth` (`tum`/`mot`) are NOT shipped — non-standard / non-words (§2).
- [ ] **AC3 — Trio composition for th-tier (`pickDistractors` + `buildChipOrder`):** for every th-target row in `TARGET_PAIRINGS`, both gentle entries are th-pool neighbours; trap entries are either (a) a t-contrast distractor word + a th-pool neighbour (strong-trap subset: `thin`, `thick`, `path`, `bath`, `math`) or (b) two th-pool neighbours (weak-trap subset: `thumb`, `moth`) per the §2 matrix. **No CVC short-vowel words (EXCEPT the t-contrast minimal-pair partner of a th-target in that trio — `bat` for `bath`, `mat` for `math`), NO sh-tier or ch-tier words, and NO voiced-/ð/ words appear as distractors in th-trios.** Every distractor string is a member of the §0-reconciled th pool or a shipped distractor-only entry (`tin`, `tick`, `pat`, `bat`, `mat`). Tuple shape is `readonly [string, string]` for both `gentle` and `trap`. Render-time positioning remains in `buildChipOrder()`; no new function needed.
- [ ] **AC4 — FORBIDDEN_PAIRS:** the §0-reconciled in-pool + cross-pool pairs added to FORBIDDEN_PAIRS. The DRAFT set is `[thin, thick]`, `[path, moth]`, `[bath, box]`.
- [ ] **AC5 — Emma's first-encounter opener:** fires the first time th-tier is encountered (lifetime-once); persists in localStorage. Includes BOTH the two-letters-one-sound review AND the **articulation scaffold** ("put your tongue between your teeth and blow", or Dave's §0-reconciled exact wording — load-bearing per the parent research note §Q3). Mirrors the sh tier's AC5 / ch tier's AC5 mechanism.
- [ ] **AC6 — Per-session th-digraph-cue:** small visual cue persists in screen corner during th-tier sessions WHILE `digraphs-th-voiceless.state === 'practicing'` (per `applyMasteryRule()` in `src/lib/progress/mastery.ts`). Hidden once the node flips to `'mastered'`. `intro` shows the cue; `practicing` shows the cue; `mastered` hides it. Maps to the existing `Progress.skillLevels[node]` field — no new accuracy-percentage infrastructure. **The cue's CONTENT (keyword-picture `th - thin` vs mouth-position illustration) is gated on Open Question Q1 (§7) — Thomas's lock.** Mirrors the sh + ch tier's AC6.
- [ ] **AC7 — picture assets shipped:** `picture-{...}.svg` for each §0-reconciled th-target at `public/assets/pictures/` (the DRAFT is `picture-{thin,thick,thumb,path,bath,math,moth}.svg`), embedded via `yarn embed-pictures`, each honouring the load-bearing picture briefs in §3 (esp. `thin` = must-read-as-thinness, `thick` = must-read-as-thickness, `thumb` = hand-with-salient-thumb, `math` = numbers-not-school, `moth` = moth-not-butterfly). Plus distractor-only picture assets for the t-contrast traps: `picture-tin.svg`, `picture-tick.svg`, `picture-pat.svg` (silhouette placeholders acceptable per Devon's picture-pack §6 finding #4 path; vector trace deferred to polish backlog). `picture-bat.svg` and `picture-mat.svg` already exist from the short-a CVC tier (reused — `bat`/`mat` are dual-role).
- [ ] **AC8 — mastery rule:** th-tier graduates at 90% accuracy across 3 consecutive sessions (inherited from the sh + ch tiers' lock; same as CVC tiers).
- [ ] **AC9 — isolated-th rule:** th-trios contain only th-pool words + t-contrast distractors, never generic CVC short-vowel words, never sh-tier or ch-tier words, never voiced-/ð/ words. (The t-contrast minimal-pair partners `bat`/`mat` ARE permitted — they are the diagnostic distractors, not generic filler. Cross-tier review of mastered content is handled outside th-trios; sh/ch/th interleaving + cross-pool `shin`/`chin`-for-`thin` distractors are a deferred downstream feature per the parent research note §Q5.)
- [ ] **AC10 — canon bake + 3-place sync:** th-tier session canon is baked via the incremental regen path (`rm public/canon/word-song/level-1/digraphs-th-voiceless.json` then `npx tsx scripts/generateSessionCanon.ts --require-keys`) post-spec-merge; canon JSON committed in the impl PR. Requires the 3-place content-sync contract in the same PR (per `.claude/docs/planner-and-canon.md` §"3-place sync contract"): (1) `WORD_SONG_FIRST_CLASS_FOCUS_NODES` in `api/_planner.ts`, (2) the `WORD_SONG_FOCUS_NODES` iteration set in `scripts/generateSessionCanon.ts`, (3) the combo-count assertion in `scripts/generateSessionCanon.test.ts`. **Confirm the canon focusNode key** — `digraphs-th-voiceless` per the PR #211 SkillNode name; verify against `api/_planner.ts` `VALID_WORD_SONG_FOCUS_NODES` at impl time (the doc currently lists a generic `digraphs` plus `digraphs-sh` — the th node string must be confirmed against the live constant, not assumed).
- [ ] **AC11 — Jessica E2E spec (per `feedback_progression_e2e_mandatory.md`):** th-tier progression includes a failing-first E2E spec — covers (a) first-encounter scaffold fires once (incl. the articulation-scaffold clause), (b) ~90% accuracy across 3 sessions transitions th-tier from `intro → practicing → mastered`, (c) trap distractor (th/t contrast) correctly counts as wrong when selected. Per `.claude/docs/testing-and-ci.md` §4.1.1b, the spec MUST call `test.setTimeout()` sized at `sessions × wall_time + ≥30s headroom` (≈240_000 for a 3-session walk-through). Per `.claude/docs/testing-and-ci.md` §"Failing-first specs for content tiers", the RED signal must be a CONTENT assertion (the first target word is in the th pool) — NOT a progression-only assertion, which would pass green pre-merge because the `digraphs-th-voiceless` progression plumbing already shipped in PR #211.
- [ ] **AC12 — `phoneme: '/θ/'` on all 7 th targets (the structural divergence from ch):** the th-tier `wordPack.ts` rows set `phoneme: '/θ/'` on ALL 7 th-target entries (§6.1) — unlike the ch tier, which OMITTED `phoneme`, and like the sh tier, which set `phoneme: '/ʃ/'`. The t-contrast distractor-only entries (`tin`, `tick`, `pat`) carry NO `phoneme` tag. Concrete tests: (i) `wordPack.ts` — all 7 th targets carry `phoneme: '/θ/'`; (ii) `tin`/`tick`/`pat` carry no `phoneme`; (iii) every §2 matrix pairing passes the `pickDistractors` phoneme-scoping check (the branch is reactivated for th-tier targets but never rejects a v1 th-tier pairing — verified against the sh tier's identical `ship`+untagged-`sip` posture); (iv) the spec documents WHY th sets `phoneme` where ch omitted it (§6.1) so the divergence is not read as an inconsistency.
- [ ] **AC13 — `hybridMode` posture documented + `thumb` resolved:** the §0-reconciled th-tier `wordPack.ts` rows reflect Dave's th-addendum ruling on `hybridMode`. The DRAFT is ZERO `hybridMode: true` entries — but `thumb`'s silent `b` is the one genuinely-uncertain word (§6.2): Dave's addendum rules `thumb` as `hybridMode: false` / `hybridMode: true` / dropped-and-replaced. Concrete tests: (i) `wordPack.ts` th entries set `hybridMode` per Dave's §0-reconciled ruling; (ii) if any th word IS `hybridMode: true`, it does NOT appear in any segmentation/spelling planner code path (the sh-tier hybrid-mode guard already exists — th would consume it, not rebuild it); (iii) the spec (§6.2 + this AC) documents the `thumb` resolution so future work does not assume th is a clean zero-`hybridMode` tier the way ch was.
- [ ] **AC14 — voiced /ð/ exclusion enforced:** no voiced-/ð/ word (`the, this, that, they, them, then, than, there`, and the voicing-ambiguous `with`) appears in the th-tier `wordPack.ts` rows as a target OR a distractor (§1.4). The `phoneme: '/θ/'` tag on the targets (AC12) is the structural enforcement floor — a future `/ð/`-tagged word would be rejected from co-occurring in a th-trio by the `pickDistractors` phoneme-scoping branch. Concrete test: no word in `TARGET_WORDS` or `DISTRACTOR_ONLY_WORDS` referenced by a th-tier `TARGET_PAIRINGS` row carries `phoneme: '/ð/'` or is a member of the §1.4 excluded set.

---

## 9. Sources

This spec is derived from:

- **`design/research/digraph-acquisition-marian.md` (Dave, 2026-05-14)** — the parent research note; sh/ch/th sequencing (§Q2 — voiceless th THIRD, with Sources 7–9 on voiceless-before-voiced), the /θ/ error profile (§Q3 — /θ/→/t/ is "the most systematic", PRODUCTION-only, the `thin` vs `tin` diagnostic distractor, the "tongue between teeth" articulation note, the mouth-position visual recommendation), word count (§Q4 — 8–10, th stock "smaller than sh or ch"), isolation-vs-interleaving (§Q5), the voiced-/ð/ deferral (Bottom-line, §Q3 "th voiced", Sources 12–13 on the structural /ð/→/d/ substitution). **The primary research authority for this DRAFT until the th-addendum lands.**
- **`design/research/digraph-th-addendum.md` (Dave — IN PARALLEL, NOT yet on disk)** — the /θ/-specific research authority; will lock the §1 pool, the voiced/voiceless boundary specifics, the `hybridMode` call (esp. `thumb`'s silent `b`), the articulation-scaffold exact wording, and the /θ/-vs-/s/ second-trap-class question. **§0 reconciliation against this file is the gating precondition (AC0) before impl tickets are filed.**
- `design/word-song/digraphs-ch-word-list.md` (2026-05-14) — the structural predecessor, cloned section-for-section: §0 reconciliation pattern, audit lens, distractor classes (Class 1 contrast-trap + Class 2 pool-neighbour), FORBIDDEN_PAIRS / TARGET_PAIRINGS shape, the dual-role cross-tier distractor pattern, AC numbering, the explicit-`hybridMode`-statement discipline. The th tier diverges from ch on TWO structural points: (1) th SETS `phoneme: '/θ/'` where ch omitted it (§6.1); (2) th's t-contrast traps include dual-role CVC targets `bat`/`mat` (where ch's s-contrast traps were all new or the `sip` dual-role) and the t-contrast pairs are WITHIN-tier-scope trap distractors, not annotation-only cross-pool pairs.
- `design/word-song/digraphs-sh-word-list.md` (2026-05-14) — the original digraph-tier template; the `phoneme`-tag posture (sh set `phoneme: '/ʃ/'` — th mirrors this, ch did not), the long-vowel-allowance §1.3 pattern (th's §1.4 voiced-/ð/-exclusion is the structural analogue), the `hybridMode` field origin.
- `.claude/docs/skill-trees-and-content.md` — `WordEntry` shape (`vowel`, `phoneme`, `hybridMode`), `digraphs-sh` first-class status, the `DIGRAPH_TIER_WORDS` exclusion Set (any digraph tier that sets `vowel` — th does — must be added to it), cross-vowel-tier-load-bearing distractor cluster, pool-extension sync points.
- `.claude/docs/planner-and-canon.md` — canon-bake pipeline, 3-place sync contract, incremental-regen trick, `VALID_WORD_SONG_FOCUS_NODES` (the th node string must be confirmed against the live constant).
- `.claude/docs/testing-and-ci.md` — failing-first E2E timeout sizing rule (§4.1.1b), the "content assertion not progression assertion" RED-signal rule for content tiers on already-shipped SkillNode infra.
- `CLAUDE.md` — Marian profile + project constraints.

---

## 10. Non-obvious findings to surface

These are surfaced for Thomas / Matt / Kevin / Dave via the PR / handoff:

1. **This spec is DRAFT-ahead-of-research — unlike the ch tier, Dave's th-addendum was NOT on disk when this spec was drafted.** Every word choice carries a "pending Dave's phonics validation" marker. §0 reconciliation against `design/research/digraph-th-addendum.md` is a GATING precondition (AC0) before any impl ticket is filed. The ch tier's spec was reconciled against an already-landed addendum; this one front-loads the structural skeleton (audit lens, distractor classes, AC numbering, schema reuse) and explicitly marks the content as provisional. If Dave's addendum diverges, the content moves and the skeleton survives — the same way the ch tier's skeleton survived its own §0 reconciliation.

2. **th SETS `phoneme: '/θ/'` on all 7 targets — the load-bearing schema divergence from the ch tier** (§6.1). The ch tier explicitly OMITTED `phoneme` (no pack-resident word shares `ch` with a different phoneme). **th is the opposite case: `th` is THE canonical multi-phoneme grapheme** (`/θ/` vs `/ð/` — the `WordEntry.phoneme` doc comment names this exact example). Setting `phoneme: '/θ/'` is the architectural floor that lets a future voiced-/ð/ sight-word tier coexist without cross-phoneme chip-trio co-occurrence bugs. th mirrors the SH tier (which set `phoneme: '/ʃ/'`), NOT the ch tier. **This must not be read as an inconsistency with ch — it is a deliberate, grapheme-driven divergence**, and AC12 pins it with a "document WHY" sub-test.

3. **The `digraphs-th-voiceless` SkillNode name itself encodes the voiced/voiceless split** (PR #211). The node is `-voiceless` precisely because `th` spells two phonemes and a `-voiced` sibling is conceivable. §1.4 (voiced /ð/ excluded entirely) + §6.1 (`phoneme: '/θ/'` tag) are the spec-side and schema-side enforcement of what the node name already declares. The voiced-/ð/ words (`the/this/that/they`) are a future SIGHT-WORD concern per the parent research note — NOT a future `digraphs-th-voiced` phonics tier; the parent note is explicit that /ð/ should probably never be a chip-tap decoding tier at all.

4. **th's scaffold gate is ARTICULATION — and it is the one digraph that may need a VISUAL cue, not just a script line** (§5, Open Question Q1). sh's gate was the "two letters, one sound" concept; ch's gate was the c-says-/k/ orthographic trap; both were handled by a script line + a zero-new-asset keyword-picture corner cue. th's gate is the tongue-between-teeth articulation — the parent research note §Q3 explicitly recommends a mouth-position ILLUSTRATION because "TTS alone ... may not be sufficient." That is a NEW asset class. **Open Question Q1 (§7) is the one genuinely-gating Thomas decision in this spec** (A: keyword-picture cue, zero new asset / B: static mouth-position illustration, one new SVG / C: Emma mouth-position animation, new asset + motion-brief item). Recommendation: Option B.

5. **`thumb`'s silent `b` is the pool's #1 uncertainty** (§6.2). The ch tier had a clean zero-`hybridMode` call. The th DRAFT is also zero-`hybridMode` — BUT `thumb` has a silent `b` (`mb`→/m/), which is exactly the non-clean grapheme→phoneme mapping the `hybridMode` flag exists for. The DRAFT leans `hybridMode: false` (the `b` is acoustically inert for chip-tap RECOGNITION, the same way `thick`'s `ck` is) — but Dave's th-addendum must rule: keep as `false` / flag `true` / drop-and-replace-with-`thud`. The impl ticket must NOT assume th is a clean zero-`hybridMode` tier the way ch was — AC13 pins the resolution to §0.

6. **The th tier has the RICHEST strong-trap subset of any digraph tier — 5 of 7** (`thin/tin`, `thick/tick`, `path/pat`, `bath/bat`, `math/mat`). sh had 3 of 7, ch had 3 of 7. Two of th's five strong traps (`bath/bat`, `math/mat`) are especially strong because `bat` and `mat` are SHIPPED CVC targets Marian already knows — making them dual-role cross-tier-load-bearing distractors (the sh/ch tiers established this pattern with `sip`). This means the th/t recognition contrast — which IS the tier's core learning target — is testable with real-word minimal pairs for 5 of 7 words.

7. **The th tier covers BOTH th-positions (initial AND final) — unlike the ch tier (initial-only)** (§1 phonetic-spread check). The DRAFT has 3 word-initial (`thin/thick/thumb`) + 4 word-final (`path/bath/math/moth`). The parent research note §Q2 Source 7 explicitly flags that voiceless /θ/ appears in BOTH positions and both are part of the /θ/ lesson. **But whether word-final th belongs in the FIRST th tier or a follow-up arc is pending Dave's th-addendum** (§7 Q3) — if Dave pulls word-final th out, the pool becomes word-initial-heavy and `think`/`thank` likely come in from reserve to reach 7.

8. **The /θ/→/t/ substitution is PRODUCTION-only and does NOT block the chip-tap recognition task** (§1 Tagalog-phonology check). The parent research note §Q3 is explicit: "the production error is not directly testable — what matters is whether Marian can match the spoken word ... to the text chip." Emma's Azure voice produces /θ/ correctly; the chip-tap test is auditory-match-to-spelling. Thomas should expect th to feel HARDER than sh or ch in terms of Marian's PRONUNCIATION (the /θ/→/t/ substitution is "structural ... not a beginner error that resolves with exposure") but NOT necessarily harder at the chip-tap RECOGNITION task the app actually measures. This is an important expectation-setting note: do not misread Marian's /t/-for-/θ/ pronunciation as a recognition failure.

9. **`bat` and `mat` become cross-tier-load-bearing dual-role distractors** — they started as short-a CVC `TARGET_WORDS` entries; the th tier now references them as `bath`'s / `math`'s t-contrast traps. This generalises the `sip` cluster pattern (`.claude/docs/skill-trees-and-content.md` §"pen is cross-vowel-tier load-bearing"; `sip` is referenced from short-i + sh + ch). Worth updating that doc's cluster table to note `bat`/`mat` are referenced from short-a (own tier) + th once this spec's impl ships. The th tier also adds three NEW distractor-only entries: `tin`, `tick`, `pat`.

10. **The th tier must be added to `DIGRAPH_TIER_WORDS`** in `wordDistractors.test.ts` (per `.claude/docs/skill-trees-and-content.md`: "Any future digraph tier that sets `vowel` ... must be added to `DIGRAPH_TIER_WORDS`"). The th DRAFT sets `vowel` on all 7 entries (like ch, unlike sh) — so the th-tier words will false-fail the cross-vowel exhaustiveness tests in `wordDistractors.test.ts` unless added to the `DIGRAPH_TIER_WORDS` exclusion Set. The impl PR must also extend the count-based / exact-match assertions in `wordDistractors.test.ts` and `wordPictures.test.tsx` (`DISTRACTOR_ONLY_WORDS` exact list — +`tin`/`tick`/`pat`; `FORBIDDEN_PAIRS` exact list — +`[thin,thick]`/`[path,moth]`/`[bath,box]`; `PENDING_PICTURE_PACK` fallback-count — +the th pictures until they ship).

11. **No `-th`-vs-`-the` orthographic complication, no trigraphs** — the DRAFT th pool is all clean `th` grapheme. `three` was rejected (`thr-` stacks digraph + consonant blend, exactly as `chr-` was rejected for ch). `think`/`thank` carry a `nk` CODA blend and are held in §7 Q3 reserve partly for that reason. The one silent-letter complication in the DRAFT pool is `thumb`'s `b` (finding #5). Otherwise the th grapheme→/θ/ mapping is clean across the pool.
    </content>
    </invoke>
