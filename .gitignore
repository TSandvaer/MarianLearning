# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies
node_modules

# Build output
dist
dist-ssr
*.local

# Env
.env
.env.local
.env.*.local

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
Thumbs.db
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Test / coverage
coverage

# Playwright e2e harness (ticket 86c9kwnmx)
playwright-report
test-results
playwright/.cache

# Local progress file (family data; never commit real sessions)
progress.json
public/progress.json

# PWA artifacts
dev-dist

# Pre-baked session canon (ticket 86c9kwhbc — D)
# Source of truth is the prompt + voice config; canon is regenerable
# via `scripts/generateSessionCanon.ts`. Blobs are large (~1.2MB each)
# and generated at deploy time. Don't commit.
public/canon/
