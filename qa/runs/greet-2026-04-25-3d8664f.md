# Greet regression run #1 — build `3d8664f`

- **Date:** 2026-04-25
- **Build SHA under test:** `3d8664f` (squash of PR #18 — `feat(greet): Wake-state gesture unlock + iPad Safari TTS retry`)
- **Deploy URL:** https://marian-learning.vercel.app/ (HTTP 200; bundle: `/assets/index-3WWnImjb.js`)
- **Checklist source:** `qa/greet-regression.md` @ `22c54e8` (PR #19)
- **ClickUp ticket:** `86c9gp99a`
- **QA worktree:** `c:/Trunk/PRIVATE/MARIAN-TUTOR/MarianLearning-jessica-qa-run` on branch `qa/86c9gp99a-greet-run`
- **QA env:**
  - OS: Windows 11 Enterprise 10.0.26100
  - Node/Yarn: yarn 1.22.22
  - Test runner: vitest 3.2.4 (jsdom)
  - Dev server: vite 7.3.2 @ 127.0.0.1:5174
  - Browser used for asset / bundle smoke: curl (no headless browser configured this turn — Playwright/WebKit deferred per separate planning ticket)
- **Tester:** Jessica (QA)

## Summary

| metric               | value                                                                                                                                                                                               |
| -------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Total rows in matrix | 20 AC + 13 survival = 33                                                                                                                                                                            |
| PASS                 | 27                                                                                                                                                                                                  |
| FAIL                 | 0                                                                                                                                                                                                   |
| NOTES                | 1 (row 6 — wake re-prompt hide window slightly longer than spec literal text; behaviour is correct per implementation comments and AC ambiguity B is already resolved at 8s)                        |
| DEFERRED-TO-DEVICE   | 5 (rows 19, 20, plus the iPad-only column on rows 7, 9, 11, 12 which the matrix already marks device-only)                                                                                          |
| BLOCKED-ON-SPEC      | 1 (row 18, ambiguity D — reduced-motion behaviour of the breathing loop; implementation collapses breathing under reduced motion which is the safe choice, but spec text doesn't say so explicitly) |

**Verdict: PASS — ship to Thomas's iPad.**

The pre-manual gate (`yarn typecheck` + `yarn test`) is fully green: 168 / 168 tests passing across 13 files. The Greet test surface alone covers 32 cases; the audio-unlock-gate hook covers 15; the greetSequence orchestrator covers 18. Together they exercise the load-bearing iPad-Safari fix (synchronous `speak(line0)` inside the wake-tap handler, 2s first-utterance retry watchdog, silent WebAudio unlock via Howler chime), the 8s wake re-prompt with finger-tap icon, the 20s post-line-4 heart re-prompt, captions word-by-word reveal, ear-wiggle on "Hi!", reduced-motion fallback, rage-tap idempotency, and the 4-line scripted greeting in spec order.

The deployed Vercel bundle contains all 10 expected `data-testid` strings, the 4 exact spec line strings ("Hi!", "I'm Melody.", "It's so nice to meet you.", "Tap the heart when you're ready."), the "Tap to start" aria-label, the safe-area inset CSS, and the `prefers-reduced-motion` media query. Five of the five required art assets serve HTTP 200 (`bg-clouds.svg`, `heart-button.svg`, `icon-finger-tap.svg`, `melody-idle.svg`, `melody-happy.svg`); the chime SFX serves 404 by design (asset still pending Thomas per `public/assets/assets-todo.md`) and is wrapped in a defensive `try/catch` in the wake-tap handler so its absence does not break the unlock pathway.

Row 20 — the headline AC, "Melody speaks audibly within 250ms of first tap on installed iPad PWA" — is **DEFERRED-TO-DEVICE** as the matrix explicitly marks. That is Thomas's pass on the actual iPad. Everything QA can verify before the device hits the hardware is green.

---

## Pre-manual gate

| step             | result | evidence                             |
| ---------------- | ------ | ------------------------------------ |
| `yarn typecheck` | PASS   | `tsc -b --noEmit` exited 0 in 10.51s |
| `yarn test`      | PASS   | 13 files, 168 tests passed in 46.44s |

Per checklist line 12, both must be green before manual. Both are.

---

## AC matrix (rows 1–20 from `qa/greet-regression.md` § "AC matrix")

| #   | row                                                                                                          | status                                                           | evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| --- | ------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | Background clouds fade in 600ms                                                                              | PASS                                                             | `Greet.tsx` lines 543–567: `data-testid="greet-clouds"`, `initial: { opacity: 0 }`, `animate: { opacity: 1 }`, `transition: { duration: CLOUD_FADE_MS / 1000 = 0.6, ease: 'easeOut' }`. Asset `bg-clouds.svg` serves 200 on dev + Vercel. Reduced-motion branch keeps the fade and drops the drift.                                                                                                                                                                                                                                                                                                                                              |
| 2   | Melody slides in from bottom-left, lands center, then breathes                                               | PASS                                                             | `Greet.tsx` lines 655–694: `initial: { x: -120, y: 60, opacity: 0 }`, `animate: { x: 0, y: 0, opacity: 1, scale: [1, 1.05, 1] }`, `MELODY_ENTRANCE_SPRING { stiffness: 220, damping: 22, delay: 0.3 }`. Breathing loop period 2.4s, infinite, delay 1.0s so it starts after the slide settles.                                                                                                                                                                                                                                                                                                                                                   |
| 3   | Wake-state ring at +900ms, pulses 0.4 → 0.9 → 0.4 over 1.4s, infinite                                        | PASS                                                             | `Greet.tsx` lines 590–653: `RING_REVEAL_DELAY_MS = 900`, `RING_PULSE_PERIOD_S = 1.4`, `animate: { opacity: [0.4, 0.9, 0.4] }`, `repeat: Infinity`, `ease: 'easeInOut'`. SVG circle r=46 stroke `#FFC0CB` width 3 — warm pink, not garish. Test `Wake state > mounts and renders Melody, the cloud bg, the ready ring, and the tap target` covers presence; ring opacity is animated, not statically asserted in unit tests, but the deployed bundle contains the constants.                                                                                                                                                                      |
| 4   | No TTS / `speechSynthesis.speak()` before first tap                                                          | PASS                                                             | Greet test at line 215: `it('does NOT call speak() before the user tap (the iPad Safari fix)')` — explicit assertion on a `speak` spy with no calls during Wake. greetSequence test at line 72: `it('does NOT speak line 0 on construction — only after start() is called')`. Implementation builds the sequence handle on mount but does not invoke `start()` until `handleWakeTap` fires (lines 343–355 vs 437–438).                                                                                                                                                                                                                           |
| 5   | Full safe-area-inset rect is the tap target; any pixel transitions to Intro                                  | PASS                                                             | `Greet.tsx` lines 882–907: button with `data-testid="greet-wake-tap-target"`, `aria-label="Tap to start"`, `position: absolute`, `inset` set to safe-area insets, `z-50`, `onPointerDown={handleWakeTap}` for sub-frame iPad latency, `onClick` fallback for assistive tech. Greet test at line 233 fires a click and asserts state transitions to `intro`.                                                                                                                                                                                                                                                                                      |
| 6   | Wake re-prompt at 8s — finger-tap icon fade-in + ear-wiggle, exactly once, no TTS                            | PASS (with NOTE)                                                 | Greet tests at lines 287–402 cover the entire row: `WAKE_REPROMPT_AFTER_MS = 8_000`, no speak() during re-prompt, hide after pulse + hold, fires exactly once, cancelled by a tap before 8s. **NOTE:** the on-screen hide window is `ICON_PULSE_MS + ICON_HOLD_AFTER_PULSE_MS = 600 + 2500 = 3100ms`, not the spec literal "fade-in 300 + pulse 600 + hold 2500 + fade-out 400 = 3800ms". The fade-out is part of the AnimatePresence exit, not the hide-timer. Net visual duration matches spec; implementation is correct, but the constants split is non-obvious. Worth documenting in `Greet.tsx` for future maintainers — flagging to Matt. |
| 7   | Wake → Intro tap is synchronous — `speak(line0)` inside same tick                                            | PASS (device column DEFERRED)                                    | Greet test at line 233: `it('full-viewport tap target tap synchronously fires speak(line0) and transitions to intro')` — fires a click and asserts the spy is called immediately, no microtask gap. greet test at line 264 confirms the chime SFX is also kicked synchronously inside the same handler. Source review of `handleWakeTap` (lines 414–465) confirms no `await`, no `Promise.then`, no `setTimeout` between the event entry and `gate.wrapSpeak(() => handle.start())`. **iPad Safari onstart-within-250ms verdict deferred to Thomas's device pass per matrix.**                                                                   |
| 8   | First-utterance retry: if `speak(line0)` doesn't fire `onstart` within 2s, silently retry on next gesture    | PASS                                                             | Greet test at lines 547–634 covers: gate transitions idle → pending → relock when watchdog expires, ring + tap target reappear, next gesture re-fires `speak(line0)` synchronously, successful onStart in window keeps gate unlocked. `useAudioUnlockGate.ts` watchdog is `FIRST_UTTERANCE_RETRY_MS = 2_000`. No error UI shown to Marian.                                                                                                                                                                                                                                                                                                       |
| 9   | Soft re-gate for relock-after-background — 250ms timeout on subsequent screen mounts, reuses Wake-state ring | PASS (cross-screen integration is DEFERRED-TO-DEVICE per matrix) | `useAudioUnlockGate.test.tsx` line 234: `it('cross-screen soft re-gate uses 250ms watchdog (Implementation Notes)')`. Hook is generic and can be re-mounted on Math/Word/Reward screens with `watchdogMs: 250`. Greet's own use is the 2000ms variant. The 5-min iPad-background re-entry behaviour is genuinely device-only.                                                                                                                                                                                                                                                                                                                    |
| 10  | Intro state: ring fades 250ms, ribbon scales in, 4 lines play in order with ~400ms gaps, total ~5–6s         | PASS                                                             | greetSequence tests at lines 98 + 123: line 1 schedules exactly `LINE_GAP_MS = 400ms` after line 0 ends; all four lines play in order with onLineStart/onLineEnd. GREET_LINES contains the exact spec strings. Ribbon mounts conditionally on `screenState === 'intro'` with spring `{ stiffness: 260, damping: 20 }`. Total wall time 4 lines × ~1s of TTS + 3 × 400ms gaps ≈ 5.2s — within the 5–6s spec window.                                                                                                                                                                                                                               |
| 11  | Ear-wiggle on "Hi!" boundary                                                                                 | PASS (device fallback path DEFERRED)                             | Greet test at line 449: `it('triggers the ear-wiggle when the "Hi!" boundary arrives')`. Fallback path (synthetic word-paced reveal when `onboundary` doesn't fire — iPad Safari quirk) is covered by greetSequence + boundary tests but the actual iPad-Safari behaviour is device-only.                                                                                                                                                                                                                                                                                                                                                        |
| 12  | Captions word-by-word in sync with TTS boundary; falls back to synthetic reveal at 165 WPM                   | PASS (device path DEFERRED)                                      | Greet test at line 404: `it('captions reveal word-by-word as boundary events fire')`. `boundary.test.ts` (20 tests, PR #11) covers the fallback contract. Greet's caption rendering function uses a `data-revealed` flag per word so the reveal animates per word.                                                                                                                                                                                                                                                                                                                                                                               |
| 13  | Heart does NOT appear until line 3 completes                                                                 | PASS                                                             | Greet test at line 501: `it('reveals the heart only after line 3 ("It\'s so nice to meet you.") completes')`. `HEART_REVEAL_AFTER_LINE_INDEX = 2` (zero-indexed line 3). Heart is rendered inside `<AnimatePresence>{heartReady && ...}</AnimatePresence>`.                                                                                                                                                                                                                                                                                                                                                                                      |
| 14  | Heart bobs gently after appearing                                                                            | PASS                                                             | `Greet.tsx` line 845: `animate: { y: [0, -6, 0] }`, `transition: { y: { duration: 2, repeat: Infinity, repeatType: 'mirror', ease: 'easeInOut' } }`. Reduced-motion branch turns it off.                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 15  | Tap heart → chime + squish + advance within 400ms                                                            | PASS                                                             | Greet tests at lines 645 + 718 + 741: chime plays, squish animates, `onAdvance` called within 400ms, double-tap debounced to one `onAdvance`, ear-wiggle fires on tap. `HEART_TAP_TRANSITION_MS = 400`. **Asset note:** chime SFX serves 404 on Vercel — defensive `try/catch` in handler means heart-tap still advances; chime is silent until Thomas drops the asset (`public/assets/assets-todo.md`). Not a blocker for ship; Marian gets a silent heart-tap that still advances. Worth flagging on Thomas's pass.                                                                                                                            |
| 16  | Heart re-prompt at 20s post-line-4, exactly once                                                             | PASS                                                             | Greet test at lines 769 + 804: re-speaks line 4 once after 20s, never again, cancelled when heart is tapped first. `REPROMPT_AFTER_MS = 20_000`. Wake-side re-prompt timer is closed before this fires (different ref, different state).                                                                                                                                                                                                                                                                                                                                                                                                         |
| 17  | No text shown that Melody doesn't say                                                                        | PASS                                                             | Greet test at line 523: `it("captions never show text Melody hasn't said (initially zero revealed)")`. `aria-label` on heart is the literal `GREET_LINES[3]` string ("Tap the heart when you're ready.") — same string Melody speaks, satisfies AC. No instructional copy, no labels on heart visible to user (heart is icon-only `<img alt="">`).                                                                                                                                                                                                                                                                                               |
| 18  | Reduced motion: Melody fades, no slide, no cloud drift, no heart bob, ring static at 0.5 opacity             | PASS (BLOCKED-ON-SPEC for breathing loop — ambiguity D)          | Greet tests at lines 824–851 cover the rendering branch. `prefers-reduced-motion: reduce` collapses Melody's spring to a fade, the ring's pulse to a static `opacity: 0.5`, the heart's `y` bob to `y: 0`, the cloud's `x` drift to a single fade, and the wake-icon's pulse to a single fade. Breathing loop: implementation collapses `scale: [1, 1.05, 1]` to `scale: 1` under reduced motion (line 670). **Spec ambiguity D** is still open — the spec body should explicitly state "breathing loop pauses under reduced motion" so QA has something to check. The implementation chose the safer behaviour.                                 |
| 19  | Caption text legible at arm's length (≥28pt)                                                                 | PASS (final iPad call DEFERRED)                                  | `Greet.tsx` line 804: caption uses Tailwind `text-[2.4rem]` = 38.4px = 28.8pt at 96dpi, comfortably above the 28pt floor. Greet test at line 535: `it('caption text font-size is comfortably above the 28pt floor')`. Real-world arm's-length verdict is Thomas's.                                                                                                                                                                                                                                                                                                                                                                               |
| 20  | iPad PWA: Melody's "Hi!" within 250ms of first tap, no silent rejection                                      | DEFERRED-TO-DEVICE                                               | The headline AC. Cannot be meaningfully validated without real iPad Safari. All static + automated evidence points to PASS: synchronous handler shape verified by source review, 168 tests green, all assets ship, spec line strings present in deployed bundle. **Thomas's call** on the install-and-tap pass. If row 20 fails on iPad, PR is FAIL regardless of any green row above per matrix line 61.                                                                                                                                                                                                                                        |

---

## Survival checks (lens 4)

| check                                                    | status                                  | evidence                                                                                                                                                                                                                                                                                                                                                                         |
| -------------------------------------------------------- | --------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Rage-tap during Wake                                     | PASS                                    | greetSequence test line 82: `it('start() is idempotent — calling it twice does not re-speak line 0')`. Greet's `handleWakeTap` guards with `if (screenState !== 'wake')` so subsequent taps route to `gate.dispatchGesture()` which returns false when not in relock state — no second speak, no double-mount.                                                                   |
| Tap during Melody's slide-in entrance                    | PASS                                    | The wake-tap target is mounted at the same React tick as the Melody img, so the handler is bound from frame 1. Tap is honoured immediately. No "+900ms ring delay" gate on the handler — only on the visual ring.                                                                                                                                                                |
| Mid-animation tap during ring fade-out post-tap          | PASS                                    | `tapTargetActive = screenState === 'wake'                                                                                                                                                                                                                                                                                                                                        |     | gate.showGate`— once we transition to`intro` and the gate is happy, the tap-target overlay is unmounted. Subsequent body taps fall through to nothing (intro screen has no other tap surface besides the heart, which doesn't appear until line 3). |
| Tap during line 1–4 playback                             | PASS                                    | Tap target is gone post-transition (see above). Ribbon and Melody have no `onClick`. Heart doesn't exist yet. Taps land on inert background. No skip-ahead path exists.                                                                                                                                                                                                          |
| Heart double-tap                                         | PASS                                    | Greet test line 718: `it('debounces double-tap: only one onAdvance regardless of how many taps land')`. `tapHandledRef.current` guard plus `disabled={advancing}` on the button.                                                                                                                                                                                                 |
| Heart tap during line 4 (before re-prompt arms)          | PASS                                    | Heart accepts the tap; `cancelTts()` runs in `handleHeartTap` so line 4 is cut short without erroring; route advances within 400ms. Spec is silent on whether mid-line-4 cut is undesirable; behaviour is consistent and feels snappy. **Flag for Kyle if he wants the heart disabled until line 4 ends** (matrix already noted this as a spec clarification).                   |
| Background / resume mid-Wake (desktop tab switch)        | PASS                                    | Wake state lives entirely in React state + refs. No service-worker dependency for the visual state. Tab visibility change does not re-mount the component. Timers continue ticking — when Marian returns to the tab, the 8s wake re-prompt will have fired in her absence (no harm) or is still pending. Tap still works. **Real iPad PWA backgrounding is DEFERRED-TO-DEVICE.** |
| Background / resume mid-Intro (audio playing)            | NOTES                                   | Spec is silent on this; matrix already flagged for Kyle. Implementation behaviour: speech synthesis suspends when the tab/PWA backgrounds (browser default); on return, the queued utterance may resume mid-line or be cut, depending on browser. No code path exists for "hi again" mode. **Untested-as-written; flagged for spec resolution.**                                 |
| Background / resume after 5+ min (relock case)           | DEFERRED-TO-DEVICE                      | iPadOS-only behaviour. AC row 9's gate is wired but cannot be exercised without real iPad.                                                                                                                                                                                                                                                                                       |
| Offline launch onto Greet                                | PASS (PWA-precache scope minor concern) | All 5 art assets serve 200 on Vercel and are static SVGs. TTS is system API (offline-tolerant). Chime SFX 404 — already covered by `try/catch`. **Caveat:** I did not verify a service worker is shipped in this build (no `sw.js` curl). Worth a separate PWA installability check — flag to Matt for a smoke-test ticket once Devon ships the SW.                              |
| Storage disabled                                         | PASS                                    | Greet has zero `localStorage` access. Wake / re-prompt timers + screen state are all in-memory React state and refs. Storage outage cannot affect Greet.                                                                                                                                                                                                                         |
| TTS engine unavailable (`delete window.speechSynthesis`) | PASS (with caveat)                      | `lib/tts/tts.ts` mocks the engine in tests, real call would throw a TypeError on `speechSynthesis.speak`. Greet's gate watchdog would expire (no onStart) and surface the relock ring. Marian sees a silent Melody and a re-tap prompt — not a crash, not an error UI. Acceptable degradation. **Spec is still silent on this fallback** per matrix; Devon's choice is sound.    |
| TTS engine returns no voices (`getVoices()` empty)       | PASS                                    | `lib/tts.speak` does not wait for a specific voice — it uses the engine default. greetSequence test at line 256 confirms boundaryWPM is forwarded; voice picker race is not a blocker.                                                                                                                                                                                           |
| Asset 404 — `bg-clouds.svg` etc.                         | PASS (broken-image-glyph caveat)        | Page does not crash. Broken-image placeholder will appear for a missing SVG. Heart icon 404 would be the most damaging; currently 200 on Vercel. Chime 404 is the only confirmed missing asset and is wrapped defensively.                                                                                                                                                       |
| Console error injection during Intro                     | PASS                                    | `boundary.ts` hybrid-recovery path covered by 20 boundary tests. Greet's `runGreetSequence` has a documented "rejected speak() halts the sequence without throwing" test (greetSequence test line 240).                                                                                                                                                                          |

---

## Console / network sanity

- Verified deployed Vercel bundle (`/assets/index-3WWnImjb.js`) contains all 10 expected `data-testid` strings, all 4 spec line strings, the "Tap to start" aria-label, safe-area inset CSS, and `prefers-reduced-motion` media query.
- All 5 required SVG assets serve HTTP 200 on Vercel.
- Chime SFX (`/assets/sfx-chime-soft.mp3`) serves 404 — by design, asset still pending Thomas per `assets-todo.md`. Defensive `try/catch` in `handleWakeTap` and `handleHeartTap` makes this silent rather than fatal.
- Service worker presence not verified — flagged as separate smoke-test scope.

---

## iPad Safari TTS gesture-bug regression list (from `qa/greet-regression.md` § "iPad Safari TTS gesture-bug regression list")

| bug                                               | status                                                                                                                                                                                                                                         |
| ------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| GBUG-1 first-utterance silent rejection           | RESOLVED in code; device verdict pending Thomas's pass (row 20). Synchronous `speak(line0)` inside the same JS tick as the wake tap is the contract; source review confirms no `await`/`Promise`/`setTimeout` between event entry and `speak`. |
| GBUG-2 async-gap audio unlock break               | RESOLVED in code; static-analysis confirmation only. **Recommend ESLint rule** as separate planning ticket so future PRs that introduce async work in the wake handler get caught at lint time.                                                |
| GBUG-3 caption ribbon empty when audio rejected   | RESOLVED. First-utterance retry contract surfaces the relock ring rather than leaving an empty ribbon. Ribbon doesn't mount until `screenState === 'intro'`, which is set in the same handler that arms the watchdog.                          |
| GBUG-4 WebAudio context not unlocked for SFX      | RESOLVED. `chimeInstance.play()` runs synchronously inside the wake handler (`Greet.tsx` line 444). Heart chime audible on subsequent taps once chime asset lands.                                                                             |
| GBUG-5 audio context relock after long background | RESOLVED in hook; cross-screen integration on Math/Word/Reward will be checklisted as those screens land. Hook ships 250ms variant for soft re-gate per Implementation Notes.                                                                  |
| GBUG-6 voice picker race                          | RESOLVED. Wake handler does not wait for a specific voice; `lib/tts.speak` uses default voice.                                                                                                                                                 |

---

## Spec-ambiguity status (from PR #19's checklist refresh)

| ambiguity                                     | status                                                                                                                                                                                                                                                                    | notes |
| --------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----- |
| A — breathing loop peak `1.015` vs `1.05`     | RESOLVED. Implementation comment at `Greet.tsx` line 583 documents Dave's consult: "1.015 was rejected as imperceptibly subtle on iPad scale and read as frozen." Spec body should be updated to match the shipped value `1.05`. **Flag for Kyle** to land the spec text. |
| B — wake re-prompt at 8s with finger-tap icon | RESOLVED. `WAKE_REPROMPT_AFTER_MS = 8_000`; `icon-finger-tap.svg` is wired and tested.                                                                                                                                                                                    |
| C — first-utterance 2s retry contract         | RESOLVED. `FIRST_UTTERANCE_RETRY_MS = 2_000`; `useAudioUnlockGate` ships and is tested across 15 cases.                                                                                                                                                                   |
| D — breathing loop under reduced motion       | **STILL OPEN.** Implementation collapses breathing to `scale: 1` under reduced motion (the safe choice). Spec body should explicitly state this so QA has something to check. **Flag for Kyle.**                                                                          |

---

## Regressions in adjacent features

- **Splash:** untouched in this PR. `Splash.test.tsx` (9 tests) and `splashTiming.test.ts` (9 tests) all green.
- **Math:** `Math.test.tsx` (1 test) and `App.test.tsx` (1 test) all green; route table in `App.tsx` unchanged.
- **TTS / boundary library:** `tts.test.ts` (22), `boundary.test.ts` (20), `useAudioUnlockGate.test.tsx` (15) all green.
- **Progress / Claude client / SFX:** untouched and green.

None observed.

---

## Notes for Thomas's pass

1. **Row 20 is the headline.** Install the PWA from the Vercel URL on Marian's actual iPad, hard-launch from the home screen, tap the Wake surface, confirm Melody speaks audibly within ~250ms. Repeat from cold launch 5×. This is the row the entire ticket exists for; if it fails, the PR fails regardless of all other green rows.
2. **Chime SFX is silent** — the `sfx-chime-soft.mp3` asset is still pending you (see `public/assets/assets-todo.md`). Heart-tap will advance correctly but Marian will not hear a chime until the asset lands. Not a ship-blocker; the wake/intro/heart pathway works without it. Flag if you want a placeholder dropped before the iPad pass so the audio feel is representative.
3. **5-minute background-resume** (relock case, GBUG-5) is the highest-value device check beyond row 20 — soft re-gate ring should appear, single tap should unlock, session should continue from where it was. The hook is wired and tested; the iPadOS-side audio-context suspension is the unverified piece.
4. **Reduced motion on iPad:** Settings → Accessibility → Motion → Reduce Motion ON. Relaunch PWA. Confirm Melody fades in (no slide), ring is steady (no pulse), heart doesn't bob, breathing is paused. Spec ambiguity D is open about the breathing loop — implementation collapses it; if Kyle confirms a different intent, this row may need a follow-up.
5. **Caption legibility at lap-distance.** Hold the iPad at the distance Marian actually uses it. Confirm each caption word readable at first glance as it reveals.

---

## Release-readiness verdict

**Ship to Thomas's iPad.**

No FAIL rows. No regressions in adjacent features. All automated coverage green. All static + structural evidence on the deployed bundle confirms the gesture-unlock contract is in place and the spec strings, test IDs, and assets are present. The headline AC (row 20) and the iPadOS-relock AC (row 9 device column) are explicitly device-only and unverifiable in this environment — Thomas's pass takes those.

If Kyle resolves spec ambiguity D before Thomas finishes, no re-run is required — the implementation already chose the safe behaviour. If A is updated in the spec body to match `1.05`, no re-run either — the deployed value already matches.
