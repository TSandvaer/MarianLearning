# Word Song screen (CVC short-a, picture discrimination) — Regression Checklist

ClickUp tickets:

- **TBD** — Word Song impl ticket. Devon is implementing in parallel on `feat/word-song-impl` (PR not yet open at time of authoring this checklist). When the PR opens, this checklist becomes runnable end-to-end. Until then, every AC row is **BLOCKED-on-impl** and recorded against the locked spec contract only.

Lineage:

- `1edfd9e` (PR #61) — canonical Word Song spec authored by Kyle. 875 lines. Adds `design/screen-4-word-song.md`. Locks: 8-problem flow, picture-discrimination input model (locked, not letter-side), adaptive 2-tier semantic distractor ramp (gentle problems 1–3, trap problems 4–8, cutoff at 3 mirroring Math), unified stardust + streak with Math (`marian-tutor.stardust.v1`, `[3, 5, 8]` thresholds), no red X, 2-wrong hint state (sound-out + blend), 3-wrong guided completion (no stardust, no streak, no `totalCorrect` bump), `word.p{N}.{read,correct,reprompt,hint,giveAnswer}` Path A audio bundle (~40 utterances, ~600 KB), per-letter phoneme MP3s via Howler (NOT Web Speech), unified HUD shared with Math.
- `938b926` (PR #62) — Dave's phonics-sequence research memo. 5 actionable findings: (i) short-vowel order should be `o → u → i → e` (NOT the diagnostic's `o → u → e → i` — `/i/`–`/e/` confusion mandates separation); (ii) curated short-a inventory `cat, hat, bat, mat, bag, map, fan, man, pan, tap, nap, cap, rat, van`; (iii) digraphs after CVC mastery on 2–3 vowels (NOT all five); (iv) 10 sight-words concurrent with CVC (`the, a, I, to, and, was, for, you, is, of`); (v) one new phonics concept per session (working-memory ceiling for 8yo L2 learner). Word Song v1 inherits findings (ii) directly via the picture-pack and (v) by capping problems at 8/session. Findings (i), (iii), (iv) inform downstream literacy screens, not v1 Word Song.
- `55c588a` (PR #64) — Kyle's CVC short-a picture-pack proposal v1. Adds `design/word-song-picture-pack.md`. Locks: 14 target words (`cat, hat, bat, mat, bag, fan, man, pan, cap, can, tag, dad, jam, van`) + 8 distractor-only pictures (`bus, sun, dog, fox, cup, pen, log, pot`). Drops `rat, tap, nap, map, sad` from the candidate union (silhouette / picture-ambiguity / second-character / abstraction / emotional-conflict reasons). Encodes 5 forbidden silhouette pairs: `cat↔dog`, `bus↔van`, `pan↔pot`, `cap↔hat`, `man↔dad`. Documents the master distractor pairing matrix (gentle + trap pairs per target). 22 illustrations, all SVG <30 KB, palette locked to Melody tokens (`--my-rose`, `--my-cream`, `--ink`, `--sparkle`).
- `f484cd6` (PR #54) — Path A `playUtterance` adapter pattern. Math screen is its first consumer; Word Song will be the second. The same `prepareXxxPathA(plan, plan.id)` + `AbortController` cleanup pattern is the implementation pointer Devon will follow for `prepareWordSongPathA(plan, plan.id)` — though as of authoring, the live `/api/claude` POST returns `tts-failed` (HTTP 502) since the Azure REST swap (PR #53). Word Song inherits the same server-side blocker; degrades cleanly to silent-but-captioned via the documented fallback.
- `0600097` (PR #60) — iOS OS-level audio session unlock per gesture. Word Song's audio-unlock-gate consumer path inherits this fix. Survival check "long-idle audio decay" tests the same surface.
- `2c3c43e` (PR #59) — Howler ctx resume await before play. Same inheritance; survival check "background/resume mid-sequence" stresses this.
- `86c9grn33` (Math impl, PR #40 + PR #54) — structural sibling. Word Song is the second screen consuming Path A; HUD + stardust + streak + audio-unlock-gate are pre-existing surfaces Word Song reuses. Math regression rows for `useAudioUnlockGate.ts`, `src/lib/sfx/**`, `src/screens/Math/stardust.ts` (or its shared-module successor) are also Word Song regression rows.

Spec / source-of-truth:

- `design/screen-4-word-song.md` (canonical, post-PR-#64 picture-pack alignment).
- `design/word-song-picture-pack.md` (canonical short-a content layer; supersedes the inline candidate list in spec §Open Questions #3).
- `design/research/phonics-sequence-marian.md` (canonical phonics ladder; Dave's memo, locked).

This checklist is reusable: rerun every merge that touches `src/screens/WordSong/**` (when it lands), `src/screens/_shared/{SessionHud,stardust,streak}.tsx` (if Devon refactors per spec §Implementation pointers), `src/lib/audio/useAudioUnlockGate.ts` (Word Song will be a consumer alongside Greet + Math), `src/lib/sfx/**` (Word Song uses `createSfx` directly per spec), `public/assets/audio/phonemes/**` (~14 new phoneme MP3s when authored), `public/assets/pictures/**` (~22 new SVG pictures when authored), or `src/App.tsx` (Word Song route wiring). Aim for ~14 minutes desktop end-to-end (slightly more than Math's ~12 because the picture-trio silhouette check + phoneme-letter-tap exploration adds two new survival rows), plus the iPad pass.

`src/screens/Greet.*` is **OUT OF SCOPE** for Word Song — Word Song must NOT regress Greet. Survival row 19 is the regression guard. If a Word Song PR ever reaches into Greet, run `qa/greet-regression.md` as well.

`src/screens/Math/**` is **OUT OF SCOPE for new code in a Word Song impl PR** — but the shared-module refactor (per spec §Implementation pointers) WILL touch the Math import path. If Devon's PR refactors stardust + HUD into `src/screens/_shared/`, run `qa/math-screen.md` AC rows 3, 6, 7 (stardust + streak threshold + persistence) as a Math regression smoke alongside this checklist.

`api/**` is **OUT OF SCOPE for new code in a Word Song impl PR** — Word Song doesn't ship server-side changes. Survival row 20 is the smoke that confirms `/api/claude` is still healthy post-merge. The Word Song session-start payload format (32–40 utterances per session) does NOT change the API contract; the `kind: "session-start"` envelope already carries arbitrary `utterances[]` arrays.

## How to run

1. `yarn install` (once per branch).
2. `yarn typecheck && yarn lint && yarn test --run` — all green before any manual step. Baseline at time of authoring this checklist (commit `5b981d0`): **29 test files, 589 passing, 51.9s**. Word Song impl will add ~25–40 tests (`WordSong.test.tsx`, `wordDistractors.test.ts`, `wordSequence.test.ts`, `phonemeAudio.test.ts`, `cvcShortaPack.test.ts`). Post-impl baseline expected: ~30+ test files, ~620+ passing. If local count diverges below the impl-PR's advertised baseline, investigate before continuing.
3. Manual desktop steps assume either `yarn dev` running locally on `http://localhost:5180/` (or default `5173`) **OR** a Vercel preview is up for the merged commit (the orchestrator will confirm the URL after Devon's impl PR deploys).
4. iPad rows: install the PWA from the Vercel preview URL onto Marian's actual iPad (or QA loaner if available), launch from home screen, navigate to Word Song (Session 2+ standalone entry, OR mixed-session entry mid-Math).
5. Walk through the matrix below. Mark each row PASS / NOTES / FAIL / DEFERRED-TO-iPad / BLOCKED-on-impl / BLOCKED-on-Azure-config / BLOCKED-on-pictures / BLOCKED-on-phonemes.

Touch-target / pixel-perfect / iPad-Safari-quirk rows are **DEFERRED-TO-DEVICE** — Thomas owns the binding verdict on a real iPad. Path A audio rows are **BLOCKED-on-Azure-config** until the Azure REST swap's missing creds are fixed (see math-screen.md run results 2026-04-26 for the same blocker). Picture-asset rows are **BLOCKED-on-pictures** until the picture pack lands (Option A/B/C decision pending Thomas per `word-song-picture-pack.md` §Open Questions #1); inline-SVG fallbacks per spec are acceptable v1 contract surface for the Devon impl PR. Phoneme audio rows are **BLOCKED-on-phonemes** until the ~14 phoneme MP3s are authored (asset-pipeline call pending Matt per spec §Audio integration contract → "Phoneme audio").

---

## Spec drift / ambiguities flagged for Kyle, Matt, Thomas, Dave

Captured during checklist authoring, before Devon's impl lands. Each row gets: **item / owner / trigger to resolve**. Items map back into the AC matrix where they affect a specific row.

| #   | Status                          | Item / owner / trigger                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| --- | ------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| A   | **Open — flag for Kyle**        | **Spec §Wrong-answer policy "Hint choreography for Word Song" line 282–293 says the hint plays the word card's letters in sequence, with each letter highlighting and playing its phoneme.** This is a literacy-specific deviation from Math's hint state (Math reads a "Look. Three. And two more. How many now?" line). Per spec line 297 the TTS line is "Let's look. {word}." plus the per-letter phoneme files between Melody's words. The exact word-by-word interleave is not pinned in the spec — does Melody's TTS pause for each phoneme, or does the phoneme play _on top of_ Melody's TTS? AC row 12 below assumes "pause and interleave" per the timeline at lines 286–293 (each letter's phoneme fires at t=600/1200/1800ms while Melody's TTS is silent in between, then Melody resumes for the blend at t=2400ms). **Owner:** Kyle. **Trigger:** Devon's impl PR — if his timing differs, flag back to Kyle for spec clarification.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| B   | **Open — flag for Kyle**        | **Spec §Audio integration contract → §Phoneme audio table at lines 338–353 lists 14 phoneme files** for short-a CVC coverage. The picture-pack proposal at PR #64 §Per-word picture briefs locks the 14 target words `cat, hat, bat, mat, bag, fan, man, pan, cap, can, tag, dad, jam, van`. Cross-checking the letters needed: `c, a, t, h, b, m, g, f, n, p, d, j, v` = **13 distinct consonants + 1 vowel = 14 phonemes**, which matches the spec table. But the spec lists `r, s` in the phoneme inventory (for `rat`/`ram`/`sat`/`sad` — words DROPPED from the picture pack per PR #64) and OMITS `j, v` (for `jam`, `van` — words ADDED in PR #64). **Action:** spec phoneme inventory needs reconciliation with PR #64's actual word list. **Owner:** Kyle (one-line table edit) or Matt routes to Devon to encode the right inventory in `phonemeAudio.ts`. **Trigger:** Devon's impl PR — encode the ACTUAL letters needed by the v1 14-word target list (`a, b, c, d, f, g, h, j, m, n, p, t, v` = 13 phonemes; `s` from `sad`, `r` from `rat`, etc. are not needed for v1). The 14-file count in spec §Audio integration contract is a useful round number but the _identities_ should match PR #64.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| C   | **Open — flag for Matt**        | **Picture-asset sourcing not yet decided.** PR #64 proposes 3 options (A/B/C — commission, curate, AI-generate) with default Option A. Until Thomas signs off and Matt routes the work, Devon's impl PR will ship with **inline-SVG placeholder shapes** (parallel to Math's `SparkleGlyph`/`FlowerGroup`/`FlowerGlyph` inline fallbacks per `Math.tsx:1051-1129`). AC row 5 below (picture chip rendering) is **BLOCKED-on-pictures** for the canonical asset verdict; placeholder render is the v1 contract surface. **Owner:** Matt (route the order), Thomas (taste sign-off on whatever Matt brings). **Trigger:** Thomas's morning review of PR #64.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| D   | **Open — flag for Matt**        | **Phoneme audio sourcing not yet decided.** Spec §Audio integration contract → "Phoneme audio" line 354–355 says "Sourcing approach is open (voice actor session vs existing phonics audio library); flag to Matt for the asset pipeline call." 14 MP3 files needed, voice-matched to `en-US-AnaNeural` (so Melody's TTS register and the static phonemes don't clash). Until authored, Devon's impl will silently no-op phoneme calls via `createSfx`'s warn-once-then-dead-handle pattern — same fallback as Greet's pre-PR-#25 era and Math's current SFX state. AC rows 13, 14 (letter-tap phoneme, hint-state phoneme sound-out) are **BLOCKED-on-phonemes** for the audible-playback verdict. **Owner:** Matt. **Trigger:** Matt's asset-pipeline routing decision (parallel to picture-pack sourcing — could be the same vendor session if Option A is picked).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| E   | **Open — flag for Matt**        | **Stardust + HUD shared-module refactor scoping.** Spec §Implementation pointers line 670–677 says "Stardust + HUD logic should be extracted to a shared module if not already done by the Math impl … If Math hasn't extracted these yet, Devon should refactor as part of this ticket." Math's current impl has `MathHud.tsx` and `stardust.ts` _local to_ `src/screens/Math/` (per Math regression list in `qa/math-screen.md`). Devon's Word Song impl will therefore include a refactor — moving `Math/stardust.ts` to `src/screens/_shared/stardust.ts` and `Math/MathHud.tsx` (if it exists) to `src/screens/_shared/SessionHud.tsx`. **This makes the impl PR larger and cross-cuts Math.** Matt should confirm with Devon whether the refactor stays in scope or gets split out. If split, Word Song duplicates code temporarily — flagged as anti-pattern by spec line 676. **Default:** include refactor in Devon's impl PR. **Owner:** Matt + Devon. **Trigger:** Devon's impl PR opens — diff confirms refactor scope.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| F   | **Open — flag for Kyle**        | **Streak unification across Math + Word Song within a session — assumed but not architected.** Spec §Streak treatment line 240 says "streak counts unbroken-correct-on-first-tap across Math + Word Song within a single session." This requires a streak-count surface that survives the Math → Word Song transition (and vice versa). Math's current impl holds `streak` state in `Math.tsx`'s component-local refs (`Math.tsx:540-552` per `qa/math-screen.md`). The transition Math → Word Song unmounts Math; the streak ref dies. **Either** the streak state needs to be lifted to a parent component (orchestrator-level), **or** stardust storage needs to gain a `currentSessionStreak` field (and Word Song reads it on mount). Spec doesn't pick a side. **Default until decided:** Devon picks the lift-to-parent approach (cleaner; no localStorage involvement for ephemeral session state). **Owner:** Kyle (architecture clarification) or Devon (impl call, with Matt's blessing). **Trigger:** Devon's impl PR — diff confirms approach. AC row 8 below depends on whichever approach lands.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| G   | **Open — flag for Thomas**      | **Streak threshold values inherited from Math — Math Open Q #2 still pending.** Math ships `[3, 5, 8]` per `constants.ts:20`. Word Song spec §Streak treatment line 242 says "Same threshold values as Math: bonuses at 3, 5, 8 (default until Thomas weighs in on Open Question #5)." Spec §Open Questions #5 explicitly carries this forward. **Default:** ship `[3, 5, 8]` — same as Math. If Thomas overrides for Math (one-line edit to `Math/constants.ts`), Word Song must inherit (single source of truth: import from `Math/constants.ts` or move to `_shared/constants.ts`). **Owner:** Thomas. **Trigger:** Thomas's taste call on Math's open question — same answer applies here.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| H   | **Open — flag for Thomas**      | **Mixed-session streak threshold — spec §Open Questions #6 explicitly open.** If a session is ~16 problems (8 Math + 8 Word Song) and streaks count across both, do thresholds shift to `[3, 6, 12]` for the longer total length, or stay at `[3, 5, 8]`? Spec defaults to staying at `[3, 5, 8]` — predictable, generous, anti-dark-pattern preferring more-but-smaller-bonuses. **Owner:** Thomas, possibly with Dave review. **Trigger:** mixed-session sequencer ticket (separate work). For Word Song impl PR, the v1 default `[3, 5, 8]` is the locked contract.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| I   | **Open — flag for Devon**       | **Speaker icon — ship in v1 or defer?** Spec §Visual layout line 94, §Implementation pointers "Optional v1 trim" line 767–769 explicitly says: "Default for spec: ship it. Flagged as a potential trim in §Implementation pointers if Devon scopes it out." If Devon defers, AC row 11 (speaker tap replays read-aloud) becomes DEFERRED-v2 and the only word-replay surface is the per-letter phoneme taps. **Default:** ship the speaker icon. If Devon trims, flag back to Matt. **Owner:** Devon. **Trigger:** Devon's impl PR — speaker icon present or not. AC row 11 verdict adjusts accordingly.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| J   | **Open — flag for Devon**       | **Session-end callback shape — spec §Transition out line 540 lists 4 fields: `{ totalCorrect, totalStardust, finalStreak, surface: 'word-song' }`.** Math currently ships 4 fields too: `{ totalCorrect, totalStardust, finalStreak, earnedThisSession }` per `Math.tsx:98-104`. The `surface` field on Word Song is parallel to Math's missing `surface` field — suggests the orchestrator-level session-end recap (ticket `86c9grnjd`) wants both `surface` AND `earnedThisSession`. **Action:** Word Song impl should ship 5 fields: `{ totalCorrect, totalStardust, finalStreak, earnedThisSession, surface: 'word-song' }`. Math then needs `surface: 'math'` added in a follow-up. Track as drift item J for Kyle to update both spec callback signatures consistently. **Owner:** Devon (impl) + Kyle (spec edit). **Trigger:** Devon's impl PR — confirm 5-field shape. AC row 17 below pins this.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| K   | **Open — flag for Kyle**        | **`bat` (animal) cultural fit — picture-pack PR #64 Open Q #4.** Bats carry Halloween/spooky connotations in Western culture; Marian's Filipino context is more neutral (`paniki` = fruit bat, common). Picture-pack defaults to "cute friendly Sanrio-style flying bat, no fangs, big eyes." If Thomas vetoes, picture-pack drops `bat` and picture-pack §Q4 says "replace with `tan` or `gas`". But `bat` is in Dave's recommended inventory and is a useful trap-tier neighbour for `cat, hat, mat, rat`. Dropping it shrinks the trap-tier surface significantly. **Default until decided:** keep `bat` with the friendly-Sanrio brief. **Owner:** Thomas. **Trigger:** Thomas's morning review of PR #64. AC row 5 below tracks the 14-target-word count, which adjusts if `bat` drops.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| L   | **Open — flag for Thomas/Kyle** | **`dad` keep-or-drop — picture-pack PR #64 Open Q #3.** Two human figures in the pack (`man` and `dad`) raises the silhouette-similarity risk; mitigation is the parent-with-child pose for `dad`. The forbidden-pair rule (`man↔dad`) prevents same-trio pairing. **Default until decided:** keep `dad` with the parental-pose mitigation. If dropped, replacement candidates are `had, sat, ham, tan` (none ideal — `had`/`sat` are verbs hard to picture, `ham`/`tan` are vocabulary edges for a Filipino 8yo). **Owner:** Thomas. **Trigger:** PR #64 review. AC rows 5, 6 (target-word count + forbidden-pair encoding) adjust if `dad` drops.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| M   | **Open — flag for Devon**       | **Letter-tap independence — spec §Open Questions #9 explicitly open.** Spec defaults to "letter taps are independent: no streak break, no attempt count, no Melody reaction." The alternative is "10+ letter taps before chip tap signals 'she's stuck' and triggers the hint proactively." Default is reasonable and easier to ship. **Owner:** Thomas / Dave. **Trigger:** v1 telemetry from Marian's actual usage. For Word Song impl PR, the v1 default (independence) is the locked contract.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| N   | **Open — flag for Kyle**        | **Session-1 listen-only `dog` screen vs Session 2+ active discrimination — spec §Why this differs from Session-1's Word Song.** The two specs (`session-1.md` lines 376–522 listen-only single-`dog` problem vs `screen-4-word-song.md` 8-problem active discrimination) are intentionally different shapes. **Action:** when Devon's impl ships, the orchestrator's session-routing logic must distinguish Session 1 entry → listen-only Word Song (a separate surface from the `WordSong` component this checklist covers) vs Session 2+ entry → 8-problem `WordSong`. AC row 1 below assumes the 8-problem flow; Session 1 listen-only is a separate component (or a separate `mode` prop on the same component). **Default:** check Devon's impl — if a single component handles both, AC row 1 needs a sub-row for the Session 1 mode. **Owner:** Kyle. **Trigger:** Devon's impl PR diff.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |

All 14 items are open at time of authoring. None are blocking the spec merge (which already happened in PR #61/#64); they're either Devon-impl decisions (E, F, I, J), Thomas/Kyle/Matt-asset-pipeline decisions (C, D, K, L), or follow-up spec/research tracking (A, B, G, H, M, N). When Devon's impl PR opens, items E, F, I, J, M get binary verdicts; items A, B will need spec one-liners; items C, D, G, H, K, L, N stay as drift through impl and resolve later.

---

## Distribution

**Pre-impl distribution (this is the "what the AC matrix will look like when Devon's PR lands" projection):**

- **AC matrix:** 17 AC rows (rows 1–17), plus 4 survival/regression rows (rows 18–21) = **21 rows total.**
- **Automated (projected):** **9** (rows 1, 2, 3, 4, 6, 7, 9, 16, 21 — coverable by Vitest in `src/screens/WordSong/{WordSong.test.tsx, wordDistractors.test.ts, wordSequence.test.ts, phonemeAudio.test.ts, cvcShortaPack.test.ts}` once they exist + cross-suite re-runs in `Greet.test.tsx`/`Math.test.tsx`).
- **Manual desktop (projected):** **5** (rows 5 picture rendering, 8 unified streak across screens, 12 hint choreography, 15 auto-advance timing, 17 session-end callback observation).
- **iPad-only (projected):** **2** (row 13 letter-tap phoneme audible after wake-tap on installed PWA, row 14 hint phoneme sound-out audible — gated on PR #64 phoneme assets + Azure config + Thomas's iPad pass).
- **Survival/regression:** rows 18 (auto + manual desktop), 19 (auto), 20 (manual desktop / curl OPTIONS), 21 (auto).
- **Path A audio rows (10, 11, 12, 13, 14):** auto portions assert the contract surface (caption rendering via `onWordTick`, ribbon `aria-live`, phoneme `Howl` instances cached lazily, etc.) without requiring real TTS. Manual/iPad portions are BLOCKED-on-Azure-config + BLOCKED-on-phonemes.

Plus 7 survival checks (lens 4 — extended) and 4 console/network sanity items below the matrix.

**Pre-impl status, every row: BLOCKED-on-impl.** When Devon's PR opens, every row gets a real verdict.

---

## Run results — pre-impl placeholder (2026-04-26 / Jessica)

This block is a **placeholder** — Devon's Word Song impl has not landed. When his PR opens, replace this block with a "Run results — YYYY-MM-DD (Jessica) — branch <slug> @ <SHA>" block following the template from `qa/math-screen.md` lines 95–253.

**Pre-impl assertions still actionable today (these are the things this checklist is reasoning over even before impl):**

- The merged spec at `1edfd9e` locks the 8-problem flow, picture-discrimination input model, adaptive 2-tier semantic distractor ramp with cutoff at 3, unified stardust/streak with Math, no red X, 2-wrong hint, 3-wrong guided completion, Path A audio bundle (~40 utterances ~600 KB), per-letter phoneme MP3s via Howler, unified HUD.
- The merged picture-pack at `55c588a` locks 14 target words + 8 distractor-only pictures + 5 forbidden silhouette pairs + the master distractor pairing matrix.
- The merged research memo at `938b926` confirms short-a CVC as the right v1 surface, 8-problems-per-session as the right session ceiling for working-memory, and picture-pairing as a vocabulary scaffold for L2 (with the caveat that the picture must not be a decoding shortcut — locked in spec via the picture appearing _alongside_ the word card, not as a substitute for the letters).
- Existing `qa/math-screen.md` AC matrix rows 3, 6, 7 (stardust + streak + persistence) are the regression surface Word Song imports. Devon's shared-module refactor (drift item E) will move the assertions; the contract is unchanged.
- `src/screens/WordSong/` does not exist yet. `git ls-files src/screens/` returns: `Greet.test.tsx, Greet.tsx, Math/, Splash.test.tsx, Splash.tsx, greetSequence.test.ts, greetSequence.ts, splashTiming.test.ts, splashTiming.ts`.
- `public/assets/audio/phonemes/` does not exist yet. `public/assets/pictures/` does not exist yet. Both blocked on asset-pipeline decisions per drift items C, D.

**Backend smoke (run on every Word Song impl PR — same canary as Math):**

```
$ curl -s -X OPTIONS -i https://marian-learning.vercel.app/api/claude --max-time 15
Expected: HTTP/1.1 204 No Content
          Access-Control-Allow-Headers: Content-Type
          Access-Control-Allow-Methods: POST, OPTIONS
          Access-Control-Max-Age: 86400

$ curl -s -X POST -H "Content-Type: application/json" \
    -d '{"kind":"session-start","payload":{"plan":{"id":"qa-smoke-wordsong","label":"WordSong QA smoke","utterances":[{"id":"qa.smoke.1","text":"Tap the cat."},{"id":"qa.smoke.2","text":"Yes! Cat."}]}}}' \
    https://marian-learning.vercel.app/api/claude --max-time 60
Expected (post Azure-creds-fix): HTTP 200 with utterances array, base64 MP3 inline.
Currently expected (pre Azure-creds-fix): HTTP 502 {"error":"tts-failed","message":"tts pipeline failed: fetch failed"} — same blocker as Math; NOT a Word Song regression.
```

If POST returns 502 with `tts-failed`: same server-side Azure-creds blocker as documented in `qa/math-screen.md` lines 113–142. Word Song degrades to silent-but-captioned via the same fallback Math uses; not a regression.

---

## AC matrix

One row per acceptance-criteria bullet from the spec's "Acceptance criteria (Jessica)" section (lines 588–650 of `design/screen-4-word-song.md`), consolidated by topic. Plus regression rows derived from cross-cutting concerns (Greet untouched, Math untouched _unless_ shared-module refactor is in scope, Path A endpoint healthy, lint rule still firing, asset-fallback rendering, picture-pack acceptance criteria from PR #64).

| #   | Acceptance criterion (source)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Owner              | How verified (projected — pre-impl)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|--------------------|------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| 1   | Word Song screen renders 8 problems sequentially from the chosen plan; each shows word card with picture (180pt) above letters (96pt, ~32pt apart) and an optional speaker icon (56pt); 3 picture chips at 96×96pt with 24pt gaps in the bottom thumb zone (spec §Visual layout + §Acceptance criteria → Functional bullets 1–3, lines 592–594).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Auto + Desktop** | **Auto (projected):** `WordSong.test.tsx` `it('renders the first problem on mount with HUD, melody, word card, and 3 picture chips')` — assert `data-testid="word-card"`, `data-testid="word-card-picture"`, three `data-testid="word-card-letter"` glyphs, three `data-testid="word-chip"` picture-chip elements. `it('completes the session on problem 8 …')` walks all 8 problems and asserts `onSessionComplete` fires exactly once on problem 8. **Desktop:** in `yarn dev`, mount Word Song (Session 2+ entry path), confirm word card renders with picture above letters, 3 chips at the bottom. Picture-chip rendering BLOCKED-on-pictures (drift item C); inline-SVG fallback per spec is the v1 contract surface. **Note:** spec §Why this differs from Session-1's Word Song (drift item N) — Session 1 entry routes to a different listen-only `dog` surface; this row asserts the 8-problem Session 2+ entry only.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 2   | Distractor rule per spec: problems 1–3 use gentle tier (clearly different category + different consonant + different vowel sound); problems 4–8 use trap tier (share at least one of: category, starting consonant, vowel, ending consonant). Always 3 picture chips total: 1 correct + 2 distractors. Correct chip position randomised per problem. Cutoff locked at 3 in `wordDistractors.ts`'s `pickTier(problemIndex)` (spec §Distractor policy + spec §Implementation pointers lines 695–701 + spec §Acceptance criteria → Functional bullets 4–6, lines 595–597).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Auto**           | **Auto (projected):** `wordDistractors.test.ts` exhaustive sweep — every target word ∈ {14 v1 targets} × every `problemIndex ∈ [1,8]` — assert tier matches `pickTier`, distractor pair from picture-pack matrix matches `pickDistractors(target, problemIndex, pool)`, 3 chips total, all distinct, target ∈ chip set, correct position in `[0,1,2]`. Plus `pickTier` cutoff canary asserts `pickTier(3) === 'gentle'` AND `pickTier(4) === 'trap'` (locks `GENTLE_RAMP_THROUGH === 3` mirror of Math). Plus `FORBIDDEN_PAIRS` assertion: no shipped problem trio violates the 5 forbidden silhouette pairs (`cat↔dog`, `bus↔van`, `pan↔pot`, `cap↔hat`, `man↔dad`) per picture-pack PR #64. **Test-needed gap:** `cvcShortaPack.test.ts` data-integrity test — every `WordEntry` in the pack has a non-empty `picture`, a single-CVC-short-a `word`, and the `vowel` field is `'a'`. ~10 minutes. **Flag to Devon.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 3   | Streak `[3, 5, 8]` thresholds trigger sparkle bonus stardust on clean run, **unified across Math + Word Song within a single session** (spec §Stardust treatment "Cross-screen accumulation" lines 206–212 + §Streak treatment lines 240–249 + §Acceptance criteria → Functional bullet 9–10, lines 599–602).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Auto + Desktop** | **Auto (projected):** `WordSong.test.tsx` `it('streak threshold [3, 5, 8] grants a bonus stardust at problem 3')` asserts `data-total='4'` after 3 clean correct picture-chip taps. `it('completes a clean Word Song run with 11 stardust')` asserts 8 + 3 bonuses = 11 (same ceiling as Math). Constant `STREAK_BONUS_THRESHOLDS = [3, 5, 8]` imported from shared module per drift item E refactor — single source of truth. **Auto (cross-screen unification, projected):** `WordSong.test.tsx` accepts an injected `initialStreak: number` prop (or reads from a shared streak ref via the orchestrator parent — drift item F decides the architecture); test mounts with `initialStreak=2`, taps correct picture chip, asserts `data-streak='3'` AND bonus stardust grant. **Desktop:** observe HUD pop animation on problem-3 clean run; sparkle indicator visible from streak ≥ 2 onward. **Cross-screen:** mount Math 2/2 in a row, then Word Song, tap correct first chip, assert HUD streak is now 3 and bonus stardust grants. Deferred to Thomas's iPad pass for visual binding verdict.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 4   | Hint utterance fires after **2** wrong attempts on the same problem (NOT 1) — locked at the same threshold as Math per Hattie & Timperley (2007) feedback-timing argument, cited by Dave's Math memo (spec §Wrong-answer policy "After 2 wrong attempts" lines 277–298 + §Acceptance criteria → Audio bullet 7, line 612).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Auto + Desktop** | **Auto (projected):** `WordSong.test.tsx` `it('hint utterance fires after 2 wrong attempts on the same problem')` taps two wrong picture chips, advances 700ms past the 600ms `HINT_DELAY_AFTER_WRONG_MS` beat, asserts the hint line `"Let's look. Cat."` (or whatever the target word is) is in `harness.spoken()`. Threshold constant `HINT_AFTER_WRONG_COUNT = 2` mirror of Math. **Desktop:** observe the hint flow live in `yarn dev`; the letter-by-letter sound-out + picture bob choreography is asserted at AC row 12 separately (more specific). This row only asserts the 2-wrong threshold + the hint-utterance-played fact.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 5   | Picture chips render at native pixel density on iPad (vector or 2x raster); no blurring at 96pt chip size. Word card picture at 180pt. **Both BLOCKED-on-pictures until PR #64 picture pack lands; inline-SVG placeholder per spec is the v1 contract surface.** All 14 target words have a target-picture rendering; all 8 distractor-only words have a distractor-picture rendering. (Spec §Acceptance criteria → iPad PWA bullet 4, line 644 + picture-pack PR #64 §Acceptance criteria → Style consistency bullets, lines 461–469.)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Desktop + iPad** | **Auto (projected):** `cvcShortaPack.test.ts` `it('exposes 14 target words and 8 distractor-only words')` asserts the pack data-shape integrity (target count = 14, distractor count = 8, no duplicates, every word has a `picture` field). Plus `WordSong.test.tsx` `it('renders a placeholder picture component for every chip until canonical assets land')` asserts the `<PicturePlaceholder>` component (or equivalent inline-SVG fallback) mounts for every chip. **Desktop:** in `yarn dev`, mount Word Song, eyeball the 14 target words across 2 sessions (with deterministic plan rotation) — every chip renders a picture (placeholder or canonical), no broken-image icons, no missing-asset 404s. **BLOCKED-on-pictures** for the canonical asset verdict; once Matt's pipeline ships the 22 SVGs, repeat the desktop walkthrough and confirm no style fragmentation. **iPad:** Thomas owns the binding pixel-density verdict; placeholder renders are sufficient for v1 contract surface.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 6   | Distractors satisfy the constraint set: from Marian's known vocabulary (per picture pack), visually distinct silhouettes at 96pt, distinct from each other, picture style consistent across the trio. **Forbidden-pair rule encoded** in `wordDistractors.ts` per picture-pack PR #64: `cat↔dog`, `bus↔van`, `pan↔pot`, `cap↔hat`, `man↔dad` never appear in the same trio (spec §Distractor policy "Constraint set" lines 173–179 + picture-pack PR #64 §Forbidden pairings + §Acceptance criteria → Functional bullet 6, line 597 + picture-pack §Acceptance criteria → Implementation handoff bullets, lines 472–476).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Auto**           | **Auto (projected):** `wordDistractors.test.ts` `it('never picks a forbidden pair as a distractor')` enumerates every (target × problemIndex) and asserts the picked distractor pair contains no forbidden-pair entry (target+distractor or distractor+distractor). `FORBIDDEN_PAIRS` constant in `wordDistractors.ts` matches the 5 pairs verbatim. **Test-needed gap:** if Devon hardcodes the picture-pack matrix (rather than computing distractors from semantic-overlap rules), the test is a data-integrity check. If Devon computes distractors algorithmically, the test must also assert that the algorithm never yields a forbidden pair under the 8-problem rotation. ~30 minutes. **Flag to Devon.** **Visual constraint** (silhouette-distinctness, illustration-style consistency) is **NOT auto-testable** in v1 — Kyle reviews each shipped trio against the §Distractor policy constraint #2 "no two SVG silhouettes may share a primary mass-and-pose silhouette as visually rendered at 96pt" eyeball test. Curation gate, not automated. Deferred to Kyle's pre-ship review of any new picture additions.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 7   | HUD: stardust counter visible (top-left), 8 problem dots visible (center), streak indicator hidden until streak ≥ 2 (top-right) — **same component as Math**. Per spec §Implementation pointers, this requires the shared-module refactor (drift item E) — `SessionHud.tsx` in `src/screens/_shared/` (spec §Visual layout HUD strip line 147 + §Implementation pointers "Stardust + HUD logic" lines 670–677 + §Acceptance criteria → Functional bullet 7, line 598).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Auto**           | **Auto (projected):** `SessionHud.test.tsx` (new file in `src/screens/_shared/`) covers the 3 HUD elements (stardust + dots + streak) — extracted from Math's existing `Math.test.tsx` HUD assertions per the refactor in drift item E. Plus `WordSong.test.tsx` asserts the SessionHud is mounted on Word Song with `data-testid="session-hud"` and the 8 dots are present. **Test-needed gap:** if Devon does NOT refactor (drift item E split out), the duplication is visible — the HUD visually identical between Math and Word Song but two separate component code paths. Anti-pattern per spec line 676 — flag to Matt as a follow-up. ~refactor work, not in-scope here.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 8   | Stardust persists in localStorage at key `marian-tutor.stardust.v1` with `schemaVersion: 1` — **shared with Math** (same key, same schema, same iOS-quota defensive posture). +1 per correct first-attempt; no stardust on retry-eventually-correct or guided completion. Streak resets to 0 on any wrong tap; streak indicator fades out over 400ms when breaking from ≥2 (spec §Stardust treatment "Persistence" lines 228–230 + §Streak treatment + §Acceptance criteria → Functional bullets 8, 11–12, lines 598–602).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Auto + Desktop** | **Auto (projected):** `WordSong.test.tsx` `it('persists stardust at marian-tutor.stardust.v1 with schemaVersion 1 after correct tap')` taps correct picture chip, then asserts `JSON.parse(stored).total === 1` AND `stored.schemaVersion === 1` from the injected memory storage adapter. Plus `it('does NOT increment stardust on retry-eventually-correct')` taps wrong chip then correct chip, asserts `data-stardust='1'` (1 stardust on a correct-after-1-wrong, per spec §States "Happy path (correct after 1 or 2 wrong attempts)" line 500: "still award stardust"). **Wait — re-read spec:** spec line 500 says **stardust IS awarded** on correct-after-1-or-2-wrong; the only stardust withhold is on **3rd-strike guided completion** (drift item J row J's spec line 307). **Action:** AC row 8's third sub-clause is incorrect as worded; the correct contract is "no stardust on **guided completion** (3rd-strike), not on retry-eventually-correct." Update the AC text in this row before running. **Drift surface:** the spec's Acceptance Criteria bullet at line 600 says "no stardust on retry-eventually-correct or guided completion" but that contradicts the spec body at line 500. Flag to Kyle as drift item O. **Default for impl until reconciled:** match spec body (stardust IS awarded on correct-after-1-or-2-wrong, withhold ONLY on guided 3rd-strike); this is the same rule as Math per `qa/math-screen.md` AC row 5. **Desktop:** in `yarn dev`, walk through 1 correct tap, open `Application → Local Storage → marian-tutor.stardust.v1`, confirm shape `{"total":1,"lastUpdatedAt":"…","schemaVersion":1}`. Hard-reload page, mount Word Song again, confirm counter shows 1. |
| 9   | After 3 wrong attempts on the same problem, guided completion fires: `giveAnswer` utterance plays, correct picture chip shimmers (`box-shadow` glow at `--my-rose`, 800ms loop), all other chips dim to opacity 0.6, correct chip is the only tappable, **NO stardust awarded, streak does NOT increment, `totalCorrect` does NOT bump** (spec §Wrong-answer policy "After 3 wrong attempts — Guided completion" lines 301–309 + §Acceptance criteria → Audio bullet 8, line 614).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Auto + Desktop** | **Auto (projected):** `WordSong.test.tsx` `it('after 3 wrong attempts, guided completion shimmers correct chip and dims others')` taps 3 wrong picture chips, asserts `data-guided='true'`, correct chip enabled, others disabled at opacity 0.6, give-answer utterance `"This one is cat."` (or actual target) spoken. **Stardust + streak + totalCorrect withhold:** explicit `it('no stardust granted on guided completion correct tap')` taps correct picture chip after the guided sequence, asserts `data-stardust` unchanged AND `data-streak === '0'` AND the eventual `onSessionComplete` callback's `totalCorrect` does NOT include this problem. ~25 minutes. Same rule as Math AC row 5. **Test-needed gap (drift item J):** the explicit "guided correct tap → no stardust + no streak + no totalCorrect bump" assertion. **Desktop:** observe live; deferred per row 4 reasoning.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 10  | Wrong-answer policy: distractor stays available, no red colour anywhere, no "X" glyph or "wrong" text, no wrong-answer counter displayed, no streak-related copy in TTS or captions, no "you lost your streak" SFX/animation/TTS when streak breaks. Streak-break visual contract: opacity fade to 0 over 400ms, no SFX, no TTS, no copy (spec §Wrong-answer policy + §Acceptance criteria → Anti-dark-pattern bullets 1–5, lines 620–624).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Auto + Desktop** | **Auto (projected):** `WordSong.test.tsx` `it('does not display any "X" or "wrong" copy on a wrong tap (anti-dark-pattern)')` taps a wrong picture chip and asserts `screen.getByTestId('word-song').textContent.toLowerCase()` does NOT contain `'wrong'` or `'incorrect'` or `'x'` (as a standalone word). Manual `grep -rni "text-red\|bg-red"` in `src/screens/WordSong/` returns zero matches — no red colour anywhere. Wrong chip's animate state is the soft horizontal shake `[0, -6, 6, -4, 4, 0]` over 400ms with **no colour change** per spec §Wrong-answer policy line 259. **Auto (streak-break fade-out contract, projected):** mirror Math's `setStreakFadingOut` test — build a clean streak of 2 across Math+Word Song, tap a wrong chip on Word Song, assert `data-fading-out='true'` for ~400ms then unmounts. **Desktop:** in `yarn dev`, build a 2-streak, tap wrong picture chip, visually confirm streak indicator fades quietly with no shake/flash/red/audio cue. Deferred to Thomas's pass for binding visual verdict.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 11  | Optional speaker icon on the word card (56pt) replays `word.p{N}.read` synchronously inside the tap handler. **Default: ship in v1** per spec §Visual layout + §Implementation pointers "Optional v1 trim". If Devon trims, this AC row becomes DEFERRED-v2 and only per-letter phoneme taps remain as in-screen replay surface (spec §Audio integration contract → "Audio dispatch sequence on speaker icon tap" lines 416–422 + §Acceptance criteria → Audio bullet 6, line 611).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Auto + Desktop** | **Auto (projected, if shipped):** `WordSong.test.tsx` `it('speaker icon tap replays read-aloud utterance')` mounts Word Song, finds `data-testid="word-card-speaker"`, taps it, asserts `harness.spoken()` includes `word.p1.read` a second time (after the screen-entry first-play). If speaker icon is trimmed: this row becomes "PASS-DEFERRED-v2 / drift item I logged for Matt". **Desktop:** in `yarn dev`, tap speaker icon mid-session, audibly verify the line replays. **BLOCKED-on-Azure-config** for the audible verdict; the synchronous-tap-handler dispatch is auto-testable without real audio.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 12  | Hint state choreography per spec §Wrong-answer policy "Hint choreography for Word Song" lines 282–293: `t=0ms` Melody puzzled-tilt + "Let's look." TTS; `t=600/1200/1800ms` each letter scales 1→1.2→1, color shifts ink→rose, plays its phoneme; `t=2400ms` all letters scale together + picture bobs + Melody says full word; `t=3300ms` Melody returns to idle, chips remain tappable. **Each letter highlight is triggered off the corresponding phoneme MP3's `play` event** (spec §Acceptance criteria → Audio bullet 8, line 613) — visual tightly coupled to audio, NOT a TTS boundary.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Auto + Desktop** | **Auto (projected):** `WordSong.test.tsx` `it('hint state plays letter-by-letter sound-out with picture bob')` taps two wrong picture chips, advances timer through the hint sequence, asserts the spoken set includes `word.p1.hint` AND each phoneme `phoneme-c.mp3 / phoneme-a-short.mp3 / phoneme-t.mp3` (for target `cat`) is invoked via the `playPhoneme` mock. Asserts the visual state machine: `data-hint-state='intro' → 'phoneme-c' → 'phoneme-a' → 'phoneme-t' → 'blend' → 'idle'`. ~35 minutes. **Test-needed gap:** the visual-tightly-coupled-to-audio assertion (drift item A) — that each letter's `data-highlight='true'` toggle fires _on the phoneme `Howl` `onplay` event_, not a TTS boundary. Inject a fake `playPhoneme` that resolves on a tick and assert the visual highlight fires within that tick. ~15 minutes. **Desktop:** observe the hint flow live in `yarn dev` once phoneme assets land — letters sound out one at a time, picture bobs at blend, Melody says the full word. **BLOCKED-on-phonemes** for the audible verdict.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 13  | Letter tap on word card triggers the corresponding `phoneme-{letter}.mp3` synchronously inside the tap handler — **NOT Web Speech API output**. Letter scales 1→1.2→1, color shifts ink→rose, returns to idle. Letter taps are independent of the answer-chip task — no streak break, no attempt count, no Melody reaction (spec §Audio integration contract → "Audio dispatch sequence on letter tap" lines 405–414 + §Acceptance criteria → Audio bullets 5, 11, lines 610, 616).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Auto + iPad**    | **Auto (projected):** `WordSong.test.tsx` `it('letter tap plays phoneme via Howler, not Web Speech')` finds `data-testid="word-card-letter-c"`, taps it, asserts the injected `playPhoneme('c')` mock fires AND the injected `playUtterance` (TTS) mock does NOT fire AND the `streak` ref / `wrongCount` ref are unchanged. Plus negative-space grep: `grep -rni "speechSynthesis\|window\.speech" src/screens/WordSong/` returns zero matches; `grep -rni "from.*lib/tts" src/screens/WordSong/` returns zero matches (mirror of Math's grep at `qa/math-screen.md` lines 244–251). `phonemeAudio.test.ts` covers the lazy-init `Howl` cache pattern (`PHONEMES` map populated on first request, reused thereafter). **iPad:** Thomas owns the audible-after-wake-tap verdict. **BLOCKED-on-phonemes** for the audible portion (drift item D); the synchronous-handler-dispatch contract is auto-testable now.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 14  | All TTS routed through `sessionAudio.playUtterance`, never `lib/tts.speak()`. All phoneme audio routed through Howler, never Web Speech. (Spec §Acceptance criteria → Audio bullets 9–10, lines 615–616.)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Auto**           | **Auto (projected):** Mirror Math's `qa/math-screen.md` AC row 10 grep + bundle inspection. `grep -rni "speechSynthesis" src/screens/WordSong/` returns zero matches. `grep -rni "from.*lib/tts" src/screens/WordSong/` returns zero matches. Production bundle inspection (post-impl): WordSong's bundle slice contains zero `speechSynthesis` literals. The Greet + Splash legitimate `speechSynthesis` references survive (Plan B consumers from PR #25 era) — they're _not_ a Word Song regression. **Test-needed gap:** add a `tests/qa/wordSongAudioPolicyCanary.test.ts` that asserts the WordSong source tree never imports from `src/lib/tts` and never references `speechSynthesis` — a one-time guard against regression. ~10 minutes. **Flag to Devon.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 15  | Path A audio integration: per-problem read-aloud (`word.p{N}.read`) plays on screen entry after problem reveal stagger completes (~1.4s); caption ribbon mirrors TTS word-by-word via Path A `onWordTick`; correct chip tap triggers `word.p{N}.correct` synchronously inside the tap handler; wrong chip tap triggers `word.p{N}.reprompt` synchronously (spec §Audio integration contract + §Acceptance criteria → Audio bullets 1–4, lines 606–609).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Auto + iPad**    | **Auto (projected):** `WordSong.test.tsx`'s `makePlayHarness` synchronously fires `playOpts.onPlay()` and calls `playOpts.onWordTick(i)` for each word; caption ribbon's per-word reveal is asserted via `data-revealed` toggles. `it('correct chip tap triggers word.p{N}.correct synchronously')` and the parallel wrong-tap test are direct mirrors of Math's `Math.test.tsx` pattern. **Live Path A wiring (projected):** Devon's impl PR will mirror PR #54's pattern — `App.tsx` injects `prepareWordSongPathA(plan, plan.id)` on `route === 'word-song'` (or whatever route Devon picks), with `AbortController` cleanup on route-leave. **BLOCKED-on-Azure-config** for live audible playback — same compound blocker as Math. The silent-but-captioned default fallback is the documented graceful degradation; no white-screen, no error chime, no nag copy. **iPad:** Thomas owns first-utterance audible playback after wake-tap on installed PWA.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 16  | First chip-tap unlocks audio context via `useAudioUnlockGate` (1.5s watchdog); Word Song is now its third consumer screen alongside Greet + Math (spec §Implementation pointers "Reuse, do not re-derive" line 680 + spec §Acceptance criteria → iPad PWA bullet 1, line 641).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Auto + iPad**    | **Auto (projected):** `WordSong.test.tsx` instantiates `useAudioUnlockGate({ watchdogMs: 1500 })`, the very first chip-tap result `speak()` call is wrapped via `gate.wrapSpeak(() => …)`, then `audioUnlocked = true` so subsequent taps skip the gate. Direct mirror of Math's pattern at `Math.tsx:278, 702-716`. The `useAudioUnlockGate.test.tsx` 20-test suite covers the watchdog contract. **iPad:** Thomas owns the first-tap-on-installed-PWA verdict — that's the only environment where iPad Safari's gating actually fires; jsdom can't reproduce it. Same gating as Math AC row 11.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 17  | Session-end callback `onSessionComplete(result)` fires exactly once on problem 8. Implementation shape per drift item J: **5 fields** `{ totalCorrect, totalStardust, finalStreak, earnedThisSession, surface: 'word-song' }` (spec §Transition out line 540 lists 4 fields including `surface`; impl should add `earnedThisSession` for parity with Math; spec needs Kyle one-line edit). Auto-advance after a correct tap fires at 1.2s (`ADVANCE_AFTER_CORRECT_MS = 1200`) — next problem mounts via cross-stagger fade (spec §Audio dispatch sequence on chip tap (correct) line 388 + §Acceptance criteria → Functional bullet 1 line 593).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Desktop + iPad** | **Auto (projected):** `WordSong.test.tsx` asserts `onSessionComplete.mock.calls[0][0]` shape `{ totalCorrect: 8, totalStardust: 11, finalStreak: 8, earnedThisSession: 11, surface: 'word-song' }` for clean 8-for-8 run. Callback signature exported via `WordSongSessionResult` interface. **Auto (auto-advance):** `it('auto-advances 1200ms after correct chip tap')` advances `vi.advanceTimersByTime(1200)` and asserts `data-problem-index='1'`. **Desktop:** confirm next-screen wiring in `App.tsx` — at the time of impl, `App.tsx` either passes `onSessionComplete` (consuming session-end recap surface ticket `86c9grnjd`) or leaves it optional. **iPad:** binding visual verdict — Thomas owns. Deferred to device pass.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |

---

## Survival / regression rows

| #      | Survival check                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Owner             | How verified (projected — pre-impl)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| ------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| **18** | **Inline-SVG / placeholder render — picture pack still on word-song-picture-pack.md.** Devon's impl ships placeholder picture components (parallel to Math's `SparkleGlyph`/`FlowerGroup` inline fallbacks) so the screen reads even before Matt's pipeline ships the 22 SVGs.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Desktop**       | **Auto-ish (projected):** `WordSong.tsx` defines a `PicturePlaceholder` component (coloured square + the target word's first letter glyph, or a generic icon — Devon picks). Component tree mounts under `WordSong.test.tsx`'s render — no missing-component errors. **Desktop visual smoke:** in `yarn dev`, mount Word Song, visually confirm 1 word card picture + 3 chip pictures render reasonably (colour squares with letter overlay, or whatever Devon picks). **Test-needed gap (Math parallel):** screenshot regression test (Playwright + visual diff) is overkill for v1; defer until 3+ visual regressions accumulate. **BLOCKED-on-pictures** for the canonical asset render verdict.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| **19** | **Greet + Math still work post-Word-Song-impl (regression check).** Devon's PR must NOT regress Greet's wake-tap → 4-line MP3 sequence → heart CTA flow, AND must NOT regress Math's screen contract.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Auto**          | **Auto (projected):** `Greet.test.tsx`, `greetSequence.test.ts`, `useAudioUnlockGate.test.tsx`, `preRecorded.test.ts`, `tests/qa/audioAssetIntegrity.test.ts` all stay green. PR diff spot-check: zero `src/screens/Greet*` or `src/lib/audio/*` source diffs (`gh pr view <N> --json files` confirms Word Song files are all under `src/screens/WordSong/` + the shared-module refactor under `src/screens/_shared/`). **Math regression — drift item E refactor surface:** `Math.test.tsx`, `distractors.test.ts`, `stardust.test.ts` all stay green AFTER the shared-module refactor moves `stardust.ts`/`MathHud.tsx` to `_shared/`. `App.test.tsx` route wiring stays green for both Math and Word Song. **Desktop:** dev-server smoke `GET /src/screens/Greet.tsx` and `GET /src/screens/Math/Math.tsx` both serve cleanly. **Iterated full Greet flow:** deferred to Thomas's iPad pass — `qa/greet-regression.md` is the binding checklist for that. **Iterated full Math flow:** deferred to Thomas's iPad pass — `qa/math-screen.md` is the binding checklist for that. If Word Song's PR does the shared-module refactor, run BOTH `qa/greet-regression.md` and `qa/math-screen.md` AC rows 3, 6, 7 (stardust + streak + persistence) at the end of the Word Song checklist as a guard against the refactor breaking Math's contract surface. |
| **20** | **Path A endpoint still smoke-green post-merge.** `/api/claude` reachable, CORS headers correct, no `FUNCTION_INVOCATION_FAILED` regression. POST with a Word-Song-shaped session-start payload (4 utterances per problem × 8 problems = 32–40 utterances) returns either HTTP 200 with utterances+base64 (post Azure-creds-fix) or HTTP 502 `tts-failed` (current state).                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Manual / curl** | **Live:** `curl -s -X OPTIONS https://marian-learning.vercel.app/api/claude` → HTTP 204 with canonical CORS headers. Devon's PR adds zero `api/` files; cannot regress the OPTIONS surface. **POST smoke (post Azure-creds-fix expected behaviour):** the function should accept a 32–40-utterance plan; the response envelope already supports arbitrary `utterances[]` arrays per Math's session-start. **POST smoke (current state):** HTTP 502 `tts-failed` per `qa/math-screen.md` lines 113–142 — same blocker, NOT a Word Song regression. **Note for orchestrator:** if the Azure creds get fixed before Devon's PR opens, this row becomes a real test surface; otherwise it stays at "OPTIONS green / POST blocked-on-Azure". POST not run from QA today to avoid spending an Anthropic API call on prod.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| **21** | **PR #39's `marian-api/require-js-extension` lint rule still catches bare imports under `api/`.** Devon's PR doesn't touch `api/`, but the rule must still load + fire correctly across the rest of the repo.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Auto**          | **Auto (projected):** `eslint-rules/require-js-extension.test.ts` 29/29 PASS via Vitest's `RuleTester` integration. `yarn lint` clean across the whole repo. Rule registered as `marian-api/require-js-extension: 'error'` in `eslint.config.js:79` for `api/**/*.ts` files only. **Test-needed:** none — the rule's own unit tests + the repo-wide `yarn lint` pass are sufficient.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |

---

## Survival checks (lens 4 — extended)

These are the Marian-realistic stress tests beyond the AC matrix. Mirror Math's lens-4 pattern with Word-Song-specific stresses called out in the task brief.

| Check                                                                                                                | How                                                                                                                                                                                                                                                                                                              | Expected                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| -------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Rage-tap correct picture chip 5× rapidly**                                                                         | Real iPad: tap correct picture chip 5 times in <500ms.                                                                                                                                                                                                                                                            | Only one stardust grant. Only one auto-advance scheduled. `problemState.resolved` short-circuits subsequent taps (mirror of Math's `Math.tsx:682` pattern). Chip becomes `disabled` after first correct tap. **Test-needed:** `WordSong.test.tsx` 5-rapid-click assertion fires 5 events synchronously and asserts only one stardust grant + one auto-advance scheduled. ~15 minutes. **Will benefit from the rage-tap fix Devon's working on in parallel** per task brief — if Devon's impl PR includes that fix, this survival row passes immediately; if Devon's PR ships without it, flag back to Matt as a P0 follow-up (rage-tap is a non-negotiable for an 8yo).                                                                                                                                |
| **Rage-tap mid-shake (wrong picture chip mid-shake animation)**                                                      | Tap wrong picture chip; while it's still shaking (≤400ms), tap another wrong picture chip.                                                                                                                                                                                                                       | Both register as wrongs; `wrongCount` increments to 2; hint timer fires correctly. Auto coverage at AC row 4 covers the 2-wrong sequence. Mid-shake DOM doesn't gate the click — chip stays tappable. Verified via implementation reading once Devon's PR lands.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| **Rage-tap letter on word card 10× rapidly**                                                                         | Tap a letter on the word card 10 times in <1s.                                                                                                                                                                                                                                                                  | Phoneme MP3 plays once per tap (Howler default behaviour: each `play()` call starts a new playback even if a prior is still ringing). Ten phoneme calls _may_ overlap audibly (acceptable per spec — letter taps are exploration). `wrongCount` does NOT increment, `streak` does NOT break, `Melody` does NOT react. Per drift item M, letter-tap independence is locked v1 default. **Test-needed:** `WordSong.test.tsx` 10-rapid-letter-tap assertion fires 10 events synchronously and asserts: (a) `playPhoneme(letter)` mock called 10 times; (b) `streakRef.current` unchanged; (c) `wrongCountRef.current` unchanged; (d) `playUtterance` mock not called. ~15 minutes. **Flag to Devon.**                                                                                                      |
| **Background / resume mid-Word-Song-sequence (iPad PWA backgrounded for 5+ min during a session)**                   | iPad PWA into a Word Song session at problem 4, swipe to home, wait 5+ min, return.                                                                                                                                                                                                                              | Audio context will be suspended (iPadOS behaviour). Next chip tap should re-engage `useAudioUnlockGate` (Word Song's `audioUnlocked = true` state survives the suspend, but the underlying Howler audio context may not — needs device verification). **Phase 5 (PR #59 ctx-resume + PR #60 OS-level audio-session unlock) just landed for this** per task brief — the relock surface should be tighter than pre-Phase-5 era. **DEFERRED-iPad** to Thomas; cross-screen integration not unit-testable. Auto coverage in `useAudioUnlockGate.test.tsx`'s relock path is the only signal today. If the resume fails, flag to Devon — could be a phoneme-cache survival edge (the lazy-init `Howl` instances may need re-initialisation after audio-context suspend, not just resume).                    |
| **Long-idle audio decay (10+ min idle on Word Song screen mid-session, no taps)**                                    | Mount Word Song, do not tap anything for 10+ min, then tap a chip.                                                                                                                                                                                                                                              | iPadOS Safari may suspend the audio context after several minutes of no-gesture-no-audio. Next chip tap should re-engage the gate. **Phase 5 territory** per task brief. Same DEFERRED-iPad as above. **Test-needed:** if Devon's impl exposes a "idle-timeout" hook for the gate, add a unit test that fakes the long-idle and asserts re-gate on next interaction. Otherwise, manual iPad observation only.                                                                                                                                                                                                                                                                                                                                                                                          |
| **Wrong picture on stumble (3rd wrong) — does the hint behave per spec?**                                            | Tap 3 wrong picture chips on the same problem.                                                                                                                                                                                                                                                                  | After the 2nd wrong: hint state fires (AC row 12). After the 3rd wrong: guided completion fires (AC row 9) — correct chip shimmers, others dim, give-answer TTS plays, only correct is tappable. Tapping correct: standard happy-path animation but no stardust + no streak + no `totalCorrect` bump. **Test-needed:** sequenced state-machine test in `WordSong.test.tsx` covering all 4 transitions (idle → wrong-1 → wrong-2-→hint → wrong-3-→guided → correct-→advance). ~30 minutes. **Flag to Devon.**                                                                                                                                                                                                                                                                                          |
| **Picture vs distractor exact-match (no false positives at the picture identity layer)**                             | For every (target × problemIndex) combination across the 14 target words and 8 distractors, verify that the "correct chip" is identified by picture identity (e.g. the `cat` picture), NOT by chip position or label.                                                                                            | The internal answer-checking logic must compare picture-identity (e.g. `chip.pictureId === target.pictureId`), not chip label or chip position. False-positive risk: if a distractor chip happens to render the same picture as the target (e.g. a bug in `wordDistractors.ts` returns the target as a "distractor"), Marian would tap it and it'd be marked WRONG even though the picture matches. **Test-needed:** `wordDistractors.test.ts` `it('never returns the target word as one of its own distractors')` enumerates every (target × problemIndex) and asserts `target.word ∉ distractors.map(d => d.word)` AND `target.pictureId ∉ distractors.map(d => d.pictureId)`. ~15 minutes. **Flag to Devon.** This is the closest thing to a picture-identity-layer regression test we can write. |
| **Stardust JSON corruption / first-run empty state**                                                                 | Manually set `localStorage['marian-tutor.stardust.v1'] = '{garbage'` and reload Word Song.                                                                                                                                                                                                                       | `loadStardust` returns `emptyStardust()`. Counter shows `0`. Marian's accumulated stardust is lost (acknowledged trade-off — see Math's `stardust.ts:110` "Prefer 'she starts at 0 again' over crashing"). **Auto coverage** at `stardust.test.ts:47, :54, :61, :74` exhaustively covers all corruption paths. Word Song inherits this directly via the shared-module refactor (drift item E).                                                                                                                                                                                                                                                                                                                                                                                                          |
| **Plan rotation collision (two consecutive sessions in the same minute)**                                            | Mount Word Song twice, ~30s apart, on the same minute boundary.                                                                                                                                                                                                                                                 | Same plan returned both times (deterministic per minute, mirror of Math's `pickStaticSessionPlan`). Behaviour is intentional — `pickStaticWordSongSessionPlan(now)` should be per-minute-deterministic so tests can pin via `now`. **NOT A BUG** — Path A will replace this with a real Claude call when `ANTHROPIC_API_KEY` lands. **Test-needed:** if Devon ships a `wordSessionPlans.ts` parallel to Math's, add the same per-minute determinism + 8-problem-count + 14-target-word-pool invariants. ~30 minutes. **Flag to Devon.**                                                                                                                                                                                                                                                                |
| **Forbidden-pair regression (silhouette-similarity)**                                                                | After 8 sessions of rotation, audit the chip trios across all problems and assert no forbidden pair appears.                                                                                                                                                                                                    | `wordDistractors.ts` `FORBIDDEN_PAIRS` constant must catch all 5 pairs (`cat↔dog`, `bus↔van`, `pan↔pot`, `cap↔hat`, `man↔dad`). Auto coverage at AC row 6. **Test-needed:** `wordDistractors.test.ts` integration test rotates through all 14 target words at all 8 problemIndex values and asserts NO forbidden pair appears in any chip trio. ~20 minutes. Already in AC row 6's test-needed gap.                                                                                                                                                                                                                                                                                                                                                                                                    |

---

## Console / network sanity

- DevTools Console must show **zero** errors and zero warnings on a Word Song screen mount with the silent-default `playUtterance`. `console.warn` is intentionally fired by `createSfx` when an SFX file 404s (assets-todo follow-ups) — those are expected and silent in production but visible in dev. `WordSong.test.tsx` should mock `console.warn` accordingly to keep the unit-test log clean (mirror of Math's `Math.test.tsx:220` pattern).
- Network tab on Word Song mount: zero outbound requests beyond the initial Vite HMR (in dev) or the bundled JS+CSS (in prod). No `/api/claude` call until Path A wiring lands. Once Devon's PR wires Path A, expect exactly ONE POST to `/api/claude` on Word Song mount (the session-start call), with `{"kind":"session-start","payload":{"plan":{...}}}` body.
- `Application → Local Storage → marian-tutor.stardust.v1` populates with `{"total":N,"lastUpdatedAt":"...","schemaVersion":1}` after the first correct tap. Reload survives. **Same key as Math** — verify by tapping correct on Math, switching to Word Song, and confirming the counter still shows the Math-earned stardust. Cross-screen accumulation per spec §Stardust treatment "Cross-screen accumulation" lines 206–212.
- Service worker precache: Devon's PR will add the 14 phoneme MP3s + 22 picture SVGs to the precache once those assets land. Pre-asset-pipeline-impl: zero precache delta. Post-pipeline: ~14 MP3s @ ~4 KB each + ~22 SVGs @ <30 KB each = ~720 KB precache delta (within the iPad PWA budget; current baseline 516.28 KiB per Math AC row 14, post-Word-Song expected ~1.2 MB which is still well under typical PWA budgets).
- **`bg-song.svg` precache:** Devon's PR will add the Word Song background SVG to the precache. Already on Session-1 Screen 4's todo. Until it lands, Devon's impl ships an inline-SVG fallback (parallel to Math's `bg-garden.svg` fallback in `Math.tsx:1051-1129`).

---

## Word Song regression list (when adding to it on future PRs)

This section will grow as Word Song gets follow-on work. Today, the regression surface is:

| Risk surface                                              | What to re-run on PRs that touch it                                                                                                                                                                                                                                                                                          |
| --------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/screens/WordSong/wordDistractors.ts`                 | `wordDistractors.test.ts`. Plus AC rows 2, 6 above + the `pickTier` cutoff canary + `FORBIDDEN_PAIRS` integrity. Spec change (Dave revisits the cutoff or Kyle revisits the forbidden pairs): re-run AC rows 2, 6 + update Spec Drift items.                                                                                |
| `src/screens/WordSong/cvcShortaPack.ts` (data file)       | `cvcShortaPack.test.ts` (data integrity). Plus AC row 5. **Picture-pack PR** (when canonical assets ship and the data shape changes): re-run AC row 5 + verify forbidden-pair encoding still matches PR #64.                                                                                                                |
| `src/screens/WordSong/wordSequence.ts`                    | `wordSequence.test.ts`. Plus AC rows 4, 9, 12 (state machine: idle → wrong-1 → wrong-2-→hint → wrong-3-→guided).                                                                                                                                                                                                            |
| `src/screens/WordSong/phonemeAudio.ts`                    | `phonemeAudio.test.ts`. Plus AC rows 13 (letter tap) + 12 (hint sound-out). Phoneme inventory change (drift item B reconciliation): re-run AC row 12 + spec sync.                                                                                                                                                            |
| `src/screens/WordSong/WordSong.tsx`                       | `WordSong.test.tsx`. Plus AC rows 1, 3, 4, 5, 9, 10, 12, 15, 16, 17. Animation timing changes (e.g. `ADVANCE_AFTER_CORRECT_MS`) — re-run AC row 17 + flag to Thomas for taste.                                                                                                                                              |
| `src/screens/_shared/SessionHud.tsx` (post-refactor)      | `SessionHud.test.tsx`. Plus AC row 7 + Math regression list row "Math.tsx" (Math also consumes this).                                                                                                                                                                                                                       |
| `src/screens/_shared/stardust.ts` (post-refactor)         | `stardust.test.ts`. Plus AC row 8 + Math regression list row "stardust.ts".                                                                                                                                                                                                                                                  |
| `src/screens/_shared/streak.ts` (post-refactor)           | `streak.test.ts`. Plus AC rows 3, 8 + Math regression list row "Math.tsx" (streak surface).                                                                                                                                                                                                                                  |
| `public/assets/audio/phonemes/*.mp3`                      | `phonemeAudio.test.ts` if it asserts file existence. Plus manual smoke that the warn-once-on-404 pattern still holds for missing phoneme files. Asset audit: confirm filename matches the inventory in spec §Audio integration contract → "Phoneme audio" lines 338–353 (post drift-item-B reconciliation).                  |
| `public/assets/pictures/*.svg`                            | `cvcShortaPack.test.ts` if it asserts file existence. Plus manual smoke that picture chips render at native pixel density on iPad. Asset audit: confirm filename matches `picture-{word}.svg` convention per picture-pack PR #64 §Open Q #7.                                                                                |
| `src/lib/audio/useAudioUnlockGate.ts`                     | Word Song is now a consumer alongside Greet + Math. Re-run `useAudioUnlockGate.test.tsx` + AC row 16 + survival row 19.                                                                                                                                                                                                     |
| `src/lib/sfx/*`                                           | Word Song uses `createSfx` directly. Re-run `sfx.test.ts` (6 tests) + manual smoke that the warn-once-on-404 pattern still holds.                                                                                                                                                                                            |
| `src/App.tsx`                                             | Word Song route wiring. Re-run `App.test.tsx` + manual smoke that Math → Word Song transition (mixed-session) works AND splash → Word Song transition (Session 2+ standalone) works.                                                                                                                                       |
| `design/screen-4-word-song.md`                            | Spec drift surface. Any change → diff the spec + re-run all impacted AC rows. Keep this checklist's row text in sync.                                                                                                                                                                                                       |
| `design/word-song-picture-pack.md`                        | Picture-pack drift surface. Any change → diff the pack + re-run AC rows 2, 5, 6 + survival row "forbidden-pair regression".                                                                                                                                                                                                  |
| `design/research/phonics-sequence-marian.md`              | Research drift — Dave revisits findings. Spec section "Why this differs from Session-1's Word Song" + spec §Open Questions #1 are the load-bearing parts. Re-run nothing if Dave's findings don't change Word Song's CVC short-a v1 scope; re-run AC row 1 (problem count) + sequencer scope if findings expand v1.        |

---

## Test-needed gaps to log for Kevin / Devon (follow-up tickets)

Captured during checklist authoring; do NOT block anything in Devon's impl PR but should be in Matt's queue once the impl PR opens:

1. **`tests/qa/wordSongAudioPolicyCanary.test.ts`** — fail the build if any source under `src/screens/WordSong/` references `speechSynthesis` or imports from `src/lib/tts/`. Locks the spec §Acceptance criteria → Audio bullets 9–10 against accidental regression. ~10 minutes. **AC row 14.**
2. **`src/screens/WordSong/wordSessionPlans.test.ts`** — assert `pickStaticWordSongSessionPlan(now)` rotation determinism + every plan has exactly 8 problems + every problem's target ∈ the 14-word v1 pool + every problem's distractor pair conforms to the picture-pack matrix. Closes the regression surface around the hardcoded plans before Path A replaces them. ~30 minutes. **Word Song regression list row "wordSessionPlans".**
3. **Rage-tap regression test** in `WordSong.test.tsx` — fire 5 click events on the correct picture chip in synchronous succession; assert `data-stardust === '1'` and `onSessionComplete` not yet called (auto-advance scheduled exactly once). ~15 minutes. **Survival check "Rage-tap correct picture chip 5× rapidly".**
4. **Letter-tap rage-tap test** in `WordSong.test.tsx` — fire 10 click events on a letter on the word card; assert `playPhoneme` called 10 times AND `streakRef.current` AND `wrongCountRef.current` unchanged AND `playUtterance` not invoked. ~15 minutes. **Survival check "Rage-tap letter on word card 10× rapidly".**
5. **Stardust withhold on guided path explicit assertion** in `WordSong.test.tsx` — currently the guided test (projected at AC row 9) asserts `data-guided='true'` and dim/disable, but does NOT then tap the correct chip and assert no stardust grant + no streak + no totalCorrect bump. Add that final tap + 3-way assertion. ~25 minutes. **AC row 9.**
6. **Hint-state visual-tightly-coupled-to-audio test** in `WordSong.test.tsx` — assert each letter's `data-highlight='true'` toggle fires _on the phoneme `Howl` `onplay` event_, not at a TTS boundary. Inject a fake `playPhoneme` that fires `onplay` on a controlled tick; assert the visual state machine advances on that tick. ~15 minutes. **AC row 12.**
7. **Forbidden-pair integration test** in `wordDistractors.test.ts` — rotate through all 14 target words at all 8 problemIndex values and assert NO forbidden pair (cat↔dog, bus↔van, pan↔pot, cap↔hat, man↔dad) appears in any chip trio. ~20 minutes. **AC row 6 + survival row "forbidden-pair regression".**
8. **Picture-identity-layer integration test** in `wordDistractors.test.ts` — `it('never returns the target word as one of its own distractors')` enumerates every (target × problemIndex) and asserts `target.word ∉ distractors.map(d => d.word)` AND `target.pictureId ∉ distractors.map(d => d.pictureId)`. ~15 minutes. **Survival check "Picture vs distractor exact-match".**
9. **Cross-screen unified streak test** — when Devon picks the streak architecture (drift item F), add a test that mounts Math 2/2 → unmounts → mounts Word Song → tapping correct first chip should grant `data-streak='3'` AND a bonus stardust (because the unified `[3, 5, 8]` threshold fires). ~25 minutes. **AC row 3.**
10. **End-to-end Path A integration test** — when `App.tsx` wires the real Path A `playUtterance` into Word Song (parallel to PR #54's Math wiring), replace AC rows 11/13/14/15's "BLOCKED-on-Azure-config" + "BLOCKED-on-phonemes" with a real iPad walk-through. Tracking note for Matt; depends on Azure creds being fixed AND phoneme assets landing.
11. **`tests/qa/wordSongAccessibility.test.ts`** — assert chip `aria-label="Picture of {word}"`, ribbon `role="status"` + `aria-live="polite"`, stardust counter `aria-label="Stardust: N"` are present. Locks the contract; mirror of Math's accessibility test from `qa/math-screen.md` test-needed gap #6. ~20 minutes.
12. **Spec phoneme-inventory reconciliation** (drift item B) — Kyle one-line edit to spec §Audio integration contract → "Phoneme audio" table at lines 338–353 to drop `r, s` (no longer needed post PR #64 word-list trim) and add `j, v` (newly needed for `jam, van`). ~5 minutes for Kyle. Not a test gap but a doc gap.

---

## Out of scope for this checklist

- Real iPad device test — Thomas's territory, deferred per task brief operating constraints.
- Production bundle inspection — Devon's impl hasn't shipped yet; nothing in the bundle to inspect. Post-impl: re-run the same `grep` patterns Math uses on `dist/assets/index-*.js` to assert WordSong's contract surface ships (`route === "word-song"` early-return, all 5 Path A error codes, `AbortController` cleanup, `prepareWordSongPathA` wiring).
- Word Song impl review — Kevin's job, peer-review pattern (PR review, not QA's surface).
- Session-end recap surface (ticket `86c9grnjd`, `design/screen-5-session-end.md`) — Word Song emits the `onSessionComplete` callback and that's it. The downstream surface is a separate ticket and a separate spec. **Spec §Transition out (session end) line 542 flags that the existing `screen-5-session-end.md` is Math-flavoured ("says 'Math problem 8'") and needs a generalising tweak when Word Song handoff lands** — flag for Matt as a follow-up edit, not in scope of this checklist.
- Mid-session interrupt and resume (ticket `86c9grnjf`, `design/mid-session-resume.md`) — Word Song's contract is "8 problems then unmount"; close-and-reopen behaviour is the orchestrator's concern.
- Subtraction screen, 2-digit arithmetic, multiplication — separate tickets, post-Math.
- Short-o, short-u, short-i, short-e Word Song variants — separate tickets per Dave's `o → u → i → e` (NOT the diagnostic's `o → u → e → i`) — see drift item N for the inversion. Each vowel family gets its own picture pack per `word-song-picture-pack.md` §Future work.
- Digraphs (sh / ch / th) — backlog. Per Dave's research §Q3, gated on CVC mastery across short-a + at least one more vowel, not all five.
- Sight words — backlog. Per Dave's research §Q4, the 10-word core set (`the, a, I, to, and, was, for, you, is, of`) should slot in concurrently with CVC instruction, not after — but no v1 sight-word screen exists yet.
- Simple sentences — backlog.
- "Tap the word that matches the picture" inverse layout — flagged for v3+ per spec §Why not "tap the word that matches the picture", once CVC reading is automatic.
- Speech-recognition input ("say the word out loud") — v3+, no ticket.
- Animations on Melody's mouth/eyes — out of scope per Session-1 implementation note. Pose swaps only.
- Stardust unlock loop / cosmetic gallery — flagged for v2.
- Streak persistence across sessions — explicitly rejected in v1.
- Curating the CVC short-a picture library — sourcing/commissioning is a parallel asset-pipeline workstream owned by Matt + Thomas. This checklist consumes whatever ships; doesn't author the pictures.
- Authoring the phoneme MP3 inventory — same. Asset-pipeline workstream owned by Matt; this checklist consumes whatever ships.
- Final voice approval — covered by `qa/path-a-server-tts.md`; Word Song inherits Path A's voice config (`en-US-AnaNeural`, rate `-10%`).
- Removing the silent-default `playUtterance` from `WordSong.tsx` after Path A wires in — out of scope for QA; that's a tech-debt cleanup ticket once Azure creds land.
- Letter-tap "she's stuck" telemetry trigger (drift item M, spec §Open Questions #9) — explicitly v2+.
