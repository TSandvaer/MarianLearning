# Math screen (Number Garden, sums to 10) — Regression Checklist

ClickUp tickets:

- `86c9grn33` — **Screen 3 Math impl: sums-to-10, distractor ramp, stardust HUD, streak indicator, audio Path A integration. PR #40, merged as `615513c` on 2026-04-26.** Adds `src/screens/Math/{Math.tsx,distractors.ts,stardust.ts,sessionPlans.ts,constants.ts,index.ts}` plus three test files (101 new tests). Replaces the old `src/screens/Math.tsx` placeholder; `App.tsx` already routes to `./screens/Math`, so the index-barrel swap is wire-compatible.

Lineage:

- `86c9grn9c` (PR #34) — canonical Math spec authored by Kyle.
- `86c9gt449` (PR #38) — Dave-locked decisions baked into the spec: gentle-ramp cutoff at problem 3 (not 2), hint after 2 wrongs (not 1), streak indicator as sparkle (not flame).
- `86c9guegd` (PR #43) — tightened the spec's gentle-ramp distractor example to `{1, 10}` (PR #40 implementation already conformed; PR #43 was a docs-only follow-up).
- `86c9grn1v` (PR #46) — retired the sprite-sheet recommendation in `session-1.md`'s Assets footnote; vector-only + AnimatePresence cross-fade is now the locked architecture across the project. No `screen-3-math.md` change in PR #46 itself; PR #49 is the follow-up sweep that touched Math.
- `86c9gv05j` (PR #49) — terminology sweep that replaced "sprite swap" / "sprite" framing with "expression swap" wording in `screen-3-math.md` (one occurrence in §Wrong-answer policy step 2: Melody's expression swaps `melody-idle.svg` → `melody-puzzled.svg` via AnimatePresence cross-fade). Substance unchanged; pure terminology consolidation. AC row 9's "How verified" copy was authored before this sweep — terminology now updated below.
- `86c9gr385` (PR #28 + #36) — Path A server-side TTS pipeline. Math is its first consumer screen; Math gates its production audio on `ANTHROPIC_API_KEY` landing in Vercel env. Until then, the screen ships with `defaultPlayUtterance` — a synchronous resolve + word-tick at ~165 wpm so Marian sees captions text even without real TTS.
- `86c9gr43t` (PR #29) — GBUG-7 fail-recovery for Greet's MP3s. Math reuses the same `useAudioUnlockGate` watchdog (1.5s) for first-tap audio unlock.
- `86c9gu2rg` (PR #39) — `marian-api/require-js-extension` ESLint rule forbids bare relative imports under `api/*.ts` (forces `.js` extensions). Math doesn't touch `api/`, so the rule is non-load-bearing for this PR; survival row 18 confirms it still fires correctly.

Spec / source-of-truth: `design/screen-3-math.md` (canonical, post-PR-#49 expression-swap terminology, post-PR-#43 distractor-example fix, post-PR-#38 Dave-locks). PR #40's "Testable acceptance criteria (Jessica)" section is the per-PR contract; the spec is the long-lived contract this checklist asserts against.

Prior siblings (templates / regression-adjacent):

- `qa/path-a-server-tts.md` — Path A server-side TTS infrastructure checklist. Math depends on it via the eventual Path A `playUtterance` injection (gated on `ANTHROPIC_API_KEY`). Until then Math runs the silent-but-captioned default.
- `qa/greet-mp3-fail-recovery.md` — GBUG-7 fail-recovery checklist. Math reuses `useAudioUnlockGate`; survival row 16 below re-asserts the Greet flow still works post-Math-impl.
- `qa/greet-regression.md` — Plan B Greet checklist; not directly re-run here, but referenced for the audio-unlock-gate baseline.

This checklist is reusable: rerun every merge that touches `src/screens/Math/**`, `src/lib/audio/useAudioUnlockGate.ts` (Math is now a consumer), `src/lib/sfx/**` (Math uses `createSfx` directly), or `src/App.tsx` (Math route wiring). Aim for ~12 minutes desktop end-to-end, plus the iPad pass.

`src/screens/Greet.*` is **OUT OF SCOPE** for Math — PR #40 explicitly leaves Greet untouched. Survival row 16 is the regression guard that confirms it. If a Math PR ever reaches into Greet, run `qa/greet-regression.md` as well.

`api/**` is **OUT OF SCOPE for new code in this PR** — Math doesn't ship server-side changes. Survival row 17 is the smoke that confirms `/api/claude` is still healthy post-merge.

## How to run

1. `yarn install` (once per branch).
2. `yarn typecheck && yarn lint && yarn test --run` — all green before any manual step. PR #40 advertised 453 passing tests; current count after subsequent merges is 482 (monotonic growth — no regression). If local count diverges below the PR's baseline, investigate before continuing.
3. Manual desktop steps assume either `yarn dev` running locally on `http://localhost:5180/` (or default `5173`) **OR** a Vercel preview is up for the merged commit (the orchestrator will confirm the URL after `615513c` deploys).
4. iPad rows: install the PWA from the Vercel preview URL onto Marian's actual iPad (or QA loaner if available), launch from home screen.
5. Walk through the matrix below. Mark each row PASS / NOTES / FAIL / DEFERRED.

Touch-target / pixel-perfect / iPad-Safari-quirk rows are **DEFERRED-TO-DEVICE** — Thomas owns the binding verdict on a real iPad. Path A audio rows are **BLOCKED-on-ANTHROPIC_API_KEY** until the env var lands; the screen runs end-to-end without it via the silent-but-captioned default.

---

## Spec drift / ambiguities flagged for Kyle, Matt, and Thomas

### Consolidation pass — 2026-04-26 (`86c9gumgk`)

Reviewer flagged 5 drift items where the QA checklist had fallen behind the locked design spec or the merged implementation. Each is reconciled below; the AC matrix rows referenced in §AC matrix have been updated in line.

| #   | Drift item                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Reconciliation                                                                                                                                                                                                                                                                                                                                                              |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | **Guided-completion withhold was incomplete in QA row 5.** Spec §Wrong-answer policy line 308–310 says guided completion withholds **stardust + streak** ("standard happy-path animation but no stardust awarded … Streak does NOT increment"). QA row 5 only mentioned the stardust withhold. Implementation also withholds `totalCorrect` increment (`Math.tsx:629-631` gates `grantStardust(1)` AND `totalCorrectRef.current += 1` on `!problemState.guidedPlayed`).                                           | AC row 5 updated to enumerate all three withholds (stardust, streak, `totalCorrect`) and cite spec line 308–310. Test-needed gap line bumped from 10 → 15 minutes to cover the broader assertion surface.                                                                                                                                                                   |
| 2   | **Stardust persistence "write on session end" was uncovered.** Spec §Stardust treatment line 203–204 specifies _"Read on Math screen mount; write after every increment **and on session end**."_ QA row 6 only validated the per-increment write + reload survival. The session-end / unmount flush at `Math.tsx:378-391` (the `useEffect` cleanup that calls `writeStardust(stardustTotalRef.current, …)`) was undocumented in the AC matrix.                                                                   | AC row 6 updated to call out spec line 203–204 explicitly + cite the unmount-flush implementation as the contract surface + flag a 15-minute test-needed gap (unmount-after-clean-run + assert stored total == in-memory total at unmount).                                                                                                                                 |
| 3   | **Streak-break visual contract had no AC row.** Spec acceptance-criteria bullet line 686 says _"streak indicator fades out over 400ms when breaking from ≥2"_, and §Wrong-answer policy "Streak break" lines 268–285 spell out the exact contract (gentle 400ms fade, no SFX, no TTS, no copy, no shake / no flash / no red). QA row 9 only covered the anti-dark-pattern text-grep half of the wrong-answer policy. The 400ms fade timing + no-SFX / no-TTS contract was not asserted anywhere in the AC matrix. | AC row 9 expanded to cover the full streak-break visual contract: 400ms fade-out, no SFX, no TTS, no copy, plus the post-PR-#49 expression-swap terminology for Melody's idle → puzzled cross-fade. Cited the exact `Math.tsx:540-552` `setStreakFadingOut(true)` + `STREAK_FADE_OUT_MS` timer block. Test-needed gap added: 20-minute fade-out-state-transition assertion. |
| 4   | **Session-end callback shape: spec is behind impl.** Spec line 456 lists 3 fields (`{ totalCorrect, totalStardust, finalStreak }`). Impl + tests + AC row 14 ship 4 fields (`{ totalCorrect, totalStardust, finalStreak, earnedThisSession }`). The 4th field is necessary for the Session End recap surface (ticket `86c9grnjd`) to distinguish _this-session_ stardust from _all-time_ persisted total. Impl is the right shape; the spec needs a one-line update.                                              | AC row 14 reframed to make the spec-vs-impl mismatch explicit + flagged the spec update as drift item J below. **Action for Matt → Kyle:** one-line spec edit to add `earnedThisSession: number` to the §Transition out (session end) callback signature line 456. Filed as drift item J; not blocking — impl + tests are the binding contract.                             |
| 5   | **Lineage + source-of-truth note had not picked up PR #46 + #49.** The "post-PR-#43" annotation in the source-of-truth statement, and the absence of PR #46 + PR #49 from the lineage block, meant any reader of this checklist would not know the spec terminology had shifted from "sprite swap" → "expression swap" (PR #49) and that vector-only + AnimatePresence was now the locked architecture (PR #46). AC row 9 also still used pre-PR-#49 terminology in its description.                              | Lineage block updated: PR #46 (`86c9grn1v`) and PR #49 (`86c9gv05j`) added with one-line summaries. Source-of-truth statement bumped to "post-PR-#49 expression-swap terminology, post-PR-#43 distractor-example fix, post-PR-#38 Dave-locks". AC row 9 expression-swap wording aligned with the spec's post-PR-#49 phrasing.                                               |

All 5 items are reconciled in-line. Item 4 (callback shape) requires a one-line follow-up edit to `design/screen-3-math.md` line 456 — flagged for Matt to route to Kyle, tracked as drift item J below. Items 1, 2, 3, 5 are pure QA-checklist alignment with the locked spec; no production code change required.

### Pre-consolidation drift register (Items A–I — historical, retained for traceability)

| #   | Status                    | Note                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| --- | ------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------- | ---------- |
| A   | **Resolved in PR #38**    | Distractor cutoff locked at `GENTLE_RAMP_THROUGH = 3` in `src/screens/Math/distractors.ts:49`. Single source of truth; one canary test at `distractors.test.ts:23-29` asserts the constant stays at 3.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| B   | **Resolved in PR #38**    | Hint threshold locked at 2 wrongs (`HINT_AFTER_WRONG_COUNT = 2` in `Math.tsx:55`). Mastery-learning rationale carried in spec §Wrong-answer policy.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| C   | **Resolved in PR #38**    | Streak indicator is sparkle (no flame asset). Verified: zero `flame` references in `src/` or `public/assets/`. The only matches in the repo are in `design/*.md` documenting the rejected option (see row 8 grep). Inline-SVG fallback `SparkleGlyph` at `Math.tsx:1051-1071` is the rendered shape; canonical asset (`sparkle-particle.svg`) is still on `assets-todo.md`.                                                                                                                                                                                                                                                                                                                                                                |
| D   | **Resolved in PR #43**    | Spec's gentle-ramp example tightened to `{1, 10}` for `correct=5`. `distractors.test.ts:80-85` already asserted this. No code change in PR #43; docs-only.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| E   | **Open — flag for Matt**  | `STREAK_BONUS_THRESHOLDS = [3, 5, 8]` is a Thomas-taste call per spec §Open Questions item 2. Dave's memo supports it from a reinforcement-schedule standpoint but flags it as taste, not developmental requirement. **Default until decided:** ship `[3, 5, 8]` (PR #40 ships this). Constant is single-source-of-truth in `src/screens/Math/constants.ts:20`; one number to change if Thomas overrides.                                                                                                                                                                                                                                                                                                                                  |
| F   | **Open — flag for Matt**  | Path A `playUtterance` is **not yet wired** into Math's mount. PR #40 ships the silent-but-captioned `defaultPlayUtterance` (resolves + ticks captions at 165 wpm). When `ANTHROPIC_API_KEY` lands and `App.tsx` injects a real Path A `playUtterance` prop, all "audio" AC rows below (rows 10, 11, 12, 13) become live-testable — until then they're auto-only on the contract surface and the desktop/iPad portions are BLOCKED-on-key. **Not a regression in PR #40 — explicitly called out in the PR body's "Risk / rollback".**                                                                                                                                                                                                      |
| G   | **Open — flag for Devon** | Three required SVG assets are still on `assets-todo.md`: `bg-garden.svg`, `flower-glyph.svg`, `sparkle-particle.svg`, plus the four SFX files (`sfx-sparkle.mp3`, `sfx-poof.mp3`, `sfx-plink.mp3`, the reused `sfx-chime-soft.mp3`). PR #40 ships inline-SVG fallbacks for the visuals (gradient garden, 5-petal CSS-circle flowers, gold star sparkle) so the screen reads. SFX silently no-op via `createSfx`'s warn-once-then-dead-handle pattern — same as Greet's pre-PR-#25 era. **Not a regression in PR #40.** Row 14 visual smoke confirms the inline fallbacks render.                                                                                                                                                           |
| H   | **Open — flag for Kevin** | The `useEffect` cleanup at `Math.tsx:378-391` calls `writeStardust(stardustTotalRef.current, …)` on unmount as a defensive flush. This is correct behaviour but means stardust gets re-persisted on every unmount of Math, even on routes that didn't grant any. Not a bug — same value gets rewritten with a fresh `lastUpdatedAt`. **Worth a one-line check in code review on future refactors so the pattern doesn't drift.**                                                                                                                                                                                                                                                                                                           |
| I   | **Open — flag for Kyle**  | Spec §Wrong-answer policy line 312 says _"No wrong-answer counter is displayed anywhere."_ The implementation respects this — `wrongCount` state is internal-only — but the test at `Math.test.tsx:648-678` checks for `'wrong'` and `'incorrect'` substrings in the rendered DOM, which is necessary but not sufficient (it doesn't assert "no integer-as-text near the chip"). Reasonable coverage for v1; if Kyle wants stronger guarantees, follow-up automation can grep `data-testid="math"` for the regex `/\b[1-9]\b\s\*(wrong                                                                                                                                                                                                     | attempts | tries)/i`. |
| J   | **Open — flag for Kyle**  | Spec §Transition out (session end) line 456 lists the callback shape as `onSessionComplete({ totalCorrect, totalStardust, finalStreak })` — **3 fields**. Implementation (`MathSessionResult` at `Math.tsx:98-104`) ships **4 fields**: adds `earnedThisSession: number`. The 4th field is needed by the Session End recap surface (ticket `86c9grnjd`) to celebrate _this-session_ stardust without confusing it with the all-time persisted total. Impl + tests + AC row 14 are the binding contract; spec is behind by one line. **Action:** Kyle, please add `earnedThisSession: number` to the spec's callback signature on line 456. One-line edit; not blocking. Surfaced by the 2026-04-26 drift consolidation pass (`86c9gumgk`). |          |            |

A/B/C/D are unblocked in the matrix. E, F, G, H, I, J are notes for Matt's queue, not gating items. Items 1–5 of the consolidation pass above were resolved in-line in this commit.

---

## Distribution

`X automated / Y manual desktop / Z iPad-only`

- **AC matrix:** 14 AC rows (rows 1–14), plus 4 survival/regression rows (rows 15–18) = **18 rows total.**
- **Automated:** **9** (rows 1, 2, 3, 4, 6, 7, 8, 9, 18 — covered by Vitest in `src/screens/Math/{Math.test.tsx, distractors.test.ts, stardust.test.ts}` + `eslint-rules/require-js-extension.test.ts` + cross-suite re-runs in `Greet.test.tsx`).
- **Manual desktop:** **5** (rows 5, 10, 11, 13, 14 — DevTools `Application` tab inspection, dev-server visual smoke for inline SVG fallbacks, default-playUtterance caption ticks, auto-advance timing, session-end recap callback observation).
- **iPad-only:** **1** (row 12 — first session utterance audible after wake-tap on installed PWA — gated on `ANTHROPIC_API_KEY` and Path A `playUtterance` wiring per Spec Drift item F).
- **Survival/regression:** rows 15 (auto + manual desktop), 16 (auto), 17 (manual desktop / curl OPTIONS), 18 (auto).
- **Path A audio rows (10, 11, 12, 13):** the auto portions assert PR #40's contract surface (caption rendering via `onWordTick`, ribbon `aria-live`, etc.) without requiring real TTS. The manual/iPad portions are BLOCKED-on-key.

Plus 5 survival checks (lens 4) and 4 console/network sanity items below the matrix.

---

## Run results — 2026-04-26 (Jessica) — branch `qa/86c9grn33-math-checklist` @ origin/main `615513c` (post-PR-#43 docs-only, base SHA `281f3cc`)

**Backend smoke (run before any other row, since Math is now Path A's first consumer screen):**

```
$ curl -s -X OPTIONS -i https://marian-learning.vercel.app/api/claude --max-time 15
HTTP/1.1 204 No Content
Access-Control-Allow-Headers: Content-Type
Access-Control-Allow-Methods: POST, OPTIONS
Access-Control-Max-Age: 86400
Cache-Control: public, max-age=0, must-revalidate
Server: Vercel
X-Vercel-Cache: MISS
```

Result: PASS. CORS preflight returns 204 with the canonical headers. Function endpoint reachable; PR #40 did not regress the Path A surface. (POST smoke is intentionally omitted in this run — orchestrator restricted to OPTIONS only to avoid a real Anthropic call against prod from QA. Functional POST coverage relies on `qa/path-a-server-tts.md`'s "Run results — 2026-04-25" block, which already validated dispatch-path smokes against `199411b`. PR #40 added zero `api/` files; per `gh pr view 40 --json files` the PR is frontend-only.)

**Auto suite (`yarn test --run`):** PASS. **24 test files, 482/482 passing in 59.3s.** No reds, no skips. PR #40 advertised 453; current count is 482 because subsequent merges (PR #43, etc.) added/modified tests. Monotonic growth — no regression.

**Auto sub-suites of interest (per AC matrix references):**

- `src/screens/Math/Math.test.tsx` — **10/10 PASS** (covers AC rows 1, 5-state-only, 6, 7, 8, 9, 13-state-only, anti-dark-pattern row 4)
- `src/screens/Math/distractors.test.ts` — **78/78 PASS** (covers AC row 2 — exhaustive sweep of every `correct ∈ [1,10]` × every tier representative)
- `src/screens/Math/stardust.test.ts` — **13/13 PASS** (covers AC rows 5-state-only, 6 contract, defensive iOS-quota row 9)
- `src/screens/Greet.test.tsx` — **49/49 PASS** (regression baseline, survival row 16)
- `src/screens/greetSequence.test.ts` — **30/30 PASS** (regression baseline, survival row 16)
- `src/lib/audio/useAudioUnlockGate.test.tsx` — **20/20 PASS** (Math is now a consumer; baseline preserved)
- `tests/qa/audioAssetIntegrity.test.ts` — **13/13 PASS** (Greet MP3 integrity; non-load-bearing for Math but in the cross-suite green wall)
- `eslint-rules/require-js-extension.test.ts` — **29/29 PASS** (PR #39 lint rule unit suite, survival row 18)
- `src/App.test.tsx` — **4/4 PASS** (route wiring smoke; Math is now `route === 'math'` with the new component, regression preserved)

**Static checks:** `yarn typecheck` PASS clean. `yarn lint` PASS clean (zero errors, zero warnings).

**Build:** `yarn build` PASS. Bundle: `dist/assets/index-Dl0MWEo5.js` — 351.68 kB (gzip 112.10 kB), `index-B-bGjUfJ.css` — 11.53 kB (gzip 3.11 kB). 456 modules transformed. SW precache 37 entries / 516.28 KiB. Math added ~12 KB gzipped per PR body, within iPad budget.

**Dev-server visual smoke (rows 5, 10, 11, 13, 14 best-effort):**

Started `yarn dev --port 5180`; confirmed:

- `GET http://localhost:5180/` → 200, 2680 bytes (index.html served).
- `GET /src/screens/Math/Math.tsx` → 200, 129,919 bytes (Math module compiles via Vite SSR transform without errors).
- `GET /src/screens/Greet.tsx` → 200, 142,320 bytes (Greet still serves; survival regression row 16 desktop portion).
- Dev-server log: zero errors, zero warnings.

This is **module-load smoke**, not a full DOM walkthrough — Vitest's jsdom render under `Math.test.tsx` already exercises mount, chip taps, hint, guided completion, session-complete callback, anti-dark-pattern text grep. Routes that demand a real iPad-portrait DOM + a tap gesture (`Math.tsx`'s `useAudioUnlockGate` first-tap path, problem-dot ring focus, sparkle-burst animation, stardust grain float-to-counter) remain DEFERRED-TO-iPad or BLOCKED-on-key per their row notes.

**`flame` non-regression grep (row 8 manual workaround):**

```
$ grep -rni "flame\|icon-flame" src/ public/assets/ public/ 2>/dev/null
(no matches)
```

Result: PASS. Zero `flame` references in `src/` or `public/assets/`. The seven matches in the repo all live in `design/*.md` documenting the rejected option (per Dave's memo). Spec invariant from §Stardust treatment "No new flame asset is authored" preserved.

**`wrong`/`incorrect` user-text non-regression grep (row 4 manual workaround beyond the auto test at `Math.test.tsx:648`):**

```
$ grep -rni "wrong\|incorrect" src/screens/Math/Math.tsx
src/screens/Math/Math.tsx:54:/** Wrong-attempt count after which the hint utterance fires. */
src/screens/Math/Math.tsx:55:const HINT_AFTER_WRONG_COUNT = 2
src/screens/Math/Math.tsx:57:/** Wrong-attempt count after which the guided-completion path fires. */
src/screens/Math/Math.tsx:58:const GUIDED_AFTER_WRONG_COUNT = 3
... (all matches are identifiers, JSDoc comments, or constant names — never JSX text or aria-labels)
```

Result: PASS. No user-visible "wrong" or "incorrect" string ever rendered. Auto test `Math.test.tsx:648-678` is the binding assertion against the rendered DOM.

**Voice-config / Web Speech non-regression grep (cross-cutting from Path A):**

```
$ grep -rni "speechSynthesis\|window\.speech" src/screens/Math/
(no matches)
$ grep -rni "from.*lib/tts" src/screens/Math/
(no matches)
```

Result: PASS. Math does not pull from `lib/tts/` and never touches `window.speechSynthesis`. Path A is the only audio path Math will eventually consume (via the `playUtterance` prop), per spec §Audio integration contract.

**Per-row verdicts:**

| #   | Verdict                        | Notes                                                                                                                                                                                                                                                                                                                                                                            |
| --- | ------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | PASS                           | Auto green. `Math.test.tsx:232` renders mount + HUD + Melody + chips. Plus dev-server module-load smoke confirmed `Math.tsx` compiles cleanly.                                                                                                                                                                                                                                   |
| 2   | PASS                           | Auto green. 78 distractor tests exhaustively sweep every `(correct, problemIndex)` combination across both tiers + the constraint-set + clamp-on-overflow + the `GENTLE_RAMP_THROUGH = 3` canary.                                                                                                                                                                                |
| 3   | PASS                           | Auto green. `Math.test.tsx:408` (streak threshold 3 grants bonus stardust) + `Math.test.tsx:579` (clean 8-for-8 yields 11 stardust = 8 + bonuses at 3/5/8) directly assert the threshold contract.                                                                                                                                                                               |
| 4   | PASS-AUTO / BLOCKED-DESKTOP    | Auto green. `Math.test.tsx:367` (hint after 2 wrongs). Manual desktop walk-through requires a real DOM tap interaction that triggers the 600ms hint timer — feasible against `yarn dev`, but not cheap to demo without a real flow narrative. **Recommend Thomas's pass picks this up on the iPad PWA where the timing is observable in real audio.**                            |
| 5   | PASS-AUTO / BLOCKED-DESKTOP    | Auto green. `Math.test.tsx:527` asserts `data-guided='true'`, correct chip enabled, others disabled, "This one is five" spoken. Stardust withhold on guided path is asserted via the `data-stardust` not changing during the guided sequence in the same test. Manual desktop deferred per row 4 reasoning.                                                                      |
| 6   | PASS-AUTO / BLOCKED-DESKTOP    | Auto green. `Math.test.tsx:267` asserts persistence — taps correct, then reads `STARDUST_STORAGE_KEY` from the injected memory adapter. Manual desktop reload-survival smoke deferred — best-tested on the deployed PWA where iPadOS Safari's localStorage quirks are real (private-mode iOS quota throw is auto-covered at `stardust.test.ts:123`).                             |
| 7   | PASS                           | Auto green. `stardust.test.ts:123` asserts `writeStardust` returns the in-memory state when `setItem` throws (iOS private-mode `QuotaExceededError`). `stardust.test.ts:138` asserts the warn fires only once across multiple failing writes.                                                                                                                                    |
| 8   | PASS                           | Auto green (no test asserts the negative — there's no test, because there's no flame import; the absence is the assertion). Manual grep above confirms zero `flame` references in `src/` / `public/`. **Recommend Devon adds `tests/qa/noFlameInUI.test.ts` as a build-time guard so a future asset rename can't accidentally re-introduce.** Flagged in test-needed gaps below. |
| 9   | PASS                           | Auto green. `Math.test.tsx:648` greps the rendered DOM for `'wrong'` and `'incorrect'` after a wrong tap — zero matches. Anti-dark-pattern enforcement at the DOM level, not just at the source level.                                                                                                                                                                           |
| 10  | PASS-AUTO / BLOCKED-on-key     | Auto green (caption + ribbon contract surface verified by `Math.test.tsx`'s `data-testid="math-caption-word"` reveal counts via `data-revealed`). Live audio playback BLOCKED until `ANTHROPIC_API_KEY` lands in Vercel env and `App.tsx` injects a real Path A `playUtterance` (Spec Drift item F).                                                                             |
| 11  | PASS-AUTO / BLOCKED-on-key     | Auto green. `Math.test.tsx`'s `makePlayHarness` asserts captions tick word-by-word via `onWordTick` — same contract a real Path A `playUtterance` will deliver. Live audio gating same as row 10.                                                                                                                                                                                |
| 12  | BLOCKED-iPad                   | iPad row. Thomas owns. Also gated on `ANTHROPIC_API_KEY` per row 10. Recommend deferring to the integration session when the env var lands.                                                                                                                                                                                                                                      |
| 13  | PASS-AUTO / BLOCKED-DESKTOP    | Auto green. `Math.test.tsx:267` advances by 1200ms via fake timers and asserts `data-problem-index` increments. Manual desktop confirmation against real animation timing — reasonable to defer to Thomas's iPad pass; not regression-critical given the auto coverage.                                                                                                          |
| 14  | PASS-AUTO / PASS-DESKTOP-smoke | Auto green: `Math.test.tsx:579` asserts `onSessionComplete` invoked exactly once at problem 8 with `{ totalCorrect: 8, totalStardust: 11, finalStreak: 8, earnedThisSession: 11 }`. Desktop smoke: confirmed Math module compiles + serves under `yarn dev`. Visual confirmation of the inline-SVG sparkle/flower/garden fallbacks rendering deferred to Thomas's pass.          |
| 15  | PASS                           | Auto green via `App.test.tsx` (Math route wiring) + visual confirmation of inline-SVG fallback components (`SparkleGlyph`, `FlowerGroup`, `FlowerGlyph`) defined in `Math.tsx:1051-1129`. Desktop smoke confirmed `Math.tsx` serves without compile errors.                                                                                                                      |
| 16  | PASS                           | Auto green: `Greet.test.tsx` 49/49 + `greetSequence.test.ts` 30/30 + `useAudioUnlockGate.test.tsx` 20/20. PR #40 did not modify any Greet source (`gh pr view 40 --json files` confirms). Desktop dev-server confirmed `Greet.tsx` still serves and compiles.                                                                                                                    |
| 17  | PASS                           | Backend smoke section above: `OPTIONS` returns 204 with canonical CORS headers. PR #40 added zero `api/` files. POST-path coverage relies on `qa/path-a-server-tts.md`'s prior run on `199411b` (function-reachable, error envelopes correct).                                                                                                                                   |
| 18  | PASS                           | Auto green. `eslint-rules/require-js-extension.test.ts` — 29/29 passing. `yarn lint` clean across the whole repo (Math doesn't touch `api/`, but the rule must still load + run cleanly).                                                                                                                                                                                        |

**Survival checks (lens 4):**

| Check                                    | Verdict                                                                                                                                                                                                                                                                                                                                                                             |
| ---------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Rage-tap correct chip 5× rapidly         | PASS-AUTO. `Math.tsx:682` `if (problemState.resolved) return` + chip `disabled={problemState.resolved}` short-circuit. Implementation reviewed; no auto test directly fires 5 rapid clicks but the gating is unconditional. **Test-needed:** add a "rage-tap" test that fires 5 events synchronously and asserts only one stardust grant + one auto-advance scheduled. ~15 minutes. |
| Rage-tap mid-shake (wrong → wrong)       | PASS-AUTO via `Math.test.tsx:367` (two wrongs trigger hint exactly once). Implementation gates `willTriggerHint` on `!problemState.hintPlayed`, preventing double-firing.                                                                                                                                                                                                           |
| Background / resume mid-session          | BLOCKED-iPad. Thomas owns. iPadOS will suspend the audio context; `useAudioUnlockGate` should re-engage on next gesture. Cross-screen integration deferred to device pass; auto coverage in `useAudioUnlockGate.test.tsx`'s relock path is the only signal today.                                                                                                                   |
| Stardust schema corruption / first-run   | PASS-AUTO. `stardust.test.ts:47` (corrupt JSON → `emptyStardust`), `:54` (missing fields → `emptyStardust`), `:61` (mismatched schema version → `emptyStardust`), `:74` (negative total clamped to 0). All recovery paths green.                                                                                                                                                    |
| Plan rotation across consecutive minutes | PASS-AUTO. `pickStaticSessionPlan(now)` in `sessionPlans.ts:202` is deterministic per-minute; `Math.tsx:255-259` captures the plan once on mount via `useMemo([])` so a parent re-render with fresh `now` does NOT re-roll mid-session. Pinning verified by reading the impl.                                                                                                       |

**Console / network sanity:**

- DevTools Console must show **zero** errors and zero warnings on a Math screen mount with the silent-default `playUtterance`. `console.warn` is intentionally fired by `createSfx` when an SFX file 404s (assets-todo follow-ups) — those are expected and silent in production but visible in dev. Math.test.tsx mocks `console.warn` accordingly to keep the unit-test log clean (`Math.test.tsx:220`).
- Network tab on Math mount: zero outbound requests beyond the initial Vite HMR (in dev) or the bundled JS+CSS (in prod). No `/api/claude` call until Path A wiring lands.
- `Application → Local Storage → marian-tutor.stardust.v1` populates with `{"total":N,"lastUpdatedAt":"...","schemaVersion":1}` after the first correct tap. Reload survives (auto coverage at `Math.test.tsx:317`; desktop manual deferred per row 6).
- Service worker precache: PR #40 does NOT change SW precache contract. Build output confirms 37 precache entries / 516.28 KiB — same baseline as post-PR-#25.

---

## AC matrix

One row per acceptance-criteria bullet from PR #40's "Testable acceptance criteria (Jessica)" section + spec §Acceptance criteria, consolidated by topic. Plus regression rows derived from "Risk / rollback" + cross-cutting concerns (Greet untouched, Path A endpoint healthy, lint rule still firing, asset-fallback rendering).

| #   | Acceptance criterion (source)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Owner              | How verified                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Math screen renders 8 problems sequentially from the chosen plan; each shows symbolic line (96pt addends), visual flower groups, and 3 answer chips (PR #40 AC).                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Auto**           | **Auto:** `Math.test.tsx:232` `it('renders the first problem on mount with HUD, melody, and chips')` asserts mount + `data-testid="math-symbolic"` + `data-testid="math-chip"` ×3 + addend values + correct ∈ chip values + chip values all distinct. `Math.test.tsx:579` `it('completes the session on problem 8 …')` walks all 8 problems and asserts session-end callback fires exactly once on problem 8.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 2   | Distractor rule per spec: problems 1–3 use gentle ramp (≥2 away, biased to range extremes); problems 4–8 use off-by-one (`correct ± 1` clamped to `[1, 10]`). Constraint set: in-range, distinct, deterministic per `(correct, problemIndex)` (PR #40 AC + spec §Distractor policy).                                                                                                                                                                                                                                                                                                                                                 | **Auto**           | **Auto (78 tests):** `distractors.test.ts` exhaustive sweep — every `correct ∈ [1, 10]` × every tier representative — asserts in-range, distinct, integer, ≠ correct. Plus per-tier worked examples: gentle `correct=5 → [1, 10]`, gentle `correct=2/10/1` with anchor + searched mate; off-by-one `correct=5 → [4, 6]`, `correct=10 → [8, 9]`, `correct=1 → [2, 3]`. Plus `pickTier` cutoff canary (`distractors.test.ts:23`) locks `GENTLE_RAMP_THROUGH === 3` per Dave's PR #38.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 3   | Streak `[3, 5, 8]` thresholds trigger sparkle bonus stardust on clean run (PR #40 AC + spec §Stardust treatment).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Auto + Desktop** | **Auto:** `Math.test.tsx:408` `it('streak threshold [3, 5, 8] grants a bonus stardust at problem 3')` asserts `data-total='4'` after 3 clean correct taps (3 base + 1 bonus). `Math.test.tsx:579` `it('completes the session on problem 8 …')` asserts `totalStardust === 11` for clean 8-for-8 (8 base + 3 bonuses at 3/5/8). Constant `STREAK_BONUS_THRESHOLDS = [3, 5, 8]` exported from `constants.ts:20` — single source of truth. **Desktop:** observe HUD pop animation on problem-3 clean run; sparkle indicator visible from streak ≥ 2 onward. Deferred to Thomas's iPad pass for visual binding verdict.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 4   | Hint utterance fires after 2 wrong attempts on the same problem (PR #40 AC + spec §Wrong-answer policy).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Auto + Desktop** | **Auto:** `Math.test.tsx:367` `it('hint utterance fires after 2 wrong attempts on the same problem')` taps two wrong chips, advances 700ms past the 600ms `HINT_DELAY_AFTER_WRONG_MS` beat, asserts the hint line `'Look. Three. And two more. How many now?'` is in `harness.spoken()`. Threshold constant `HINT_AFTER_WRONG_COUNT = 2` at `Math.tsx:55`. **Desktop:** observe the hint flow live in `yarn dev`; flower-group pulse choreography is spec'd but currently rendered as static SVG (`FlowerGroup`/`FlowerGlyph` inline) — pulse animation is NOT a separate AC bullet in PR #40 (spec §Wrong-answer policy → Hint state describes it but PR #40's AC list omits it). **Flag for Matt:** is the per-flower 150ms scale pulse in the v1 hint surface, or deferred? Spec line 298 says yes, AC list is silent. Default verdict: scope-deferred.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 5   | After 3 wrong attempts, guided completion fires: `giveAnswer` utterance plays, correct chip is the only tappable one, others disabled at opacity 0.6, NO stardust granted, **streak does NOT increment**, and `totalCorrect` is NOT bumped (PR #40 AC + spec §Wrong-answer policy "After 3 wrong attempts" line 308–310: _"standard happy-path animation but **no stardust awarded** … **Streak does NOT increment**"_).                                                                                                                                                                                                             | **Auto + Desktop** | **Auto:** `Math.test.tsx:527` `it('after 3 wrong attempts, guided completion dims other chips and disables them')` taps 3 wrong chips, asserts `data-guided='true'`, correct chip not disabled, others disabled, give-answer utterance spoken. **Stardust + streak + totalCorrect withhold:** all three are gated on `!problemState.guidedPlayed` at `Math.tsx:629-631` (the `if (!problemState.guidedPlayed) { grantStardust(1); totalCorrectRef.current += 1; … setStreak(streakRef.current) }` block). The streak-no-increment is also implicit from the wrong-tap path: `streakRef.current = 0` fires at `Math.tsx:540` on the first wrong tap, so by the 3rd-strike guided tap streak is already 0. **Test-needed gap:** add an explicit assertion that tapping the correct chip after the guided sequence does NOT increment `data-stardust`, does NOT bump `data-streak`, and does NOT increase the `totalCorrect` reported by `onSessionComplete`. ~15 minutes. **Desktop:** observe live; deferred per row 4 reasoning.                                                                                                                                                                                                                                                                                                                                                                  |
| 6   | Stardust persists in localStorage at key `marian-tutor.stardust.v1` with `schemaVersion: 1`. Re-load picks up the persisted total. Per spec §Stardust treatment "Persistence" line 203–204: _"Read on Math screen mount; write after every increment **and on session end**."_ The session-end write is the defensive flush that survives a mid-session iOS background-kill (PR #40 AC + spec §Stardust treatment "Persistence").                                                                                                                                                                                                    | **Desktop**        | **Auto (per-increment):** `Math.test.tsx:267` taps correct chip, then asserts `JSON.parse(stored).total === 1` from the injected memory storage adapter. `stardust.test.ts:39` round-trip test. Schema version locked at `STARDUST_SCHEMA_VERSION = 1` in `stardust.ts:41`. **Auto (session-end / unmount flush):** the `useEffect` cleanup at `Math.tsx:378-391` calls `writeStardust(stardustTotalRef.current, …)` on unmount. This is the contract surface for the spec's "write on session end" — the Math component unmounts when the session-end callback completes its consumer's transition. **Test-needed gap:** add an explicit `Math.test.tsx` test that unmounts after a clean run and asserts the stored total in the storage adapter equals the in-memory total at unmount time. ~15 minutes. **Desktop:** in `yarn dev`, walk through 1 correct tap, open `Application → Local Storage → marian-tutor.stardust.v1`, confirm shape `{"total":1,"lastUpdatedAt":"…","schemaVersion":1}`. Hard-reload page, mount Math again, confirm stardust counter shows `1` (not `0`). **BLOCKED-DESKTOP** for the _binding_ manual smoke until Vercel preview is up for `615513c` — local dev is sufficient for the contract verdict but Thomas's iPad pass is the Marian-realistic one.                                                                                                        |
| 7   | Stardust persistence survives an iOS-quota-exceeded `setItem` throw — in-memory total continues, write fails silently, warn fires once (PR #40 AC + spec §Stardust treatment "Defensive posture").                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Auto**           | **Auto:** `stardust.test.ts:123` `it('returns the in-memory state even if setItem throws')` injects a `setItem` that throws `QuotaExceededError`, asserts return value still has `.total === 11`. `stardust.test.ts:138` `it('warns only once across multiple failing writes')` asserts the once-only warn flag works. **Test seam `_resetStardustWarn()`** at `stardust.ts:163` allows test isolation.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 8   | Streak indicator renders as **sparkle, not flame** (per Dave's PR #38 lock + spec §Stardust treatment "Streak indicator visual").                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Auto**           | **Auto (negative-space invariant):** `grep -rni "flame" src/ public/assets/` returns zero matches. The seven matches in the repo all live in `design/*.md` documenting the rejected option. `SparkleGlyph` inline-SVG component at `Math.tsx:1051-1071` is the rendered shape; canonical `sparkle-particle.svg` asset still on `assets-todo.md`. **Test-needed:** `tests/qa/noFlameInUI.test.ts` — fail the build if any source file under `src/` or any asset under `public/` references `flame` or `icon-flame`. ~15 minutes. **Flag to Devon.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 9   | Wrong-answer policy: distractor stays available, no red X, no shame text. No "wrong"/"incorrect" string ever rendered. No wrong-answer counter displayed. **On a wrong tap that breaks an active streak (`streak ≥ 2`), the streak indicator gently fades to opacity 0 over 400ms with no SFX, no TTS, no copy** (spec §Wrong-answer policy "Streak break" lines 268–285 + acceptance-criteria bullet line 686 _"streak indicator fades out over 400ms when breaking from ≥2"_). Melody's expression cross-fades idle → puzzled (200ms `AnimatePresence` cross-fade per spec line 256–258, post-PR-#49 expression-swap terminology). | **Auto + Desktop** | **Auto (no red / no "wrong" text):** `Math.test.tsx:648` `it('does not display any "X" or "wrong" copy on a wrong tap (anti-dark-pattern)')` taps a wrong chip and asserts `screen.getByTestId('math').textContent.toLowerCase()` does NOT contain `'wrong'` or `'incorrect'`. Manual `grep -rni "text-red\|bg-red"` in `src/screens/Math/` returns zero matches — no red colour anywhere. Wrong chip's animate state at `Math.tsx:990` is the soft horizontal shake `[0, -6, 6, -4, 4, 0]` over 400ms, with **no colour change**. **Auto (streak-break fade-out contract):** `Math.tsx:540-552` flips `streakRef.current = 0` and conditionally calls `setStreakFadingOut(true)` when prior streak was `≥ 2`, then schedules `setStreak(0); setStreakFadingOut(false)` after the 400ms fade window. **Test-needed gap:** add a `Math.test.tsx` test that builds a clean streak of 2, taps a wrong chip, asserts the streak HUD element transitions to `data-fading-out='true'` for ~400ms then unmounts (or `data-streak='0'`), with no SFX call and no TTS line referencing the streak. ~20 minutes. **Desktop:** in `yarn dev`, build a 2-streak, tap wrong, visually confirm streak indicator fades quietly with no shake / no flash / no red / no audio cue. Deferred to Thomas's pass for binding visual verdict; auto coverage on the fade-state transition closes the regression surface. |
| 10  | Path A audio integration: per-problem read-aloud (`math.p{N}.read`) plays on screen entry after problem reveal stagger completes; caption ribbon mirrors TTS word-by-word via Path A `onWordTick` (PR #40 AC + spec §Audio integration contract).                                                                                                                                                                                                                                                                                                                                                                                    | **Auto + iPad**    | **Auto (contract surface):** `Math.test.tsx`'s `makePlayHarness` synchronously fires `playOpts.onPlay()` and calls `playOpts.onWordTick(i)` for each word; `Math.tsx:415` `setCaptionRevealed((prev) => Math.max(prev, wordIndex + 1))` then drives the per-word `data-revealed` toggle on `<m.span data-testid="math-caption-word">`. The contract surface (caption-ribbon word reveal via `onWordTick`) is fully exercised. **Live Path A audio:** BLOCKED-on-key per Spec Drift item F. Until `ANTHROPIC_API_KEY` lands and `App.tsx` injects a real Path A `playUtterance`, Math runs the silent-but-captioned default — captions do reveal word-by-word at ~165 wpm, but there's no audible voice. **iPad:** Thomas owns first-utterance audible playback after wake-tap on installed PWA. BLOCKED-on-key.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 11  | First chip-tap unlocks audio context via `useAudioUnlockGate` (1.5s watchdog); Math is now its second consumer screen alongside Greet (PR #40 AC + spec §Implementation pointers).                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Auto + iPad**    | **Auto:** `Math.tsx:278` instantiates `useAudioUnlockGate({ watchdogMs: 1500 })`. `Math.tsx:702-716` wraps the very first chip-tap result `speak()` call via `gate.wrapSpeak(() => …)`, then sets `audioUnlocked = true` so subsequent taps skip the gate. The `useAudioUnlockGate.test.tsx` 20-test suite covers the watchdog contract. **iPad:** Thomas owns the first-tap-on-installed-PWA verdict — that's the only environment where iPad Safari's "no audio without a real gesture" gating actually fires; jsdom can't reproduce it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 12  | First session utterance plays after wake-tap on installed iPad PWA within ~1.5s of tap; if not, the gate's relock ring re-engages per `useAudioUnlockGate` contract (PR #40 AC + spec line 716–717).                                                                                                                                                                                                                                                                                                                                                                                                                                 | **iPad**           | **DEVICE:** Install PWA from Vercel preview (when up for `615513c`), launch from home screen, navigate to Math (Session 2+ entry — Greet still runs in Session 1, Math is the second screen via `goMath`). Tap the first chip; first utterance should be audible within 1.5s. If not, the gate ring re-appears (Greet's GBUG-7 recovery contract carries over). **BLOCKED-on-key** until `ANTHROPIC_API_KEY` lands AND `App.tsx` injects Path A `playUtterance`. Until then the silent-default leaves the gate timing assertable but not the _audio_. Recommend Thomas tests the captions-only path on the iPad first to confirm screen renders, then re-runs after Path A wiring.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 13  | Auto-advance after a correct tap fires at 1.2s (`ADVANCE_AFTER_CORRECT_MS = 1200`) — next problem mounts via cross-stagger fade (PR #40 AC + spec §Audio dispatch sequence on chip tap (correct)).                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Desktop + iPad** | **Auto:** `Math.test.tsx:308` advances `vi.advanceTimersByTime(1200)` and asserts `data-problem-index='1'` (incremented from 0). The 1200ms constant locked at `Math.tsx:61`. **Desktop:** in `yarn dev`, tap correct chip and stopwatch the time-to-next-problem visually. Should feel like roughly one beat — long enough to see the celebration, short enough to keep momentum. **iPad:** binding visual verdict — Thomas owns. Deferred to device pass.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 14  | Session-end callback `onSessionComplete(result)` fires exactly once on problem 8. Implementation shape (`MathSessionResult`, `Math.tsx:98-104`): `{ totalCorrect, totalStardust, finalStreak, earnedThisSession }` — **4 fields**. Spec line 456 currently lists only `{ totalCorrect, totalStardust, finalStreak }` — **3 fields, missing `earnedThisSession`**. Implementation + tests are the binding contract; spec is behind. Drift flagged in §Spec drift item J for Kyle to update the spec line (PR #40 AC + spec §Transition out (session end) line 456).                                                                   | **Desktop + iPad** | **Auto:** `Math.test.tsx:579` asserts `onSessionComplete.mock.calls[0][0]` shape `{ totalCorrect: 8, totalStardust: 11, finalStreak: 8, earnedThisSession: 11 }` for clean 8-for-8 run. Callback signature exported via `MathSessionResult` interface (`Math.tsx:98-104`). **Note:** `earnedThisSession` distinguishes _this-session_ stardust from _all-time_ persisted total — necessary for the Session End recap surface (ticket `86c9grnjd`) which celebrates "you earned X today" without confusing it with the all-time counter. The 4-field shape is correct; the spec needs the one-line update flagged in §Spec drift item J. **Desktop:** confirm next-screen wiring in `App.tsx` — at the time of this checklist, `App.tsx` does NOT pass `onSessionComplete` (it's optional per `MathProps`). Session-end recap surface is a separate ticket (`86c9grnjd`, spec at `design/screen-5-session-end.md`). **Defer the live session-end recap walkthrough to that ticket's QA.** **iPad:** same; deferred per device + ticket scope.                                                                                                                                                                                                                                                                                                                                                      |

---

## Survival / regression rows

| #      | Survival check                                                                                                                                                                                                                                                              | Owner             | How verified                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **15** | **Inline-SVG fallback render — `bg-garden.svg` / `flower-glyph.svg` / `sparkle-particle.svg` are still on assets-todo.md.** PR #40 ships inline fallbacks (gradient garden, 5-petal CSS flowers, gold star sparkle) so the screen reads even before Thomas drops the files. | **Desktop**       | **Auto-ish:** `Math.tsx:1051-1129` defines `SparkleGlyph` (gold star path), `FlowerGroup` (count-many `<FlowerGlyph>`), `FlowerGlyph` (5-petal pink circle ring + yellow center). Component tree mounts under `Math.test.tsx:232`'s render — no missing-component errors. **Desktop visual smoke:** in `yarn dev`, mount Math, visually confirm three things render reasonably: pastel garden background, pink-ish flower groups under the symbolic equation, gold sparkle in the HUD stardust counter. **Test-needed:** screenshot regression test (Playwright + visual diff) is overkill for v1; defer until 3+ visual regressions accumulate. |
| **16** | **Greet still works post-Math-impl (regression check).** PR #40 must NOT regress Greet's wake-tap → 4-line MP3 sequence → heart CTA flow.                                                                                                                                   | **Auto**          | **Auto:** `Greet.test.tsx` 49/49 PASS, `greetSequence.test.ts` 30/30 PASS, `useAudioUnlockGate.test.tsx` 20/20 PASS, `preRecorded.test.ts` 20/20 PASS, `tests/qa/audioAssetIntegrity.test.ts` 13/13 PASS. PR #40 added zero `src/screens/Greet*` or `src/lib/audio/*` source diffs (`gh pr view 40` confirms files are all under `src/screens/Math/`). **Desktop dev-server smoke:** `GET /src/screens/Greet.tsx` → 200, 142,320 bytes; module compiles cleanly. **Iterated full Greet flow:** deferred to Thomas's iPad pass — `qa/greet-regression.md` is the binding checklist for that.                                                      |
| **17** | **Path A endpoint still smoke-green post-merge.** `/api/claude` reachable, CORS headers correct, no `FUNCTION_INVOCATION_FAILED` regression.                                                                                                                                | **Manual / curl** | **Live:** `curl -s -X OPTIONS https://marian-learning.vercel.app/api/claude` → HTTP 204 with `Access-Control-Allow-Methods: POST, OPTIONS`, `Access-Control-Allow-Headers: Content-Type`, `Access-Control-Max-Age: 86400`, `Cache-Control: public, max-age=0, must-revalidate`. PR #40 added zero `api/` files; cannot regress. POST-path coverage relies on `qa/path-a-server-tts.md`'s prior run (`config-missing` envelope on session-start, function reachable). **POST not re-run here** to avoid spending an Anthropic API call on prod from QA — orchestrator scope was OPTIONS only.                                                     |
| **18** | **PR #39's `marian-api/require-js-extension` lint rule still catches bare imports under `api/`.** PR #40 doesn't touch `api/`, but the rule must still load + fire correctly across the rest of the repo.                                                                   | **Auto**          | **Auto:** `eslint-rules/require-js-extension.test.ts` 29/29 PASS via Vitest's `RuleTester` integration. `yarn lint` clean across the whole repo. Rule is registered as `marian-api/require-js-extension: 'error'` in `eslint.config.js:79` for `api/**/*.ts` files only. **Test-needed:** none — the rule's own unit tests + the repo-wide `yarn lint` pass are sufficient. If a future PR ever changes the rule's source or scope, this row's status survives via auto re-runs.                                                                                                                                                                 |

---

## Survival checks (lens 4 — extended)

| Check                                                                 | How                                                                                   | Expected                                                                                                                                                                                                                                                                                                                |
| --------------------------------------------------------------------- | ------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Rage-tap correct chip 5× rapidly                                      | Real iPad: tap correct answer 5 times in <500ms.                                      | Only one stardust grant. Only one auto-advance scheduled. `problemState.resolved` short-circuits subsequent taps at `Math.tsx:682`. Chip becomes `disabled` after first correct tap. **Test-needed:** `Math.test.tsx` 5-rapid-click assertion. ~15 minutes.                                                             |
| Rage-tap mid-shake (wrong chip mid-shake animation)                   | Tap wrong chip; while it's still shaking (≤400ms), tap another wrong chip.            | Both register as wrongs; `wrongCount` increments to 2; hint timer fires correctly. **Auto coverage** in `Math.test.tsx:367` covers the 2-wrong sequence (back-to-back). Mid-shake DOM doesn't gate the click — chip stays tappable. Verified via implementation reading.                                                |
| Background / resume mid-session (iPad PWA backgrounded for 5+ min)    | iPad PWA into a Math session, swipe to home, wait 5+ min, return.                     | Audio context will be suspended (iPadOS behavior). Next chip tap should re-engage `useAudioUnlockGate` (Math's `audioUnlocked = true` state survives the suspend, but the underlying Howler audio context may not — need device verification). **DEFERRED-iPad** to Thomas; cross-screen integration not unit-testable. |
| Stardust JSON corruption / first-run empty state                      | Manually set `localStorage['marian-tutor.stardust.v1'] = '{garbage'` and reload Math. | `loadStardust` returns `emptyStardust()`. Counter shows `0`. Marian's accumulated stardust is lost (acknowledged trade-off — see `stardust.ts:110` "Prefer 'she starts at 0 again' over crashing"). **Auto coverage** at `stardust.test.ts:47, :54, :61, :74` exhaustively covers all corruption paths.                 |
| Plan rotation collision (two consecutive sessions in the same minute) | Mount Math twice, ~30s apart, on the same minute boundary.                            | Same plan returned both times (deterministic per minute). Behaviour is intentional — `pickStaticSessionPlan` is per-minute-deterministic so tests can pin via `now`. **NOT A BUG** — Path A will replace this with a real Claude call when `ANTHROPIC_API_KEY` lands. Verified by reading `sessionPlans.ts:202-210`.    |

---

## Console / network sanity

- DevTools Console must show **zero** errors and zero warnings on Math mount (with the silent-default `playUtterance` and assets-still-todo SFX). `console.warn` fires from `createSfx` when SFX 404 — expected during the assets-todo era. The `Math.test.tsx` suite mocks `console.warn` to keep test logs clean (`Math.test.tsx:220`).
- Network tab on Math mount with silent-default `playUtterance`: **zero** outbound `/api/claude` requests. Math doesn't hit the backend until `App.tsx` injects a Path A `playUtterance`.
- `Application → Local Storage → marian-tutor.stardust.v1`: populates after first correct tap. Shape `{"total":N,"lastUpdatedAt":"…","schemaVersion":1}`. Survives reload (auto coverage at `Math.test.tsx:317`; desktop manual deferred per row 6).
- Service worker precache: 37 entries / 516.28 KiB after `yarn build` — same as post-PR-#25 baseline. PR #40 didn't add anything to the precache (Math's audio is per-session-dynamic via Path A; SVGs still on assets-todo would re-add when they land).

---

## Math regression list (when adding to it on future PRs)

This section will grow as Math gets follow-on work. Today, the regression surface is:

| Risk surface                          | What to re-run on PRs that touch it                                                                                                                                                                                                                                                |
| ------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/screens/Math/distractors.ts`     | `distractors.test.ts` (78 tests). Plus AC row 2 above + the `GENTLE_RAMP_THROUGH` canary. Spec change (Dave revisits the cutoff): re-run AC row 2 + update Spec Drift item A.                                                                                                      |
| `src/screens/Math/stardust.ts`        | `stardust.test.ts` (13 tests). Plus AC rows 6, 7. Schema change → bump `STARDUST_SCHEMA_VERSION`, write a migration, re-run all stardust tests + smoke a manual-corrupt-localStorage test on the iPad.                                                                             |
| `src/screens/Math/sessionPlans.ts`    | None of the existing tests directly cover `pickStaticSessionPlan`'s rotation determinism. **Test-needed:** `sessionPlans.test.ts` — assert deterministic per-minute selection + 8-problem-count invariant + sums-to-10 invariant on every problem. ~30 minutes. **Flag to Devon.** |
| `src/screens/Math/Math.tsx`           | `Math.test.tsx` (10 tests). Plus AC rows 1, 3, 4, 5, 9, 10, 11, 13, 14. Animation timing changes (e.g. tweaking `ADVANCE_AFTER_CORRECT_MS`) — re-run AC row 13 + flag to Thomas for taste.                                                                                         |
| `src/screens/Math/constants.ts`       | `STREAK_BONUS_THRESHOLDS` is exported and consumed by `Math.tsx`'s streak-bonus check. Any change → re-run AC row 3 + flag to Thomas (taste call).                                                                                                                                 |
| `src/lib/audio/useAudioUnlockGate.ts` | Math is now a consumer alongside Greet. Re-run `useAudioUnlockGate.test.tsx` + AC row 11 + survival row 16.                                                                                                                                                                        |
| `src/lib/sfx/*`                       | Math uses `createSfx` directly. Re-run `sfx.test.ts` (6 tests) + manual smoke that the warn-once-on-404 pattern still holds.                                                                                                                                                       |
| `src/App.tsx`                         | Math route wiring. Re-run `App.test.tsx` (4 tests) + manual smoke that Greet → Math transition still works.                                                                                                                                                                        |
| `design/screen-3-math.md`             | Spec drift surface. Any change → diff the spec + re-run all impacted AC rows. Keep this checklist's row text in sync.                                                                                                                                                              |

---

## Test-needed gaps to log for Kevin / Devon (follow-up tickets)

Captured during checklist authoring; do NOT block anything in PR #40 but should be in Matt's queue:

1. **`tests/qa/noFlameInUI.test.ts`** — fail the build if any source under `src/` or any asset under `public/assets/` references `flame` or `icon-flame`. Locks the Dave-PR-#38 sparkle-not-flame decision against accidental regression. ~15 minutes. **AC row 8.**
2. **`src/screens/Math/sessionPlans.test.ts`** — assert `pickStaticSessionPlan(now)` rotation determinism + every plan has exactly 8 problems + every problem's `correct === addendA + addendB ≤ 10`. Closes the regression surface around the hardcoded plans before Path A replaces them. ~30 minutes. **Math regression list row "sessionPlans".**
3. **Rage-tap regression test in `Math.test.tsx`** — fire 5 click events on the correct chip in synchronous succession; assert `data-stardust === '1'` and `onSessionComplete` not yet called (auto-advance scheduled exactly once). ~15 minutes. **Survival check "Rage-tap correct chip 5× rapidly".**
4. **Stardust withhold on guided path explicit assertion** in `Math.test.tsx` — currently the guided test (`Math.test.tsx:527`) asserts `data-guided='true'` and disabled chips, but does NOT then tap the correct chip and assert no stardust grant. Add that final tap + `data-stardust` assertion. ~10 minutes. **AC row 5.**
5. **End-to-end Path A integration test** — when `App.tsx` wires the real Path A `playUtterance` into Math, replace AC rows 10/11/12's "BLOCKED-on-key" with a real iPad walk-through. Tracking note for Matt; depends on `ANTHROPIC_API_KEY` landing in Vercel env (per `reference_deploy.md`).
6. **`tests/qa/mathScreenAccessibility.test.ts`** — assert chip `aria-label="Answer N"`, ribbon `role="status"` + `aria-live="polite"`, stardust counter `aria-label="Stardust: N"` are present. PR #40 ships all three (verified via grep at `Math.tsx:791, 887-888, 962`); a guard test would lock the contract. ~20 minutes.

---

## Out of scope for this checklist

- Session-end recap surface (ticket `86c9grnjd`, `design/screen-5-session-end.md`) — Math emits the `onSessionComplete` callback and that's it. The downstream surface is a separate ticket and a separate spec.
- Mid-session interrupt and resume (ticket `86c9grnjf`, `design/mid-session-resume.md`) — Math's contract is "8 problems then unmount"; close-and-reopen behaviour is the orchestrator's concern.
- Subtraction screen, 2-digit arithmetic, multiplication — separate tickets, post-Math.
- Stardust unlock loop / cosmetic gallery — flagged for v2 in spec §Inline answers item 4.
- Streak persistence across sessions — explicitly rejected in v1 per spec §Stardust treatment "Persistence" → "No streak persistence".
- Real Claude session-start wiring — depends on `ANTHROPIC_API_KEY` landing; tracked via `reference_deploy.md`.
- iPad real-device installability + PWA-precache integrity — Thomas's pass.
- Final voice approval — covered by `qa/path-a-server-tts.md`; Math inherits Path A's voice config.
- Removing the silent-default `playUtterance` from `Math.tsx` after Path A wires in — out of scope for QA; that's a tech-debt cleanup ticket once the env var lands.
- `design/screen-3-math.md`'s "Optional finger-counting helper visual (out of v1)" — explicitly v2.
